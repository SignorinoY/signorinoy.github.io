<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>二叉树</title>
    <url>/post/binary-tree/</url>
    <content><![CDATA[<p>在计算机科学中，二叉树（英语：Binary tree）是每个节点最多只有两个分支（即不存在分支度大于 2 的节点）的树结构。通常分支被称作“左子树”或“右子树”。二叉树的分支具有左右次序，不能随意颠倒。</p>
<span id="more"></span>
<h2 id="二叉树链式存储结构结点类">二叉树链式存储结构结点类</h2>
<p>在二叉链表结构中，二叉树中的每一个结点设置有 3 个域：数据域、左指针域、右指针域，其中，数据域用来存放结点的值；左、右指针域分别用来存放该结点的左、右孩子结点的存储地址。</p>
<p>二叉树链式存储结构的结点类的描述如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BinaryTreeNode</span> </span>&#123;</span><br><span class="line">    <span class="comment">//数据域</span></span><br><span class="line">    <span class="keyword">public</span> Object data;</span><br><span class="line">    <span class="comment">//左指针域</span></span><br><span class="line">    BinaryTree.BinaryTreeNode left;</span><br><span class="line">    <span class="comment">//右指针域</span></span><br><span class="line">    BinaryTree.BinaryTreeNode right;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BinaryTreeNode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(<span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BinaryTreeNode</span><span class="params">(Object item)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(item, <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BinaryTreeNode</span><span class="params">(Object item, BinaryTree.BinaryTreeNode left, BinaryTree.BinaryTreeNode right)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.data = item;</span><br><span class="line">        <span class="keyword">this</span>.left = left;</span><br><span class="line">        <span class="keyword">this</span>.right = right;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="二叉链式存储结构二叉树类">二叉链式存储结构二叉树类</h2>
<p>在二叉链表结构中，每一个二叉树以其根结点唯一标识。二叉树链式存储结构的二叉树类的描述如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.Queue;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BinaryTree</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> BinaryTreeNode root;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BinaryTree</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.root = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BinaryTree</span><span class="params">(BinaryTreeNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.root = root;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="建立二叉树">建立二叉树</h2>
<h3 id="由先根和中根遍历序列建立二叉树">由先根和中根遍历序列建立二叉树</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 由先根和中根序列建立一棵二叉树</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">BinaryTree</span><span class="params">(String preOrder, String inOrder)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(preOrder, inOrder, <span class="number">0</span>, <span class="number">0</span>, inOrder.length());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="title">BinaryTree</span><span class="params">(String preOrder, String inOrder, <span class="keyword">int</span> preStart, <span class="keyword">int</span> inStart, <span class="keyword">int</span> count)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (count &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">char</span> node = preOrder.charAt(preStart);</span><br><span class="line">        root = <span class="keyword">new</span> BinaryTreeNode(node);</span><br><span class="line">        <span class="keyword">int</span> nodeIndex;</span><br><span class="line">        <span class="keyword">for</span> (nodeIndex = <span class="number">0</span>; nodeIndex &lt; count; nodeIndex++)</span><br><span class="line">            <span class="keyword">if</span> (node == inOrder.charAt(inStart + nodeIndex)) <span class="keyword">break</span>;</span><br><span class="line">        root.left = <span class="keyword">new</span> BinaryTree(preOrder, inOrder, preStart + <span class="number">1</span>, inStart, nodeIndex).root;</span><br><span class="line">        root.right = <span class="keyword">new</span> BinaryTree(preOrder, inOrder, preStart + nodeIndex + <span class="number">1</span>, inStart + nodeIndex + <span class="number">1</span>, count - nodeIndex - <span class="number">1</span>).root;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="由标明空子树的先根遍历序列建立二叉树">由标明空子树的先根遍历序列建立二叉树</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 利用标明空子树的先根遍历建立一棵二叉树</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">BinaryTree</span><span class="params">(String preOrder)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(preOrder, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="title">BinaryTree</span><span class="params">(String preOrder, <span class="keyword">int</span> preIndex)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">char</span> node = preOrder.charAt(preIndex++);</span><br><span class="line">    <span class="keyword">if</span> (node != <span class="string">&#x27;#&#x27;</span>) &#123;</span><br><span class="line">        root = <span class="keyword">new</span> BinaryTreeNode(node);</span><br><span class="line">        root.left = <span class="keyword">new</span> BinaryTree(preOrder, preIndex).root;</span><br><span class="line">        root.right = <span class="keyword">new</span> BinaryTree(preOrder, preIndex).root;</span><br><span class="line">    &#125; <span class="keyword">else</span></span><br><span class="line">        root = <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>在本算法中以<code>#</code>标明为空子树</p>
</blockquote>
<h2 id="统计二叉树深度">统计二叉树深度</h2>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 返回一棵二叉树的深度</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getDepth</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> getDepth(root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">getDepth</span><span class="params">(BinaryTreeNode node)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (node == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (node.left == <span class="keyword">null</span> &amp;&amp; node.right == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> rDepth = getDepth(node.right);</span><br><span class="line">        <span class="keyword">int</span> lDepth = getDepth(node.left);</span><br><span class="line">        <span class="keyword">int</span> depth = (lDepth &gt; rDepth ? lDepth : rDepth) + <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">return</span> depth;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="统计二叉树叶结点数目">统计二叉树叶结点数目</h2>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 返回一棵二叉树的叶节点个数</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">countLeafNode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> countLeafNode(root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">countLeafNode</span><span class="params">(BinaryTreeNode node)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (node == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (node.left == <span class="keyword">null</span> &amp;&amp; node.right == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> countLeafNode(node.left) + countLeafNode(node.right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="图形化输出二叉树">图形化输出二叉树</h2>
<p>在本算法中，需要按层进行打印，但是无论是用层次遍历还是先（中、后）根遍历都无法很方便地获得每一个结点的数据与位置，但是，如果我们使用顺序结构进行存储，那么我们可以很容易地获得每一个结点的数据与所处位置，在该算法中，仍然是使用层次遍历的方法，但是对空子树使用 <code></code> 进行标识，将链式存储结构转换为顺序存储结构的具体算法如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 将二叉树链式存储结构转换为二叉树顺序存储结构</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="keyword">public</span> Object[] toSequence() &#123;</span><br><span class="line">    BinaryTreeNode node = root;</span><br><span class="line">    <span class="keyword">int</span> depth = getDepth();</span><br><span class="line">    <span class="keyword">int</span> length = (<span class="number">1</span> &lt;&lt; depth) - <span class="number">1</span>;</span><br><span class="line">    Object[] Sequence = <span class="keyword">new</span> Object[length];</span><br><span class="line">    <span class="keyword">int</span> index = <span class="number">0</span>;</span><br><span class="line">    Queue queue = <span class="keyword">new</span> LinkedList();</span><br><span class="line">    queue.offer(node);</span><br><span class="line">    Sequence[index++] = node.data;</span><br><span class="line">    <span class="keyword">while</span> (index != length) &#123;</span><br><span class="line">        node = (BinaryTreeNode) queue.poll();</span><br><span class="line">        <span class="keyword">if</span> (node.left != <span class="keyword">null</span>) &#123;</span><br><span class="line">            queue.offer(node.left);</span><br><span class="line">            Sequence[index++] = node.left.data;</span><br><span class="line">        &#125; <span class="keyword">else</span></span><br><span class="line">            Sequence[index++] = <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">        <span class="keyword">if</span> (node.right != <span class="keyword">null</span>) &#123;</span><br><span class="line">            queue.offer(node.right);</span><br><span class="line">            Sequence[index++] = node.right.data;</span><br><span class="line">        &#125; <span class="keyword">else</span></span><br><span class="line">            Sequence[index++] = <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> Sequence;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>因为二叉树知道其深度，就可以知道该二叉树的最大宽度，所以我们可以根据这一性质，来图形化输出二叉树。在该算法中，需要判断每一层的结点是否存在，根据是否存在来判断是否输出每一棵树的枝干。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 图形化输出二叉树</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">repeatPrint</span><span class="params">(<span class="keyword">int</span> count, <span class="keyword">char</span> ch)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        System.out.print(ch);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">printTree</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> depth = getDepth();</span><br><span class="line">    Object[] sequence = toSequence();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; depth; i++) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; <span class="number">1</span> &lt;&lt; i; j++) &#123;</span><br><span class="line">            repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">1</span>), <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">            System.out.print(sequence[(<span class="number">1</span> &lt;&lt; i) + j - <span class="number">1</span>]);</span><br><span class="line">            repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">1</span>) - <span class="number">1</span>, <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println();</span><br><span class="line">        <span class="keyword">if</span> (i != depth - <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; <span class="number">1</span> &lt;&lt; i; j++) &#123;</span><br><span class="line">                repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">1</span>), <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">                <span class="keyword">if</span> (!sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j - <span class="number">1</span>].toString().equals(<span class="string">&quot; &quot;</span>) || !sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j].toString().equals(<span class="string">&quot; &quot;</span>)) &#123;</span><br><span class="line">                    System.out.print(<span class="string">&#x27;|&#x27;</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    System.out.print(<span class="string">&#x27; &#x27;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">1</span>) - <span class="number">1</span>, <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (i != depth - <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; <span class="number">1</span> &lt;&lt; i + <span class="number">1</span>; j++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (j % <span class="number">2</span> == <span class="number">0</span> &amp;&amp; !sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j - <span class="number">1</span>].toString().equals(<span class="string">&quot; &quot;</span>)) &#123;</span><br><span class="line">                    repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">2</span>), <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">                    repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">2</span>), <span class="string">&#x27;-&#x27;</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (j % <span class="number">2</span> == <span class="number">0</span> &amp;&amp; sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j - <span class="number">1</span>].toString().equals(<span class="string">&quot; &quot;</span>)) &#123;</span><br><span class="line">                    repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">1</span>), <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (j % <span class="number">2</span> == <span class="number">1</span> &amp;&amp; !sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j - <span class="number">1</span>].toString().equals(<span class="string">&quot; &quot;</span>)) &#123;</span><br><span class="line">                    repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">2</span>), <span class="string">&#x27;-&#x27;</span>);</span><br><span class="line">                    repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">2</span>) - <span class="number">1</span>, <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (j % <span class="number">2</span> == <span class="number">1</span> &amp;&amp; sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j - <span class="number">1</span>].toString().equals(<span class="string">&quot; &quot;</span>)) &#123;</span><br><span class="line">                    repeatPrint((<span class="number">1</span> &lt;&lt; depth - i - <span class="number">1</span>) - <span class="number">1</span>, <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (j % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (!sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j - <span class="number">1</span>].toString().equals(<span class="string">&quot; &quot;</span>) || !sequence[(<span class="number">1</span> &lt;&lt; i + <span class="number">1</span>) + j].toString().equals(<span class="string">&quot; &quot;</span>)) &#123;</span><br><span class="line">                        System.out.print(<span class="string">&#x27;-&#x27;</span>);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        System.out.print(<span class="string">&#x27; &#x27;</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="二叉树测试类">二叉树测试类</h2>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * BinaryTree Tester.</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BinaryTreeTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testGetDepth</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        BinaryTree binaryTree = <span class="keyword">new</span> BinaryTree(<span class="string">&quot;ABDEGCFH&quot;</span>, <span class="string">&quot;DBGEAFHC&quot;</span>);</span><br><span class="line">        System.out.println(binaryTree.getDepth());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testCountLeafNode</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        BinaryTree binaryTree = <span class="keyword">new</span> BinaryTree(<span class="string">&quot;ABDEGCFH&quot;</span>, <span class="string">&quot;DBGEAFHC&quot;</span>);</span><br><span class="line">        System.out.println(binaryTree.countLeafNode());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testPrintTree</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        BinaryTree binaryTree = <span class="keyword">new</span> BinaryTree(<span class="string">&quot;ABDEGCFH&quot;</span>, <span class="string">&quot;DBGEAFHC&quot;</span>);</span><br><span class="line">        binaryTree.printTree();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">        A</span><br><span class="line">        |</span><br><span class="line">    ---------</span><br><span class="line">    B       C</span><br><span class="line">    |       |</span><br><span class="line">  -----   ---</span><br><span class="line">  D   E   F</span><br><span class="line">      |   |</span><br><span class="line">     --   --</span><br><span class="line">     G     H</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>Bootstrap</title>
    <url>/post/bootstrap/</url>
    <content><![CDATA[<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/pull-yourself-up-by-the-boot-straps.png" alt="" /><figcaption>Pull up by your own bootstraps.</figcaption>
</figure>
<span id="more"></span>
<p>在统计学中，Bootstrap 是一种从给定训练集中有放回的均匀抽样，来估计几乎任何统计量的抽样分布的方法。</p>
<h2 id="标准方差估计">标准方差估计</h2>
<p>当我们用一个统计量作为某个参数的估计量时，为考察这个估计量的好坏程度，我们需要求出这个估计量的期望以考察无偏性，方差以考察有效性等。</p>
<p>考虑这样一个例子，样本 <span class="math inline">\(X=(x_1,\dots,x_n),x_i\stackrel{iid}{\sim}N(\mu,\sigma^2)\)</span>，其分布函数为 <span class="math inline">\(F_x(x)\)</span>，密度函数为 <span class="math inline">\(p_x(x)\)</span> 在数理统计中我们知道 <span class="math inline">\(\bar{x}\sim N(\mu,\frac{\sigma^2}{n})\)</span>，从而可以得出 <span class="math inline">\(\hat{se}(\bar{x})=\sqrt{\frac{\sigma^2}{n}}=\sqrt{\frac{\sum\limits_{i=1}^n(x_i-\bar{x})^2}{n(n-1)}}\)</span>。若我们希望知道 <span class="math inline">\(Y=median(x_1,\dots,x_n)\)</span> 的标准方差估计，我们应该怎么解决呢？</p>
<p>我们知道，一个统计量除少数几个外往往形式非常复杂，很难用解析的形式求出其方差及其方差的估计，那么，我们希望是否有一种更简单的方法来获取。</p>
<p>我们回顾一下，我们为什么不能从样本 <span class="math inline">\(X=(x_1,\dots,x_n)\)</span> 得出其标准方差估计，因为只存在一组样本，所以我们只能得到一个中位数的估计值，其标准方差估计恒为 0 显然不行。那么，我们就希望获得更多的样本来计算其标准方差估计，而这种方法就是 Bootstrap 方法的思想。</p>
<p>以下，我们以 Bootstrap 方法来估计 <span class="math inline">\(Y=median(x_1,\dots,x_n)\)</span> 的标准方差估计：</p>
<ol type="1">
<li>由样本 <span class="math inline">\(X=(x_1,\dots,x_n)\)</span> 得出 <span class="math inline">\(\mu\)</span> 与 <span class="math inline">\(\sigma^2\)</span> 的估计，从而确定 <span class="math inline">\(x\)</span> 的分布为 <span class="math inline">\(N(\hat{\mu},\hat{\sigma}^2)\)</span>。</li>
<li>从 <span class="math inline">\(N(\hat{\mu},\hat{\sigma}^2)\)</span> 中生产 <span class="math inline">\(B\)</span> 组 Bootstrap 样本 <span class="math inline">\(X_b^\star(x_1^{(i)},\dots,x_n^{(i)})\quad i=(1,\dots,B)\)</span>。</li>
<li>计算得到 <span class="math inline">\(B\)</span> 个中位数样本 <span class="math inline">\(y_i=median(x_1^{(i)},\dots,x_n^{(i)})\quad i=(1,\dots,B)\)</span>。</li>
<li>计算得出 <span class="math inline">\(\hat{se}(y)=\sqrt{\frac{1}{B-1}\sum\limits_{i=1}^n(y_i-\bar{y})^2}\)</span>。</li>
</ol>
<blockquote>
<p>当然，我们可以从次序统计量的角度出发得出 <span class="math inline">\(Y\)</span> 的密度函数（这里假设 <span class="math inline">\(n\)</span> 为奇数），如下： <span class="math display">\[p_Y(y)=\frac{n!}{(\frac{n+1}{2}-1)!(n-\frac{n+1}{2})!}(F_x(y))^{\frac{n+1}{2}-1}(1-F_x(y))^{n-\frac{n+1}{2}}p_x(y)\]</span> 其形式极为复杂，但仍可以解决该问题，只要我们得出 <span class="math inline">\(\hat{F_x(x)}\)</span>，即可得出 <span class="math inline">\(Y=median(x_1,\dots,x_n)\)</span> 的标准方差估计。若我们希望得到的是样本 <span class="math inline">\(X=(x_1,\dots,x_n)\)</span> 的众数标准方差估计，我们应该怎样解决呢？显然，以自己目前所学的知识是很难找到显式解的。</p>
</blockquote>
<p>Bootstrap 方法也分非参数的 Bootstrap 和参数化的 Bootstrap，上述4个步骤实际上是参数化的 Bootstrap 过程。若样本 <span class="math inline">\(X=(x_1,\dots,x_n)\)</span> 的分布 <span class="math inline">\(F(x)\)</span> 未知（非参数）情况下，我们如何使用 Bootstrap 来进行估计呢？</p>
<p>回顾一下，我们仍然希望获得足够多的样本来进行计算，而 Bootstrap 样本是由 <span class="math inline">\(X\)</span> 的分布 <span class="math inline">\(F(x)\)</span> 生成的，那么 <span class="math inline">\(F(x)\)</span> 怎么得到呢？</p>
<p>由格里纹科定理可知，我们可以考虑用经验分布函数 <span class="math inline">\(F_n(x)\)</span> 来近似地模拟 <span class="math inline">\(F(x)\)</span>。</p>
<div class="note "><p><strong>（格里纹科定理）</strong> 设 <span class="math inline">\(x_1,\dots,x_n\)</span> 是取自总体分布函数为 <span class="math inline">\(F(x)\)</span> 的样本，<span class="math inline">\(F_n(x)\)</span> 是其经验分布函数，当 <span class="math inline">\(n\to\inf\)</span> 时，有 <span class="math display">\[P(\sup_{-\inf&lt;x&lt;\inf}|F_n(x)-F(x)|\to 0)=1\]</span></p>
</div>
<p>我们考虑如下离散分布：</p>
<table>
<thead>
<tr class="header">
<th style="text-align: center;"><span class="math inline">\(X\)</span></th>
<th style="text-align: center;"><span class="math inline">\(x_1\)</span></th>
<th style="text-align: center;"><span class="math inline">\(x_2\)</span></th>
<th style="text-align: center;"><span class="math inline">\(\dots\)</span></th>
<th style="text-align: center;"><span class="math inline">\(x_n\)</span></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;"><span class="math inline">\(p\)</span></td>
<td style="text-align: center;"><span class="math inline">\(\frac{1}{n}\)</span></td>
<td style="text-align: center;"><span class="math inline">\(\frac{1}{n}\)</span></td>
<td style="text-align: center;"><span class="math inline">\(\dots\)</span></td>
<td style="text-align: center;"><span class="math inline">\(\frac{1}{n}\)</span></td>
</tr>
</tbody>
</table>
<p>其分布函数为：</p>
<p><span class="math display">\[G(x)=P(X\leq x)=\frac{1}{n}\sum\limits_{i=1}^nI(x_i\leq x)\]</span></p>
<p>可以证明</p>
<p><span class="math display">\[x_1^{(i)},x_2^{(i)},\dots,x_n^{(i)}\stackrel{iid}{\sim}\hat{F}(x)\Leftrightarrow x_1^{(i)},x_2^{(i)},\dots,x_n^{(i)}\stackrel{iid}{\sim}G(x)\]</span></p>
<p>所以，我们只需要有放回地抽样就能使得样本服从 <span class="math inline">\(G(x)\)</span>。</p>
<p>以下，我们总结一下，如何使用 Bootstrap 方法来计算非参数情况下，任意一个统计量标准方差估计。</p>
<p>设有样本 <span class="math inline">\(X=(x_1,\dots,x_n)\)</span>，统计量 <span class="math inline">\(s(X)=s(x_1,\dots,x_n)\)</span>，求 <span class="math inline">\(s(x)\)</span> 的标准方差估计。</p>
<ol type="1">
<li>从 <span class="math inline">\(x_1,\dots,x_n\)</span> 有放回地抽样 <span class="math inline">\(n\)</span> 次，得到一个 Bootstrap 样本 <span class="math inline">\(X^{\ast,b}=(X^{\ast,1},\dots,X{\ast,n},b)\)</span>。</li>
<li>计算得到的 Bootstrap 样本的统计量 <span class="math inline">\(s(X^{\ast, b})\)</span>。</li>
<li>重复第 1 步 <span class="math inline">\(B\)</span> 次，得到 <span class="math inline">\(B\)</span> 个 Bootstrap 样本的统计量 <span class="math inline">\(s(X^{\ast,i})\quad(i=1,\dots,n)\)</span>。</li>
<li>计算标准方差估计 <span class="math inline">\(\hat{se}(s(X))=\sqrt{\frac{1}{B-1}\sum\limits_{i=1}^n(s(X)_i-\bar{s(X)})^2}\)</span>。</li>
</ol>
<p>而参数 Bootstrap 与上面的步骤类似，参数化的 Bootstrap 假设 <span class="math inline">\(F\)</span> 是参数模型。因此，参数化的 Bootstrap 比非参数化的多了一个步骤：利用数据估计估计参数 <span class="math inline">\(\hat{\theta}\)</span>，从而用 <span class="math inline">\(f(x:\hat{\theta})\)</span> 来获得 Bootstrap 样本。</p>
<p>其实，不仅可以用 Bootstrap 来估计方差，也可以用来估计CDF、偏差、置信区间等，其思想是一致的。</p>
]]></content>
      <categories>
        <category>统计学</category>
      </categories>
      <tags>
        <tag>Bootstrap</tag>
      </tags>
  </entry>
  <entry>
    <title>C# 语言 —— C# 语法基础</title>
    <url>/post/c-sharp-basic-grammar/</url>
    <content><![CDATA[<p>C#，与其他 C 风格的语言一样</p>
<ul>
<li>语句都以分号（<code>;</code>）结尾</li>
<li>语句可以写在多个代码行上，不需要使用续行字符</li>
<li>单行注释 （<code>//</code>），多行注释（<code>\**\</code>）</li>
<li>区分大小写</li>
</ul>
<span id="more"></span>
<h2 id="变量">变量</h2>
<p>在C#中声明变量使用如下语法：</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line">datatype identifier;</span><br></pre></td></tr></table></figure>
<ul>
<li>声明变量之后，可以用赋值运算符（<code>=</code>）为它赋值；可以在一行代码中，同时声明变量，并初始化它的值；只能在一条语句中声明具有相同数据类型的变量</li>
<li>在C#中必须初始化变量</li>
<li>类型推断使用 <code>var</code> 关键字，变量必须初始化；且在推断出了类型后，就不能改变变量的类型</li>
<li>注意变量的作用域</li>
<li>在变量的前面加上关键字 <code>const</code>，就可以把该变量指定为一个常量，常量必须在声明时初始化；常量总是隐式静态的，不必在常量声明中包含修饰符 <code>static</code></li>
</ul>
<h2 id="预定义数据类型">预定义数据类型</h2>
<p>值类型直接存储其值，值类型存储在堆栈中；引用类型存储对值的引用，引用类型存储在托管堆上。</p>
<h3 id="值类型">值类型</h3>
<ul>
<li>整型（<code>sbyte</code>，<code>short</code>，<code>int</code>，<code>long</code>，<code>byte</code>，<code>ushort</code>，<code>uint</code>，<code>ulong</code>）</li>
<li>浮点类型（<code>double</code>，<code>float</code>）</li>
<li>decimal 类型（精度更高的浮点数，专用于财务计算的类型）</li>
<li>bool 类型（只能使用值<code>true</code>，<code>false</code>）</li>
<li>字符类型（字面量需要用单引号括起来，并且可以用 4 位十六进制的 Unicode 值（<code>'\u0041'</code>）、带有强制类型转换的整数值（<code>(char)65</code>）、十六进制数（<code>'\x0041'</code>）、转义序列）</li>
</ul>
<h3 id="引用类型">引用类型</h3>
<ul>
<li>object 类型（？）</li>
<li>string 类型（字面量放在双引号中，可以包含 Unicode 和十六进制数转义序列。C# 中可以用 <code>@</code> 在这个字符后所有字符看成表示原来的含义；字符串插值格式，用 <code>$</code> 前缀来标记）</li>
</ul>
<h2 id="程序流控制">程序流控制</h2>
<ul>
<li>条件语句</li>
<li>循环</li>
<li>跳转</li>
</ul>
<h2 id="名称空间">名称空间</h2>
<p>名称空间可以提供了一种组织相关类和其他类型的方式。在 C# 中定义类时，可以把它包括在名称空间中，以后，在定义另一个类时，就可以把它包括在同一个名称空间中，创建一个逻辑组合，告诉使用类的其他开发人员，这两个类是如何相关的以及如何使用它们。</p>
<h2 id="使用注释">使用注释</h2>
<h3 id="源文件中的内部注释">源文件中的内部注释</h3>
<p>单行注释使用 <code>//</code>，多行注释使用 <code>/**/</code>。</p>
<h3 id="xml-文档">XML 文档</h3>
<p>C# 有一个非常出色的功能，可根据特定的注释自动创建 XML 格式的文档说明。这些注释都是单行注释，但都以三条斜杠开头。</p>
<p>编译器可以识别的标记见<span class="exturl" data-url="aHR0cHM6Ly9kb2NzLm1pY3Jvc29mdC5jb20vemgtY24vZG90bmV0L2NzaGFycC9wcm9ncmFtbWluZy1ndWlkZS94bWxkb2MvcmVjb21tZW5kZWQtdGFncy1mb3ItZG9jdW1lbnRhdGlvbi1jb21tZW50cw==">参考文档<i class="fa fa-external-link-alt"></i></span>.</p>
<h2 id="c-预处理器指令">C# 预处理器指令</h2>
<p>C# 还有许多名为“预处理器指令”的命令。这些命令从来不会转化为可执行代码中的命令。但会影响编译过程中的各个方面。例如，使用预处理器指令可以禁止编译器编译代码的某一部分。</p>
<h3 id="define-和-undef"><code>#define</code> 和 <code>#undef</code></h3>
<p><code>#define</code> 的用法如下：</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DEBUG</span></span><br></pre></td></tr></table></figure>
<p>它告诉编译器存在给定名称的符号，类似与声明一个变量，但这个变量并没有真正的值，只是存在而已。只在编译器编译代码时存在，在 C# 代码中并没有任何意义。</p>
<p><code>#undef</code>正好相反——它删除符号的意义：</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">undef</span> DEBUG</span></span><br></pre></td></tr></table></figure>
<p>如果符号不存在，<code>#undef</code> 没有任何意义；同样，如果符号存在，则 <code>#define</code> 不起作用。</p>
<p>必须把 <code>#define</code> 和 <code>#undef</code> 放在 C# 源文件的开头位置，在声明药编译的任何对象的代码之前。</p>
<h3 id="ifelifelse-和-endif"><code>#if</code>、<code>#elif</code>、<code>#else</code> 和 <code>#endif</code></h3>
<p>这些指令告诉编译器是否要编译代码块。<code>#if</code> 和 <code>#elif</code> 还支持逻辑运算符。</p>
<h3 id="warning-和-error"><code>#warning</code> 和 <code>#error</code></h3>
<p>如果编译器遇到 <code>#warning</code> 指令，会向用户显示 <code>#warning</code> 指令后面的文本，之后编译继续进行。如果编译器遇到 <code>#error</code> 指令，就会向用户显示 <code>#error</code> 后面的文本，作为一条编译错误信息，然后会立即退出编译，不会生成 IL 代码。</p>
<h3 id="region-和-endregion"><code>#region</code> 和 <code>endregion</code></h3>
<p><code>#region</code> 和 <code>endregion</code> 指令用于把一段代码视为有给定名称的一个块，如下所示：</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">region</span> member field declarations</span></span><br><span class="line">  <span class="built_in">int</span> x;</span><br><span class="line">  stinng y;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endregion</span></span></span><br></pre></td></tr></table></figure>
<h3 id="line"><code>#line</code></h3>
<p><code>#line</code> 指令用于改变编译器在警告和错误信息中显示的文件名和行号信息。</p>
<h3 id="pragma"><code>#pragma</code></h3>
<p><code>#pragma</code> 指令可以抑制或还原指定的编译警告。与命令行选项不同，指令可以在类或方法级别实现，对抑制警告的内容和抑制的时间进行更精确的控制。</p>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
      <tags>
        <tag>C#</tag>
      </tags>
  </entry>
  <entry>
    <title>中文文档排版指南</title>
    <url>/post/chinese-document-style-guide/</url>
    <content><![CDATA[<h2 id="标题">标题</h2>
<ol type="1">
<li>一级标题下，不能直接出现三级标题。</li>
<li>标题要避免孤立编号（即同级标题只有一个）。</li>
<li>下级标题不重复上一级标题的名字。</li>
<li>谨慎使用四级标题，尽量避免出现，保持层级的简单，防止出现过于复杂的章节。如果三级标题下有并列性的内容，建议只使用项目列表（Item list）。</li>
</ol>
<span id="more"></span>
<h2 id="段落">段落</h2>
<ul>
<li>一个段落只能有一个主题，或一个中心句子。</li>
<li>段落的中心句子放在段首，对全段内容进行概述。后面陈述的句子为核心句服务。</li>
<li>一个段落的长度不能超过七行，最佳段落长度小于等于四行。</li>
<li>段落的句子语气要使用陈述和肯定语气，避免使用感叹语气。</li>
<li>段落之间使用一个空行隔开。</li>
<li>段落开头不要留出空白字符。</li>
</ul>
<h2 id="文本">文本</h2>
<h3 id="名词">名词</h3>
<ol type="1">
<li>专有名词使用正确的大小写。</li>
<li>不要使用不地道的缩写。</li>
</ol>
<h3 id="句子">句子</h3>
<ol type="1">
<li>避免使用长句。</li>
<li>尽量使用简单句和并列句，避免使用复合句。</li>
<li>同样一个意思，尽量使用肯定句表达，不使用否定句表达。</li>
<li>避免使用双重否定句。</li>
</ol>
<h3 id="写作风格">写作风格</h3>
<ol type="1">
<li>尽量不使用被动语态，改为使用主动语态。</li>
<li>不使用非正式的语言风格。</li>
<li>不使用冷僻、生造或者文言文的词语，而要使用现代汉语的常用表达方式。</li>
<li>用对“的”、“地”、“得”。</li>
<li>使用代词时（比如“其”、“该”、“此”、“这”等词），必须明确指代的内容，保证只有一个含义。</li>
<li>名词前不要使用过多的形容词。</li>
</ol>
<h3 id="英文处理">英文处理</h3>
<ol type="1">
<li>英文原文如果使用了复数形式，翻译成中文时，应该将其还原为单数形式。</li>
<li>外文缩写可以使用半角圆点(<code>.</code>)表示缩写。</li>
<li>表示中文时，英文省略号（<code>⋯</code>）应改为中文省略号（<code>……</code>）。</li>
<li>英文书名或电影名改用中文表达时，双引号应改为书名号。</li>
<li>第一次出现英文词汇时，在括号中给出中文标注。此后再次出现时，直接使用英文缩写即可。</li>
<li>专有名词中每个词第一个字母均应大写，非专有名词则不需要大写。</li>
</ol>
<h2 id="标点">标点</h2>
<ol type="1">
<li>中文语句的标点符号，均应该采取全角符号，这样可以与全角文字保持视觉的一致。</li>
<li>如果整句为英文，则该句使用英文/半角标点。</li>
<li>句号、问号、叹号、逗号、顿号、分号和冒号不得出现在一行之首。</li>
</ol>
<h3 id="空格">空格</h3>
<ol type="1">
<li>中文与英文之间、中文与数字之间、英文与数字之间增加空格。</li>
<li>一段文字中有超链接的部分，在超链接的前后增加空格。</li>
<li>数字与单位之间、全角标点与其他字符之间不加空格。</li>
<li>对于有特殊用法的专有名词，如 4K、1080p、iOS 10 等，英文和数字之间是否空格以官方标准为准。</li>
</ol>
<h2 id="样式">样式</h2>
<h3 id="latex-公式">LaTeX 公式</h3>
<h3 id="笔记">笔记</h3>
<h3 id="脚注">脚注</h3>
<h2 id="参考文献">参考文献</h2>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3J1YW55Zi9kb2N1bWVudC1zdHlsZS1ndWlkZQ==">ruanyf/document-style-guide：中文技术文档的写作规范<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3NwYXJhbm9pZC9jaGluZXNlLWNvcHl3cml0aW5nLWd1aWRlbGluZXM=">sparanoid/chinese-copywriting-guidelines：中文文案排版指北<i class="fa fa-external-link-alt"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
  </entry>
  <entry>
    <title>描述性时序分析</title>
    <url>/post/descriptive-time-series-analysis/</url>
    <content><![CDATA[<p>早期的时序分析通常都是通过直观的数据比较或绘图观测，寻找序列中蕴涵的发展规律，这种分析方法就称为描述性时序分析。操作简单、直观有效是描述性时间序列分析方法的突出特点。它通常也是人们进行统计时序分析的第一步，通过图示的方法可以直观地反映出序列的特征。</p>
<span id="more"></span>
<h2 id="数据准备">数据准备</h2>
<p>下载数据集 <span class="exturl" data-url="aHR0cHM6Ly9kYXRhbWFya2V0LmNvbS9kYXRhL3NldC8yMzI0L2RhaWx5LW1pbmltdW0tdGVtcGVyYXR1cmVzLWluLW1lbGJvdXJuZS1hdXN0cmFsaWEtMTk4MS0xOTkwIw==">Daily minimum temperatures in Melbourne<i class="fa fa-external-link-alt"></i></span>。这个数据集描述了澳大利亚墨尔本市十年（1981-1990）来的最低日温。单位是摄氏度，有3650个观测值。数据的来源是澳大利亚气象局。</p>
<p>尝试使用 <code>read.table</code> 将数据读入 R，并存为 <code>temp_data</code>。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">temp_data &lt;- read.table(<span class="string">&quot;daily-minimum-temperatures-in-melbourne.csv&quot;</span>, header = <span class="literal">TRUE</span>, sep = <span class="string">&quot;,&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>查看 <code>temp_data</code> 内部结构，发现 <code>temp_data$Date</code> 为 <code>Factor</code> 数据类型，无法直接转换为时间序列，且最低温度的列名较长，可在后续步骤中对以上两点进行修改。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">str(temp_data)</span><br></pre></td></tr></table></figure>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;data.frame&#x27;</span>:    <span class="number">3650</span> obs. of  <span class="number">2</span> variables:</span><br><span class="line"> $ Date                                                         : Factor w/ <span class="number">3650</span> levels <span class="string">&quot;1981/1/1&quot;</span>,<span class="string">&quot;1981/1/10&quot;</span>,..: <span class="number">1</span> <span class="number">12</span> <span class="number">23</span> <span class="number">26</span> <span class="number">27</span> <span class="number">28</span> <span class="number">29</span> <span class="number">30</span> <span class="number">31</span> <span class="number">2</span> ...</span><br><span class="line"> $ Daily.minimum.temperatures.in.Melbourne..Australia..1981.1990: num  <span class="number">20.7</span> <span class="number">17.9</span> <span class="number">18.8</span> <span class="number">14.6</span> <span class="number">15.8</span> <span class="number">15.8</span> <span class="number">15.8</span> <span class="number">17.4</span> <span class="number">21.8</span> <span class="number">20</span> ...</span><br></pre></td></tr></table></figure>
<p>对 <code>Daily.minimum.temperatures.in.Melbourne..Australia..1981.1990</code> 列重命名为 <code>MinTemp</code>。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line"><span class="built_in">names</span>(temp_data)[<span class="built_in">names</span>(temp_data)==<span class="string">&quot;Daily.minimum.temperatures.in.Melbourne..Australia..1981.1990&quot;</span>]=<span class="string">&quot;MinTemp&quot;</span></span><br></pre></td></tr></table></figure>
<p>R 语言无法自动将读取的数据转化为时间序列格式，所以利用 R 语言画时间序列图的一个关键步骤就是将读取的数据转变为时间序列格式。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">temp_data$Date &lt;- as.Date(temp_data$Date, format=<span class="string">&#x27;%Y/%m/%d&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>加载 <code>xts</code> 包将 <code>temp_data</code> 转换为时间序列的数据类型。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">library(xts)</span><br><span class="line">temp_data &lt;- xts(temp_data$MinTemp, temp_data$Date)</span><br></pre></td></tr></table></figure>
<h2 id="数据可视化">数据可视化</h2>
<p>加载 <code>ggplot2</code> 包来对 <code>temp_data</code> 进行数据可视化。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">library(ggplot2)</span><br></pre></td></tr></table></figure>
<h3 id="时间序列线图">时间序列线图</h3>
<blockquote>
<p>时间序列的首选也可能是最流行的可视化方法是线图，即时间显示在 <span class="math inline">\(x\)</span> 轴上，而观察值则沿着 <span class="math inline">\(y\)</span> 轴的。</p>
</blockquote>
<p>Q： 试将最低日温数据集直观地可视化为线图的示例。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">Date &lt;- index(temp_data)</span><br><span class="line">MinTemp &lt;- temp_data</span><br><span class="line">ggplot(temp_data, aes(Date, MinTemp)) + geom_line()</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/time-series-analysis-descriptive-analysis-1.png" alt="" /><figcaption>时间序列线图</figcaption>
</figure>
<p>从图中可以看出，墨尔本市十年来的最低日温具有明显的周期性波动。</p>
<h3 id="时间序列直方图和密度图">时间序列直方图和密度图</h3>
<blockquote>
<p>另一个重要的可视化是关于观察值本身的分布。这意味着一个没有时间顺序仅观测值本身的图像。一些线性时间序列预测方法假定观测值具有良好的分布（即钟形曲线或正态分布）。这可以使用统计假设检验等工具来明确地检查。但是，对于原始观测值和经任何类型的数据转换后的值，图像也能提供对其分布进行有用的初次检查。</p>
</blockquote>
<p>Q：试画出最低日温数据集中创建观测值的直方图。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">ggplot(MinTemp, aes(MinTemp)) + geom_histogram()</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/time-series-analysis-descriptive-analysis-2.png" alt="" /><figcaption>时间序列直方图</figcaption>
</figure>
<p>从图中可以看出，墨尔本市十年来的最低日温大致服从于正态分布。</p>
<h3 id="时间序列区间箱线图">时间序列区间箱线图</h3>
<blockquote>
<p>直方图和密度图显示了所有观测值的分布情况，但我们可能对一定时间区间内值的分布感兴趣。另一种有助于总结观察值分布的图像是箱线图。可以创建箱线图，并按时间序列（如年，月，日）对每个时间区间进行比较。</p>
</blockquote>
<p>Q：试将最低日温数据集按年分组，然后对每年的数据创建一个箱须图像，并排列在一起以直接比较。</p>
<p>加载 <code>lubridate</code> 包来获取每一个日期的年份，从而可以进行按年分组。</p>
<figure class="highlight r"><table><tr><td class="code"><pre><span class="line">library(lubridate)</span><br><span class="line">year &lt;- factor(year(Date))</span><br><span class="line">ggplot(temp_data, aes(year, MinTemp)) + geom_boxplot()</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/time-series-analysis-descriptive-analysis-3.png" alt="" /><figcaption>时间序列区间箱线图</figcaption>
</figure>
]]></content>
      <categories>
        <category>时间序列分析</category>
      </categories>
      <tags>
        <tag>描述性时序分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 入门指南</title>
    <url>/post/docker-tutorial/</url>
    <content><![CDATA[<p>Docker 是一个开源的应用容器引擎，让开发者可以打包他们的应用以及依赖包到一个可移植的容器中，然后发布到任何流行的 Linux 机器或 Windows 机器上，也可以实现虚拟化，容器是完全使用沙箱机制，相互之间不会有任何接口。</p>
<span id="more"></span>
<h2 id="安装">安装</h2>
<p>使用官方安装脚本自动安装：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun</span><br></pre></td></tr></table></figure>
<h2 id="实例">实例</h2>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker network create -d bridge application</span><br></pre></td></tr></table></figure>
<h3 id="mysql-mariadb">MySql/ MariaDB</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run --detach \</span><br><span class="line">    --name mysql \</span><br><span class="line">    --restart always \</span><br><span class="line">    --publish 3306:3306 \</span><br><span class="line">    --volume /data/mysql/conf:/etc/mysql \</span><br><span class="line">    --volume /data/mysql/logs:/var/log/mysql \</span><br><span class="line">    --volume /data/mysql/data:/var/lib/mysql \</span><br><span class="line">    --network application \</span><br><span class="line">    -e MYSQL_ROOT_PASSWORD=my-secret-pw \</span><br><span class="line">    mysql:latest</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run --detach \</span><br><span class="line">    --name mariadb \</span><br><span class="line">    --restart always \</span><br><span class="line">    --publish 3306:3306 \</span><br><span class="line">    --volume /data/mysql/conf:/etc/mysql \</span><br><span class="line">    --volume /data/mysql/logs:/var/log/mysql \</span><br><span class="line">    --volume /data/mysql/data:/var/lib/mysql \</span><br><span class="line">    --network application \</span><br><span class="line">    -e MYSQL_ROOT_PASSWORD=my-secret-pw \</span><br><span class="line">    mariadb:latest</span><br></pre></td></tr></table></figure>
<h3 id="mongodb">MongoDB</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run --detach \</span><br><span class="line">    --name mongo \</span><br><span class="line">    --restart always \</span><br><span class="line">    --publish 27017:27017 \</span><br><span class="line">    --volume /data/mongo/data:/data/db \</span><br><span class="line">    --network application \</span><br><span class="line">    mongo:latest</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">use admin</span><br><span class="line">db.createUser( &#123; user: &quot;admin&quot;, pwd: &quot;my-secret-pw&quot;, roles: [ &#123; role: &quot;root&quot;, db: &quot;admin&quot; &#125; ] &#125; )</span><br></pre></td></tr></table></figure>
<h3 id="wordpress">WordPress</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">create database wordpress;</span><br><span class="line">create user &#x27;wordpress&#x27;@&#x27;wordpress.application&#x27; identified by &#x27;wordpress&#x27;;</span><br><span class="line">grant all privileges on wordpress.* to &#x27;wordpress&#x27;@&#x27;wordpress.application&#x27; identified by &#x27;wordpress&#x27;;</span><br><span class="line">flush privileges;</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -detach \</span><br><span class="line">    --name wordpress \</span><br><span class="line">    --restart always \</span><br><span class="line">    --publish 81:80 \</span><br><span class="line">    --network application \</span><br><span class="line">    wordpress:latest</span><br></pre></td></tr></table></figure>
<h3 id="nextcloud">NextCloud</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">create database nextcloud;</span><br><span class="line">create user &#x27;nextcloud&#x27;@&#x27;nextcloud.application&#x27; identified by &#x27;nextcloud&#x27;;</span><br><span class="line">grant all privileges on nextcloud.* to &#x27;nextcloud&#x27;@&#x27;nextcloud.application&#x27; identified by &#x27;nextcloud&#x27;;</span><br><span class="line">flush privileges;</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -detach \</span><br><span class="line">    --name nextcloud \</span><br><span class="line">    --restart always \</span><br><span class="line">    --publish 82:80 \</span><br><span class="line">    --volume /data/nextcloud:/var/www/html \</span><br><span class="line">    --network application \</span><br><span class="line">    nextcloud:latest</span><br></pre></td></tr></table></figure>
<h3 id="gitea">Gitea</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">create database gitea;</span><br><span class="line">create user &#x27;gitea&#x27;@&#x27;gitea.application&#x27; identified by &#x27;gitea&#x27;;</span><br><span class="line">grant all privileges on gitea.* to &#x27;gitea&#x27;@&#x27;gitea.application&#x27; identified by &#x27;gitea&#x27;;</span><br><span class="line">flush privileges;</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run --detach \</span><br><span class="line">    --name gitea \</span><br><span class="line">    --restart always \</span><br><span class="line">    --publish 83:3000 --publish 84:22 \</span><br><span class="line">    --volume /data/gitea:/data \</span><br><span class="line">    --network application \</span><br><span class="line">    gitea/gitea:latest</span><br></pre></td></tr></table></figure>
<h3 id="nodebb">NodeBB</h3>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">use nodebb</span><br><span class="line">db.createUser( &#123; user: &quot;nodebb&quot;, pwd: &quot;nodebb&quot;, roles: [ &#123; role: &quot;readWrite&quot;, db: &quot;nodebb&quot; &#125;, &#123; role: &quot;clusterMonitor&quot;, db: &quot;admin&quot; &#125; ] &#125; )</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run --detach \</span><br><span class="line">    --name nodebb \</span><br><span class="line">    --restart always \</span><br><span class="line">    --publish 4567:4567 \</span><br><span class="line">    --network application \</span><br><span class="line">    nodebb/docker:latest</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
  </entry>
  <entry>
    <title>EM 算法</title>
    <url>/post/em-algorithm/</url>
    <content><![CDATA[<p>最大期望算法（Expectation-maximization algorithm，又译期望最大化算法）在统计中被用于寻找，依赖于不可观察的隐性变量的概率模型中，参数的最大似然估计。</p>
<p>在统计计算中，最大期望（EM）算法是在概率模型中寻找参数最大似然估计或者最大后验估计的算法，其中概率模型依赖于无法观测的隐变量。最大期望算法经常用在机器学习和计算机视觉的数据聚类（Data Clustering）领域。最大期望算法经过两个步骤交替进行计算，第一步是计算期望（E），利用对隐藏变量的现有估计值，计算其最大似然估计值；第二步是最大化（M），最大化在 E 步上求得的最大似然值来计算参数的值。M 步上找到的参数估计值被用于下一个E步计算中，这个过程不断交替进行。<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<span id="more"></span>
<h3 id="gaussian-mixture-model">Gaussian Mixture Model</h3>
<p>考虑 Gaussian Mixture Model：<span class="math inline">\(X\sim qN(\mu_1,\sigma_1^2)+(1-q)N(\mu_2,\sigma_2^2)\)</span>，请用 EM 算法推导 <span class="math inline">\(\theta=\left(q,\mu_1,\mu_2,\sigma_1^2,\sigma_2^2\right)\)</span> 的估计，并用数据求得其估计值。</p>
<p>为引入 EM 算法，我们通过引入一个隐变量 <span class="math inline">\(Z\)</span> 来描述 Gaussian Mixture Model，其中：</p>
<p><span class="math display">\[x\mid(z=1)\sim N(\mu_1,\sigma_1^2),x\mid(z=2)\sim N(\mu_2,\sigma_2^2).\]</span></p>
<p><span class="math display">\[P(Z=1)=\tau_1=q,P(Z=2)=\tau_2=1-q.\]</span></p>
<p>所以：<span class="math inline">\(p(x)=\sum_zp(z)p(x\mid z)\)</span></p>
<p>为估计 Gaussian Mixture Model 的参数 <span class="math inline">\(\theta=\left(q,\mu_1,\mu_2,\sigma_1^2,\sigma_2^2\right)\)</span>，写出完整数据的对数似然函数如下：</p>
<p><span class="math display">\[\log{L(\theta;\mathbf{x},\mathbf{z})}=\log{p(\mathbf{x},\mathbf{z}\mid\theta)}=\sum_{i=1}^{n}\log{p(x_i,z_i\mid\theta)}\]</span></p>
<p><span class="math display">\[\because p(x_i,z_i)=p(z_i)p(x_i\mid z_i)=\prod_{j=1}^{2}\left[f(x_i; \mu_j,\sigma_j)\tau_j\right]^{\mathbb{I}(z_i=j)}\]</span></p>
<p><span class="math display">\[\begin{aligned}\therefore\log{L(\theta;\mathbf{x},\mathbf{z})}&amp;=\log{p(\mathbf{x},\mathbf{z}\mid\theta)}\\&amp;=\sum_{i=1}^n\sum_{j=1}^{2}\mathbb{I}(z_i=j)\log{\left[f(x_i; \mu_j,\sigma_j)\tau_j\right]}\\&amp;=\sum_{i=1}^n\sum_{j=1}^{2}\mathbb{I}(z_i=j)\left[\log{\tau_j-\frac{1}{2}\log{\sigma_j^2}-\frac{(x_i-\mu_j)^2}{2\sigma_j^2}-\frac{1}{2} \log{(2\pi)}}\right]\end{aligned}\]</span></p>
<p><strong>E-Step：</strong> 在已有观测数据 <span class="math inline">\(\mathbf{x}\)</span> 及第 <span class="math inline">\(t\)</span> 步估计值 <span class="math inline">\(\theta=\theta^{(t)}\)</span> 的条件下，求出基于完全数据的对数似然函数的期望。</p>
<p><span class="math display">\[\begin{aligned}Q_{\theta^{(t)}}(\theta) &amp;= E_{\theta^{(t)}}\left[\log{L(\theta; \mathbf{x},\mathbf{z})} \mid \mathbf{x} \right]\\&amp;= E_{\theta^{(t)}} \left\{\sum_{i=1}^n\sum_{j=1}^{2} \mathbb{I}(z_i=j) \left[\log{\tau_j - \frac{1}{2} \log{\sigma_j^2} - \frac{(x_i-\mu_j)^2}{2\sigma_j^2} - \frac{1}{2} \log{(2\pi)}}\right] \mid \mathbf{x}\right\}\\&amp;= \sum_{i=1}^n E_{\theta^{(t)}} \left\{\sum_{j=1}^{2} \mathbb{I}(z_i=j) \left[\log{\tau_j - \frac{1}{2} \log{\sigma_j^2} - \frac{(x_i-\mu_j)^2}{2\sigma_j^2} - \frac{1}{2} \log{(2\pi)}}\right] \mid x_i\right\}\\&amp;= \sum_{i=1}^n \sum_{j=1}^{2} \left[\log{\tau_j - \frac{1}{2} \log{\sigma_j^2} - \frac{(x_i-\mu_j)^2}{2{\sigma_j}^2} - \frac{1}{2} \log{(2\pi)}}\right] E_{\theta^{(t)}} \left[ \mathbb{I}(z_i=j) \mid x_i\right]\end{aligned}\]</span></p>
<p><span class="math display">\[\because T_{j,i}^{(t)} := P(Z_i=j \mid X_i=x_i;\theta^{(t)})=\frac{\tau_j^{(t)}f(x_i;\mu_j^{(t)},{\sigma_j^2}^{(t)})}{\tau_1^{(t)}f(x_i;\mu_1^{(t)},{\sigma_1^2}^{(t)})+\tau_2^{(t)}f(x_i;\mu_2^{(t)},{\sigma_2^2}^{(t)})}.\]</span></p>
<p><span class="math display">\[\therefore E_{\theta^{(t)}} \left[ \mathbb{I}(z_i=j) \mid x_i\right] = T_{j,i}^{(t)}\]</span></p>
<p><span class="math display">\[\therefore Q_{\theta^{(t)}}(\theta) = \sum_{i=1}^n \sum_{j=1}^{2} \left[\log{\tau_j - \frac{1}{2} \log{\sigma_j^2} - \frac{(x_i-\mu_j)^2}{2{\sigma_j}^2} - \frac{1}{2} \log{(2\pi)}}\right] T_{j,i}^{(t)}\]</span></p>
<p><strong>M-Step：</strong> 求 <span class="math inline">\(Q_{\theta^{(t)}}(\theta)\)</span> 关于 <span class="math inline">\(\theta\)</span> 的最大值 <span class="math inline">\(\theta^{(t+1)}\)</span>，即找到<span class="math inline">\(\theta^{(t+1)}\)</span> 使得</p>
<p><span class="math display">\[\theta^{(t+1)} = {\arg\max}_\theta Q_{\theta^{(t)}}(\theta).\]</span></p>
<p>首先，我们考虑 <span class="math inline">\(\boldsymbol{\tau}\)</span> ，其中 <span class="math inline">\(\tau_1+\tau_2=1\)</span>：</p>
<p><span class="math display">\[\begin{aligned}\boldsymbol{\tau}^{(t+1)} &amp;= {\arg\max}_{\boldsymbol{\tau}} Q_{\theta^{(t)}}(\theta)\\&amp;= {\arg\max}_{\boldsymbol{\tau}} \left\{\left[\sum_{i=1}^n T_{1,i}^{(t)}\right]\log\tau_1 + \left[\sum_{i=1}^n T_{2,i}^{(t)}\right]\log\tau_2\right\}\end{aligned}\]</span></p>
<p>将 <span class="math inline">\(\tau_2\)</span> 用 <span class="math inline">\(\tau_1\)</span> 代换后，对 <span class="math inline">\(\tau_1\)</span> 求偏导可得：</p>
<p><span class="math display">\[\frac{\partial Q_{\theta^{(t)}}(\theta)}{\partial\tau_1} = \frac{\sum_{i=1}^n T_{1,i}^{(t)}}{\tau_1} - \frac{\sum_{i=1}^n T_{2,i}^{(t)}}{1-\tau_1} = 0 \quad (\tau_1+\tau_2=1)\]</span></p>
<p><span class="math display">\[\therefore \tau_1^{(t+1)} = \frac{\sum_{i=1}^{n} T_{1,i}^{(t)}}{\sum_{i=1}^{n}\left(T_{1,i}^{(t)}+T_{2,i}^{(t)}\right)}=\frac{1}{n} \sum_{i=1}^{n} T_{1,i}^{(t)} \quad \tau_2^{(t+1)} = 1 - \tau_1^{(t+1)} = \frac{1}{n} \sum_{i=1}^{n} T_{2,i}^{(t)}.\]</span></p>
<p>接着我们考虑 <span class="math inline">\((\mu_1,\sigma_1^2)\)</span>：</p>
<p><span class="math display">\[\begin{aligned}\left(\mu_1^{(t+1)},{\sigma_1^2}^{(t+1)}\right) &amp;= {\arg\max}_{\mu_1,\sigma_1^2} Q\left(\theta | \theta^{(t)}\right) \\&amp;= {\arg\max}_{\mu_1,\sigma_1^2} \sum_{i=1}^{n} T_{1,i}^{(t)}\left\{-\frac{1}{2} \log {\sigma_1} - \frac{(x_i-\mu_1)^2}{2{\sigma_1}^2}\right\}\end{aligned}\]</span></p>
<p>对 <span class="math inline">\(\mu_1,\sigma_1^2\)</span> 分别求偏导可得：</p>
<p><span class="math display">\[\frac{\partial Q_{\theta^{(t)}}(\theta)}{\partial\mu_1} = \sum_{i=1}^{n} T_{1,i}^{(t)}\frac{x_i-\mu_i}{\sigma_1^2} = 0\]</span></p>
<p><span class="math display">\[\frac{\partial Q_{\theta^{(t)}}(\theta)}{\partial\sigma_1^2} = \sum_{i=1}^{n} T_{1,i}^{(t)}\left[-\frac{1}{2\sigma_1^2} + \frac{(x_i-\mu_1)^2}{2\sigma_1^4}\right] = 0\]</span></p>
<p><span class="math display">\[\therefore \mu_1^{(t+1)} = \frac{\sum_{i=1}^n T_{1,i}^{(t)} x_i}{\sum_{i=1}^n T_{1,i}^{(t)}} \quad {\sigma_1^2}^{(t+1)} = \frac{\sum_{i=1}^n T_{1,i}^{(t)} (x_i - \mu_1^{(t+1)})^2}{\sum_{i=1}^n T_{1,i}^{(t)}}.\]</span></p>
<p>最后，我们考虑 <span class="math inline">\((\mu_2,\sigma_2^2)\)</span>，其求解方法与上述相同：</p>
<p><span class="math display">\[\therefore \mu_2^{(t+1)} = \frac{\sum_{i=1}^n T_{2,i}^{(t)} x_i}{\sum_{i=1}^n T_{2,i}^{(t)}} \quad {\sigma_2^2}^{(t+1)} = \frac{\sum_{i=1}^n T_{2,i}^{(t)} (x_i - \mu_2^{(t+1)})^2}{\sum_{i=1}^n T_{2,i}^{(t)}}.\]</span></p>
<p><strong>终止：</strong> 重复 E-Step 与 M-Step 直至收敛，即，对于给定的 <span class="math inline">\(\varepsilon&gt;0\)</span> ，<span class="math inline">\(Q_{\theta^{(t)}}(\theta) - Q_{\theta^{(t-1)}}(\theta) \leq \varepsilon\)</span>，即可得到 <span class="math inline">\(\theta\)</span> 的极大似然估计。</p>
<p>由此我们可以得到该高斯混合模型参数估计的 EM 算法如下：</p>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/em-algorithm-gaussian-mixture-model-sample.png" alt="" /><figcaption>该高斯混合模型参数估计的 EM 算法</figcaption>
</figure>
<p>在得到估计该高斯混合模型的 EM 算法后，由于 EM 算法对初始值设置极为敏感，我们根据观测到的数据分布情况设定高斯混合模型的初始参数如下：</p>
<p><span class="math display">\[q = 0.7,\mu_1=1,\mu_2=2,\sigma_1^2=2,\sigma_2^2=2\]</span></p>
<p>在设定高斯混合模型的初始参数后，使用上述算法对其进行迭代，最终得到：</p>
<p><span class="math display">\[q = 0.45,\mu_1=0,\mu_2=2,\sigma_1^2=1,\sigma_2^2=1\]</span></p>
<p>Python源程序</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="comment"># coding: utf-8</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> scipy.stats <span class="keyword">import</span> norm</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> seaborn <span class="keyword">as</span> sns</span><br><span class="line">sns.<span class="built_in">set</span>()</span><br><span class="line"></span><br><span class="line">data = pd.read_csv(<span class="string">&quot;../data/EM.csv&quot;</span>,index_col=<span class="number">0</span>)</span><br><span class="line">X = np.array(data.x)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 绘制数据分布图</span></span><br><span class="line">sns.distplot(X)</span><br><span class="line">plt.savefig(<span class="string">&quot;distplot.png&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设定高斯混合模型初始参数</span></span><br><span class="line">n_samples = X.shape[<span class="number">0</span>]</span><br><span class="line">n_components = <span class="number">2</span></span><br><span class="line">tau = np.array([<span class="number">0.7</span>,<span class="number">0.3</span>])</span><br><span class="line">mu = np.array([<span class="number">1</span>,<span class="number">2</span>])</span><br><span class="line">sigma2 = np.array([<span class="number">2</span>,<span class="number">2</span>])</span><br><span class="line">max_iters = <span class="number">100</span></span><br><span class="line">T = np.zeros((n_components,n_samples))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 EM 算法进行迭代</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(max_iters):</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(n_components):</span><br><span class="line">        T[i] = tau[i] * norm.pdf(X,mu[i],np.sqrt(sigma2[i]))</span><br><span class="line">    T = T / np.<span class="built_in">sum</span>(T,axis=<span class="number">0</span>)</span><br><span class="line">    tau = np.<span class="built_in">sum</span>(T,axis=<span class="number">1</span>)/n_samples</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(n_components):</span><br><span class="line">        mu[i] = np.<span class="built_in">sum</span>(T[i] * X,axis=<span class="number">0</span>) / np.<span class="built_in">sum</span>(T[i],axis=<span class="number">0</span>)</span><br><span class="line">        sigma2[i] = np.<span class="built_in">sum</span>(T[i] * np.power(X - mu[i],<span class="number">2</span>),axis=<span class="number">0</span>) / np.<span class="built_in">sum</span>(T[i],axis=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出估计结果</span></span><br><span class="line">print(tau,mu,sigma2)</span><br></pre></td></tr></table></figure>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly9lbi53aWtpcGVkaWEub3JnL3dpa2kvRXhwZWN0YXRpb24lRTIlODAlOTNtYXhpbWl6YXRpb25fYWxnb3JpdGht">https://en.wikipedia.org/wiki/Expectation%E2%80%93maximization_algorithm<i class="fa fa-external-link-alt"></i></span><a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>统计学</category>
      </categories>
      <tags>
        <tag>EM 算法</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 使用指南</title>
    <url>/post/git-tutorial/</url>
    <content><![CDATA[<div class="note info"><ul>
<li>课程名称： 用 Git 进行版本控制</li>
<li>课程地址： <span class="exturl" data-url="aHR0cHM6Ly9jbGFzc3Jvb20udWRhY2l0eS5jb20vY291cnNlcy91ZDEyMw==">https://classroom.udacity.com/courses/ud123<i class="fa fa-external-link-alt"></i></span></li>
</ul>
</div>
<p>版本控制系统的主要目的是帮助你保留项目的详细历史记录，并且能够在不同的版本上进行工作。保留详细的项目历史记录很重要，因为这样可以看出一段时间内项目的进度。如果需要，你还可以回到项目的某个阶段，并恢复数据或文件。</p>
<p>版本控制系统模型包括两大主要类型：</p>
<ul>
<li>集中式模型---所有用户都连接到一个中央的主仓库 (master repository)</li>
<li>分布式模型---每个用户都在自己的计算机上拥有完整的仓库</li>
</ul>
<span id="more"></span>
<h2 id="相关术语">相关术语</h2>
<ul>
<li>版本控制系统 / 源代码管理器： 版本控制系统 (简称 VCS) 是一个管理源代码不同版本的工具。源代码管理器 (简称 SCM) 是版本控制系统的另一个名称。</li>
<li>提交 (Commit) ： Git 将数据看做微型文件系统的一组快照。每次 commit (在 Git 中保持项目状态) ，它都对文件当时的状况拍照，并存储对该快照的引用。你可以将其看做游戏中的保存点，它会保存项目的文件和关于文件的所有信息。在 Git 中的所有操作都是帮助你进行 commit，因此 commit 是 Git 中的基本单位。</li>
<li>仓库 (Repository / repo) ： 仓库是一个包含项目内容以及几个文件 (在 Mac OS X 上默认地处于隐藏状态) 的目录，用来与 Git 进行通信。仓库可以存储在本地，或作为远程副本存储在其他计算机上。仓库是由 commit 构成的。</li>
<li>工作目录 / 工作区 (Working Directory) ： 工作目录是你在计算机的文件系统中看到的文件。当你在代码编辑器中打开项目文件时，你是在工作目录中处理文件。与这些文件形成对比的是保持在仓库中 (在 commit 中！) 的文件。在使用 Git 时，工作目录与命令行工具的 current working directory (当前工作目录) 不一样，后者是 shell 当前正在查看的目录。</li>
<li>检出 (Checkout) ： 检出是指将仓库中的内容复制到工作目录下。</li>
<li>暂存区 / 暂存索引 / 索引 (Staging Area / Staging Index / Index) ： Git 目录下的一个文件，存储的是即将进入下个 commit 内容的信息。可以将暂存区看做准备工作台，Git 将在此区域获取下个 commit。暂存索引中的文件是准备添加到仓库中的文件。</li>
<li>SHA： SHA 是每个 commit 的 ID 编号。以下是 commit 的 SHA 示例： e2adf8ae3e2e4ed40add75cc44cf9d0a869afeb6。它是一个长 40 个字符的字符串 (由 0–9 和 a–f 组成) ，并根据 Git 中的文件或目录结构的内容计算得出。SHA 的全称是"Secure Hash Algorithm" (安全哈希算法) 。</li>
<li>分支 (Branch) ： 分支是从主开发流程中分支出来的新的开发流程。这种分支开发流程可以在不更改主流程的情况下继续延伸下去。</li>
</ul>
<h2 id="git-的一般工作流程">Git 的一般工作流程</h2>
<ol type="1">
<li>在工作目录中修改某些文件。</li>
<li>对修改后的文件进行快照，然后保存到暂存区域。</li>
<li>提交更新，将保存在暂存区域的文件快照永久转储到 Git 目录中。</li>
</ol>
<h2 id="windows-下-git-的安装">Windows 下 Git 的安装</h2>
<ol type="1">
<li>转到<span class="exturl" data-url="aHR0cHM6Ly9naXQtc2NtLmNvbS9kb3dubG9hZHM=">https://git-scm.com/downloads<i class="fa fa-external-link-alt"></i></span></li>
<li>下载 Windows 版软件</li>
<li>安装 Git 并选择所有默认选项</li>
</ol>
<h2 id="初次运行-git-前的配置">初次运行 Git 前的配置</h2>
<p>第一个要配置的是你个人的用户名称和电子邮件地址。这两条配置很重要，每次 Git 提交时都会引用这两条信息，说明是谁提交了更新，所以会随更新内容一起被永久纳入历史记录：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 设置你的 Git 用户名</span></span><br><span class="line">git config --global user.name &quot;&lt;Your-Full-Name&gt;&quot;</span><br><span class="line"><span class="meta">#</span><span class="bash"> 设置你的 Git 邮箱</span></span><br><span class="line">git config --global user.email &quot;&lt;your-email-address&gt;&quot;</span><br></pre></td></tr></table></figure>
<p>可选配置项：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 确保 Git 输出内容带有颜色标记</span></span><br><span class="line">git config --global color.ui auto</span><br><span class="line"><span class="meta">#</span><span class="bash"> 对比显示原始状态</span></span><br><span class="line">git config --global merge.conflictstyle diff3</span><br><span class="line"><span class="meta">#</span><span class="bash"> 配置 Git 文本编辑器为 VS Code</span></span><br><span class="line">git config --global core.editor &quot;code --wait&quot;</span><br></pre></td></tr></table></figure>
<p>要检查已有的配置信息，可以使用 git config --list 命令：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git config --list</span><br></pre></td></tr></table></figure>
<h2 id="取得项目的-git-仓库">取得项目的 Git 仓库</h2>
<h3 id="在工作目录中初始化新仓库">在工作目录中初始化新仓库</h3>
<p>要对现有的某个项目开始用 Git 管理，只需到此项目所在的目录，执行：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure>
<p>初始化后，在当前目录下会出现一个名为 <code>.git</code> 的目录，所有 Git 需要的数据和资源都存放在这个目录中。</p>
<h3 id="从现有仓库克隆">从现有仓库克隆</h3>
<p>如果你想获得一份已经存在了的 Git 仓库的拷贝，比如说，你想为某个开源项目贡献自己的一份力，这时就要用到 <code>git clone</code> 命令。</p>
<p>克隆仓库的命令格式是 <code>git clone [url]</code>， 比如，要克隆 Git 的可链接库 libgit2，可以用下面的命令：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/libgit2/libgit2</span><br></pre></td></tr></table></figure>
<p>这会在当前目录下创建一个名为 “libgit2” 的目录，并在这个目录下初始化一个 <code>.git</code> 文件夹，从远程仓库拉取下所有数据放入 <code>.git</code> 文件夹，然后从中读取最新版本的文件的拷贝。</p>
<h2 id="记录每次更新到仓库">记录每次更新到仓库</h2>
<figure>
<img data-src="https://xn--i0v668g.com/uploads/images/udacity-version-control-with-git-lifecycle.png" alt="" /><figcaption>文件的状态变化周期</figcaption>
</figure>
<h3 id="判断仓库当前的状态">判断仓库当前的状态</h3>
<p>要查看哪些文件处于什么状态，可以用 <code>git status</code> 命令。 如果在克隆仓库后立即使用此命令，会看到类似这样的输出：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">On branch master</span><br><span class="line">Your branch is up-to-date with &#x27;origin/master&#x27;.</span><br><span class="line">nothing to commit，working directory clean</span><br></pre></td></tr></table></figure>
<p>现在，让我们在项目下创建一个新的 README 文件。 如果之前并不存在这个文件，使用 <code>git status</code> 命令，你将看到一个新的未跟踪文件：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">On branch master</span><br><span class="line">Untracked files:</span><br><span class="line">  (use &quot;git add &lt;file&gt;...&quot; to include in what will be committed)</span><br><span class="line"></span><br><span class="line">    README</span><br><span class="line"></span><br><span class="line">nothing added to commit but untracked files present (use &quot;git add&quot; to track)</span><br></pre></td></tr></table></figure>
<h3 id="跟踪新文件">跟踪新文件</h3>
<p>使用命令 <code>git add</code> 开始跟踪一个文件。 所以，要跟踪 README 文件，运行：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git add README</span><br></pre></td></tr></table></figure>
<p>此时再运行 <code>git status</code> 命令，会看到 README 文件已被跟踪，并处于暂存状态：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">On branch master</span><br><span class="line">Changes to be committed:</span><br><span class="line">  (use &quot;git reset HEAD &lt;file&gt;...&quot; to unstage)</span><br><span class="line"></span><br><span class="line">    new file:   README</span><br></pre></td></tr></table></figure>
<p><code>git add</code> 命令可接受多个文件名 (用空格分隔)，此外，可以使用句点 <code>.</code> 来代替文件列表，告诉 git 添加当前目录至暂存区 (以及所有嵌套文件)。</p>
<h2 id="查看提交历史">查看提交历史</h2>
<p>在提交了若干更新，又或者克隆了某个项目之后，你也许想回顾下提交历史。 完成这个任务最简单而又有效的工具是 <code>git log</code> 命令。</p>
<p>默认不用任何参数的话，<code>git log</code> 会按提交时间列出所有的更新，最近的更新排在最上面。 正如你所看到的，这个命令会列出每个提交的 SHA-1 校验和、作者的名字和电子邮件地址、提交时间以及提交说明。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">commit 6f04ddd1fb41934c52e290bc937e45f9cd5949aa</span><br><span class="line">Author: Richard Kalehoff &lt;richardkalehoff@gmail.com&gt;</span><br><span class="line">Date:   Mon Dec 5 16:30:40 2016 -0500</span><br><span class="line"></span><br><span class="line">    Add breakpoint for large-sized screens</span><br></pre></td></tr></table></figure>
<blockquote>
<p>在 Git 中使用 less 程序作为其分页器，可阅读 <span class="exturl" data-url="aHR0cHM6Ly9lbi53aWtpcGVkaWEub3JnL3dpa2kvTGVzc18oVW5peCk=">less<i class="fa fa-external-link-alt"></i></span> 查阅其使用说明。</p>
</blockquote>
<p><code>git log</code> 命令有一个选项，可以用来更改仓库信息的显示方式。该选项为 <code>--oneline</code>：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git log --oneline</span><br></pre></td></tr></table></figure>
<p><code>--oneline</code> 将每个提交放在一行显示，显示 commit 的 SHA 的前 7 个字符及消息，对查看的提交数很大时非常有用。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">6f04ddd Add breakpoint for large-sized screens</span><br><span class="line">50d835d Add breakpoint for medium-sized screens</span><br><span class="line">0768f3d Add space around page edge</span><br><span class="line">f9720a9 Style page header</span><br><span class="line">8aa6668 Convert social links from text to images</span><br></pre></td></tr></table></figure>
<p><code>--stat</code> 选项在每次提交的下面列出所有被修改过的文件、有多少文件被修改了以及被修改过的文件的哪些行被移除或是添加了。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git log --stat</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">commit 6f04ddd1fb41934c52e290bc937e45f9cd5949aa</span><br><span class="line">Author: Richard Kalehoff &lt;richardkalehoff@gmail.com&gt;</span><br><span class="line">Date:   Mon Dec 5 16:30:40 2016 -0500</span><br><span class="line"></span><br><span class="line">    Add breakpoint for large-sized screens</span><br><span class="line"></span><br><span class="line"> css/app.css |  31 ++++++++++++++++</span><br><span class="line"> index.html  | 118 ++++++++++++++++++++++++++++++------------------------------</span><br><span class="line"> 2 files changed, 91 insertions(+), 58 deletions(-)</span><br></pre></td></tr></table></figure>
<p><code>--patch</code> 用来显示对文件作出实际更改，可以简写为 <code>-p</code>：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git log -p</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">commit a3dc99a197c66ccb87e3f4905502a6c6eddd15b1 (HEAD -&gt; master, origin/master, origin/HEAD)</span><br><span class="line">Author: Richard Kalehoff &lt;richardkalehoff@gmail.com&gt;</span><br><span class="line">Date:   Mon Dec 5 16:34:15 2016 -0500</span><br><span class="line"></span><br><span class="line">    Center content on page</span><br><span class="line"></span><br><span class="line">diff --git a/css/app.css b/css/app.css</span><br><span class="line">index 07c36fa..3cbd0b8 100644</span><br><span class="line">--- a/css/app.css</span><br><span class="line">+++ b/css/app.css</span><br><span class="line">@@ -38,6 +38,11 @@ p &#123;</span><br><span class="line">     line-height: 1.5;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">+.container &#123;</span><br><span class="line">+    margin: auto;</span><br><span class="line">+    max-width: 1300px;</span><br><span class="line">+    max-width: 1300px;</span><br><span class="line">+&#125;</span><br><span class="line">+</span><br><span class="line"></span><br><span class="line"> /*** Header Styling ***/</span><br><span class="line"> .page-header &#123;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>补丁输出显示所有行先被删掉，然后在新的缩进位置又被重新添加。但是显示所有的缩进更改让我们很难发现实际添加了什么代码。 <code>git log -p -w</code> 将会忽略空格更改。</p>
</blockquote>
<p>可以向所有这些命令提供 commit 的 SHA 作为最后一个参数。如：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git log -p fdf5493</span><br></pre></td></tr></table></figure>
<p>通过提供 SHA，<code>git log -p</code> 命令将从这条 commit 开始。</p>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
  </entry>
  <entry>
    <title>Hadoop学习笔记（1）：安装与伪分布式</title>
    <url>/post/hadoop-tutorial-1/</url>
    <content><![CDATA[<p>Hadoop 是一个开源框架，允许使用简单的编程模型在跨计算机集群的分布式环境中存储和处理大数据。它的设计是从单个服务器扩展到数千个机器，每个都提供本地计算和存储。</p>
<p>本次实验基于<span class="exturl" data-url="aHR0cDovL2RibGFiLnhtdS5lZHUuY24vYmxvZy9pbnN0YWxsLWhhZG9vcC1pbi1jZW50b3Mv">Hadoop安装教程_伪分布式配置_CentOS6.4/Hadoop2.6.0<i class="fa fa-external-link-alt"></i></span>与<span class="exturl" data-url="aHR0cDovL2hhZG9vcC5hcGFjaGUub3JnL2RvY3Mvc3RhYmxlL2hhZG9vcC1wcm9qZWN0LWRpc3QvaGFkb29wLWNvbW1vbi9TaW5nbGVDbHVzdGVyLmh0bWw=">官网教程<i class="fa fa-external-link-alt"></i></span>，实现了在 Ubuntu 操作系统的主机上安装 Hadoop-3.3.0、并实现单机与伪分布式配置并运行实例.</p>
<span id="more"></span>
<h2 id="准备工作">准备工作</h2>
<ul>
<li>安装 SSH 与 Java 8；</li>
<li>（可选）准备用户 <code>hadoop</code>。</li>
</ul>
<h2 id="安装-hadoop">安装 Hadoop</h2>
<p>从清华镜像网站下载 Hadoop，此处我选择的是其<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2FwYWNoZS9oYWRvb3AvY29tbW9uL3N0YWJsZS9oYWRvb3AtMy4zLjAudGFyLmd6">稳定版本<i class="fa fa-external-link-alt"></i></span>。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">wget https://mirrors.tuna.tsinghua.edu.cn/apache/hadoop/common/stable/hadoop-3.3.0.tar.gz</span><br></pre></td></tr></table></figure>
<p>解压 Hadoop 至 <code>/usr/local/hadoop</code></p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">mkdir /usr/<span class="built_in">local</span>/hadoop</span><br><span class="line">tar -zxvf ./hadoop-3.3.0.tar.gz -C /usr/<span class="built_in">local</span>/hadoop</span><br></pre></td></tr></table></figure>
<p>Hadoop 解压后即可使用. 输入如下命令来检查 Hadoop 是否可用：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/hadoop/hadoop-3.3.0</span><br><span class="line">./bin/hadoop version</span><br></pre></td></tr></table></figure>
<div class="note info"><p>在开始运行前，需修改<code>./etc/hadoop/hadoop-env.sh</code>中定义的<code>JAVA_HOME</code>。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">vim ./etc/hadoop/hadoop-env.sh</span><br></pre></td></tr></table></figure>
<p>修改 <code>export JAVA_HOME=${JAVA_HOME}</code> 为</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64</span><br></pre></td></tr></table></figure>
</div>
<p>Hadoop 成功安装后，则会显示 Hadoop 版本信息，如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Hadoop 3.3.0</span><br><span class="line">Source code repository https://gitbox.apache.org/repos/asf/hadoop.git -r aa96f1871bfd858f9bac59cf2a81ec470da649af</span><br><span class="line">Compiled by brahma on 2020-07-06T18:44Z</span><br><span class="line">Compiled with protoc 3.7.1</span><br><span class="line">From source with checksum 5dc29b802d6ccd77b262ef9d04d19c4</span><br><span class="line">This command was run using /usr/local/hadoop/hadoop-3.3.0/share/hadoop/common/hadoop-common-3.3.0.jar</span><br></pre></td></tr></table></figure>
<h2 id="hadoop-非分布式">Hadoop 非分布式</h2>
<p>Hadoop 默认模式为非分布式模式，无需进行其他配置即可运行。非分布式即单 Java 进程，方便进行调试。</p>
<p>在此我们选择运行 <code>grep</code> 例子，我们将 <code>input</code> 文件夹中的所有文件作为输入，筛选当中符合正则表达式 <code>dfs[a-z.]+</code> 的单词并统计出现的次数，最后输出结果到 output 文件夹中。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/hadoop/hadoop-3.3.0</span><br><span class="line">mkdir ./input</span><br><span class="line">cp ./etc/hadoop/*.xml ./input</span><br><span class="line">./bin/hadoop jar ./share/hadoop/mapreduce/hadoop-mapreduce-examples-*.jar grep ./input ./output <span class="string">&#x27;dfs[a-z.]+&#x27;</span></span><br><span class="line">cat ./output/*</span><br></pre></td></tr></table></figure>
<p>运行结果如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1       dfsadmin</span><br></pre></td></tr></table></figure>
<div class="note warning"><p>Hadoop 默认不会覆盖结果文件，因此再次运行上面实例会提示出错，需要先将 <code>./output</code> 删除</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">rm -r ./output</span><br></pre></td></tr></table></figure>
</div>
<h2 id="hadoop-伪分布式">Hadoop 伪分布式</h2>
<p>修改配置文件 <code>core-site.xml</code></p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">vim ./etc/hadoop/core-site.xml</span><br></pre></td></tr></table></figure>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">name</span>&gt;</span>fs.defaultFS<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">value</span>&gt;</span>hdfs://localhost:9000<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>修改配置文件 <code>hdfs-site.xml</code></p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">vim ./etc/hadoop/hdfs-site.xml</span><br></pre></td></tr></table></figure>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">name</span>&gt;</span>dfs.replication<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">value</span>&gt;</span>1<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>配置完成后，执行 <code>NameNode</code> 的格式化:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./bin/hdfs namenode -format</span><br></pre></td></tr></table></figure>
<p>接着开启 <code>NaneNode</code> 和 <code>DataNode</code> 守护进程：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./sbin/start-dfs.sh</span><br></pre></td></tr></table></figure>
<div class="note warning"><p>在执行时，发现出现 <code>no HDFS_NAMENODE_USER defined</code> 错误（未定义 NameNode 用户），参考 <span class="exturl" data-url="aHR0cHM6Ly9zdGFja292ZXJmbG93LmNvbS9xdWVzdGlvbnMvNDgxMjkwMjkvaGRmcy1uYW1lbm9kZS11c2VyLWhkZnMtZGF0YW5vZGUtdXNlci1oZGZzLXNlY29uZGFyeW5hbWVub2RlLXVzZXItbm90LWRlZmluZWQ=">HDFS_NAMENODE_USER, HDFS_DATANODE_USER &amp; HDFS_SECONDARYNAMENODE_USER not defined<i class="fa fa-external-link-alt"></i></span>，定义相关用户</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> HDFS_NAMENODE_USER=<span class="string">&quot;root&quot;</span></span><br><span class="line"><span class="built_in">export</span> HDFS_DATANODE_USER=<span class="string">&quot;root&quot;</span></span><br><span class="line"><span class="built_in">export</span> HDFS_SECONDARYNAMENODE_USER=<span class="string">&quot;root&quot;</span></span><br><span class="line"><span class="built_in">export</span> YARN_RESOURCEMANAGER_USER=<span class="string">&quot;root&quot;</span></span><br><span class="line"><span class="built_in">export</span> YARN_NODEMANAGER_USER=<span class="string">&quot;root&quot;</span></span><br></pre></td></tr></table></figure>
</div>
<p>启动完成后，可以通过命令 jps 来判断是否成功启动，若成功启动则会列出如下进程: <code>NameNode</code>、 <code>DataNode</code>和<code>SecondaryNameNode</code>。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">8487 DataNode</span><br><span class="line">8298 NameNode</span><br><span class="line">8715 SecondaryNameNode</span><br><span class="line">9387 Jps</span><br></pre></td></tr></table></figure>
<div class="note info"><p>如果 <code>SecondaryNameNode</code> 没有启动，请运行 <code>./sbin/stop-dfs.sh</code> 关闭进程，然后再次尝试启动尝试.</p>
</div>
<p>成功启动后，可以访问 Web 界面 <span class="exturl" data-url="aHR0cDovL2xvY2FsaG9zdDo1MDA3MA==">http://localhost:50070<i class="fa fa-external-link-alt"></i></span> (主机地址) 查看 <code>NameNode</code> 和 <code>Datanode</code> 信息，还可以在线查看 <code>HDFS</code> 中的文件.</p>
<p>上面的单机模式，<code>grep</code> 例子读取的是本地数据，伪分布式读取的则是 <code>HDFS</code> 上的数据. 要使用 <code>HDFS</code>，首先需要在 <code>HDFS</code> 中创建用户目录：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./bin/hdfs dfs -mkdir -p /user/hadoop</span><br></pre></td></tr></table></figure>
<p>接着将 <code>./etc/hadoop</code> 中的 <code>xml</code> 文件作为输入文件复制到分布式文件系统中，即将 <code>/usr/local/hadoop/etc/hadoop</code> 复制到分布式文件系统中的 <code>/user/hadoop/input</code> 中. 我们使用的是 <code>hadoop</code> 用户，并且已创建相应的用户目录 <code>/user/hadoop</code> ，因此在命令中就可以使用相对路径如 <code>input</code>，其对应的绝对路径就是 <code>/user/hadoop/input</code>：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./bin/hdfs dfs -mkdir input</span><br><span class="line">./bin/hdfs dfs -put ./etc/hadoop/*.xml input</span><br></pre></td></tr></table></figure>
<p>复制完成后，可以通过如下命令查看 <code>HDFS</code> 中的文件列表：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./bin/hdfs dfs -ls input</span><br></pre></td></tr></table></figure>
<p>伪分布式运行 <code>MapReduce</code> 作业的方式跟单机模式相同，区别在于伪分布式读取的是 <code>HDFS</code> 中的文件.</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./bin/hadoop jar ./share/hadoop/mapreduce/hadoop-mapreduce-examples-*.jar grep input output <span class="string">&#x27;dfs[a-z.]+&#x27;</span></span><br></pre></td></tr></table></figure>
<p>查看运行结果的命令（查看的是位于 <code>HDFS</code> 中的输出结果）：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./bin/hdfs dfs -cat output/*</span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1       dfsadmin</span><br><span class="line">1       dfs.replication</span><br></pre></td></tr></table></figure>
<p>我们也可以将运行结果取回到本地：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">rm -r ./output</span><br><span class="line">./bin/hdfs dfs -get output ./output</span><br><span class="line">cat ./output/*</span><br></pre></td></tr></table></figure>
<div class="note warning"><p>同样在 Hadoop 运行程序时，输出目录不能存在. 因此若要再次执行，需要执行如下命令删除 <code>output</code> 文件夹:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./bin/hdfs dfs -rm -r output</span><br></pre></td></tr></table></figure>
</div>
<p>若要关闭 <code>Hadoop</code>，则运行</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">./sbin/stop-dfs.sh</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
      <tags>
        <tag>Hadoop</tag>
        <tag>分布式计算</tag>
      </tags>
  </entry>
  <entry>
    <title>Hadoop学习笔记（2）：MapReduce 基础编程</title>
    <url>/post/hadoop-tutorial-2/</url>
    <content><![CDATA[<p>MapReduce 采用“分而治之”的思想，把对大规模数据集的操作，分发给一个主节点管理下的各个分节点共同完成，然后通过整合各个节点的中间结果，得到最终结果。简单地说，MapReduce 就是“任务的分解与结果的汇总”。</p>
<span id="more"></span>
<p>在 Hadoop 中，用于执行 MapReduce 任务的机器角色有两个：</p>
<ul>
<li>JobTracker 用于调度工作的，一个 Hadoop 集群中只有一个 JobTracker，位于 master</li>
<li>TaskTracker 用于执行工作，位于各 slave 上</li>
</ul>
<p>在分布式计算中，MapReduce 框架负责处理了并行编程中分布式存储、工作调度、负载均衡、容错均衡、容错处理以及网络通信等复杂问题，把处理过程高度抽象为两个函数：map 和 reduce，map 负责把任务分解成多个任务，reduce 负责把分解后多任务处理的结果汇总起来。</p>
<p>需要注意的是，用 MapReduce 来处理的数据集（或任务）必须具备这样的特点：待处理的数据集可以分解成许多小的数据集，而且每一个小数据集都可以完全并行地进行处理。</p>
<h2 id="wordcount">WordCount</h2>
<p>修改 WordCount 源代码，使得输出的词频统计满足</p>
<ul>
<li>数据清洗：对每个词的格式进行规范化（去除不以英文字母开头的所有词）</li>
<li>词频少于 3 次的数据不在结果中显示</li>
<li>结果以有限数量的“+”表示词频统计</li>
</ul>
<p>因此，</p>
<ul>
<li>在 map 函数中进行修改，只有当单词以英文字母开头（此处可考虑使用正则表达式进行匹配）时，才将其输出。</li>
<li>在 reduce 中进行判断，只有当 reduce 函数的 Key 的 Value 之和大于 3，才将其作为 reduce 函数的输出。</li>
<li>由于 reduce 所输出的 Key 与 Value 的类型均为 Text 类型，需要修改 reduce 函数的输出类型，并修改配置文件中的输出类型。</li>
</ul>
<p>我们这里采用的是古德堡项目中的部分英文小说，部分运行结果如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">abaft    +++</span><br><span class="line">abandon    +++++</span><br><span class="line">abandoned    +++++++++++++++++++++</span><br><span class="line">abandoning    +++</span><br><span class="line">abandonment    ++++</span><br><span class="line">abated    +++++</span><br><span class="line">abbey    +++</span><br><span class="line">abed    ++++</span><br><span class="line">abhorred    ++++</span><br><span class="line">abide    ++++++++++++++++++</span><br><span class="line">ability    ++++</span><br><span class="line">abject    +++</span><br></pre></td></tr></table></figure>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvV29yZENvdW50LmphdmE=">Source Code<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
<h2 id="invertedindex">InvertedIndex</h2>
<p>倒排索引是目前几乎所有支持全文检索的搜索引擎都需要依赖的一个数据结构，该索引结构用来存储某个单词在一个文档或一组文档中存储位置的映射，即提供了一种根据内容来查找文档的方式。</p>
<p>改进 WordCount 代码：Map 函数输出 <code>&lt;单词，文件名@位置&gt;</code>，Reduce 函数输出 <code>&lt;单词，list&lt;文件名@位置&gt;&gt;</code>即可。</p>
<ul>
<li>清洗一部分明显不合理的单词的统计</li>
<li>根据上述要求，完成真实环境中文档倒排索引的 MapReduce 框架代码</li>
<li>生成一个 input 文件夹，搜索一个文本文件的集合，拷贝到 input 文件夹中</li>
<li>运行 MapReduce 框架将生成的倒排文档索引放入目标文件中</li>
</ul>
<p>因此，</p>
<ul>
<li>通过查询相关文献，我们采取删除在 StopWords 的单词的方法。与在 WordCount 中一样，只有当单词不在 StopWords 列表中时，才将其输出。</li>
<li>在此处若直接将键值对 <code>&lt;单词，文件名&gt;</code> 作为 Reduce 的输入，在数据量极大时，会因为网络带宽，对性能造成极大地降低，那么我们是否可以先在 map 服务器上对键值对进行合并，那么传递给 Reduce 的输入的数据量就会大大减少。基于这个思想，我们在此处使用 Combiner 函数对 map 的输出进行预先的合并。</li>
<li>此处还需要注意一点，这里要求输出单词在文件中的位置，而 map 是按行进行读入的，并不像我们平常使用的 Java 读入文件那样，可以可以根据换行符来判断他在第几行。这里，由于文件名或者说文件的存储路径是不同的，我们可以使用其来进行判断他是否属于同一个文件，若属于同一个文件，我们对行数加一，若不属于同一个文件，我们将行数归为 1。因此，我们就实现了判断单词在文件中的位置。</li>
</ul>
<p>我们这里采用的是古德堡项目中的部分英文小说，部分运行结果如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">aback    98-0.txt@70690;pg345.txt@55123;2701-0.txt@12402;2701-0.txt@5472;</span><br><span class="line">abaft    2701-0.txt@15814;2701-0.txt@9732;pg345.txt@51608;pg345.txt@51265;</span><br><span class="line">abandon    pg345.txt@55383;2701-0.txt@8558;2701-0.txt@944;2701-0.txt@15471;98-0.txt@69667;</span><br></pre></td></tr></table></figure>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvSW52ZXJ0ZWRJbmRleC5qYXZh">Source Code<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
<p>在编写代码时，我遇到了这样一个问题，就是因为 Combiner 的输入与输出类型与 map 的输出和 reduce 的输入不同而报错。通过查阅相关资料，了解到 Combiner 的输入和 reduce 的完全一致，输出和 map 的完全一致。</p>
<p>在结果中，我们看到因为英语有单复数、时态等，相同的单词可能被认为是不同的，查阅相关资料，我们了解到可以用词干提取与词干还原等方式将单词变为原型。</p>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
      <tags>
        <tag>Hadoop</tag>
        <tag>分布式计算</tag>
      </tags>
  </entry>
  <entry>
    <title>隐式马尔科夫模型</title>
    <url>/post/hidden-markov-model/</url>
    <content><![CDATA[<p>隐马尔可夫模型（Hidden Markov Model；缩写：HMM）或称作隐性马尔可夫模型，是统计模型，它用来描述一个含有隐含未知参数的马尔可夫过程。其难点是从可观察的参数中确定该过程的隐含参数，然后利用这些参数来作进一步的分析，例如模式识别。</p>
<p>在正常的马尔可夫模型中，状态对于观察者来说是直接可见的，这样状态的转换概率便是全部的参数。而在隐马尔可夫模型中，状态并不是直接可见的，但受状态影响的某些变量则是可见的，每一个状态在可能输出的符号上都有一概率分布，因此输出符号的序列能够透露出状态序列的一些信息。</p>
<span id="more"></span>
<h2 id="隐式马尔科夫模型的定义">隐式马尔科夫模型的定义</h2>
<p>我们考虑这样一个例子：</p>
<p>假设你有一个住得很远的朋友，他每天跟你打电话告诉你他那天做了什么。你的朋友仅仅对三种活动感兴趣：公园散步，购物以及清理房间。他选择做什么事情只凭天气。你对于他所住的地方的天气情况并不了解，但是你知道总的趋势。在他告诉你每天所做的事情基础上，你想要猜测他所在地的天气情况。</p>
<p>你认为天气的运行就像一个马尔可夫链。其有两个状态“雨”和“晴”，但是你无法直接观察它们，也就是说，它们对于你是隐藏的。每天，你的朋友有一定的概率进行下列活动：“散步”、“购物”、“清理”。因为你朋友告诉你他的活动，所以这些活动就是你的观察数据。这整个系统就是一个隐马尔可夫模型。</p>
<p>你知道这个地区的总的天气趋势，并且平时知道你朋友会做的事情。也就是说这个隐马尔可夫模型的参数是已知的。<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/hidden-markov-model-example.png" alt="" /><figcaption>A Simple Hidden Markov Model Example</figcaption>
</figure>
<p>初始概率分布为：</p>
<p><span class="math display">\[\pi=\left({0.6},{0.4}\right)&#39;\]</span></p>
<p>状态转移概率分布为：</p>
<p><span class="math display">\[A=\left(\begin{array}{cc}{0.7}&amp;{0.3}\\{0.4}&amp;{0.6}\end{array}\right)\]</span></p>
<p>观察概率分布为：</p>
<p><span class="math display">\[B=\left(\begin{array}{ccc}{0.1}&amp;{0.4}&amp;{0.5}\\{0.6}&amp;{0.3}&amp;{0.1}\end{array}\right)\]</span></p>
<h2 id="观察序列的生成过程">观察序列的生成过程</h2>
<div class="note primary"><p><strong>输入：</strong> 隐式马尔可夫模型 <span class="math inline">\(\lambda=\left(A,B,\pi\right)\)</span>，观察序列长度 <span class="math inline">\(T\)</span>； <strong>输出：</strong> 观察序列 <span class="math inline">\(O=\left(o_1,o_2,\dots,o_T\right)\)</span>；</p>
<p><strong>过程：</strong></p>
<ol type="1">
<li>按照初始状态分布 <span class="math inline">\(\pi\)</span> 生成状态 <span class="math inline">\(i_1\)</span></li>
<li>令 <span class="math inline">\(t=1\)</span></li>
<li>按照状态 <span class="math inline">\(i_t\)</span> 的观测概率分布 <span class="math inline">\(b_{i_t}(k)\)</span> 生成 <span class="math inline">\(o_t\)</span></li>
<li>按照状态 <span class="math inline">\(i_t\)</span> 的状态转移概率分布 <span class="math inline">\(\left\{a_{i_ti_{t+1}}\right\}\)</span> 生成状态 <span class="math inline">\(i_{t+1}\)</span></li>
<li>令 <span class="math inline">\(t=t+1\)</span>； 如果 <span class="math inline">\(t&lt;T\)</span>，转步（3）；否则，终止</li>
</ol>
</div>
<p>所以，其生成程序如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sample</span>(<span class="params">self, l, trans, emis, statenames=<span class="literal">None</span>, symbols=<span class="literal">None</span></span>):</span></span><br><span class="line">    seq = []</span><br><span class="line">    states = []</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Trans must be square</span></span><br><span class="line">    states_num, m = trans.shape</span><br><span class="line">    <span class="keyword">if</span> states_num != m:</span><br><span class="line">        <span class="keyword">raise</span> TypeError(<span class="string">&quot;BadTransitions.&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Number of rows of emis must be same as number of states</span></span><br><span class="line">    m, emissions_num = emis.shape</span><br><span class="line">    <span class="keyword">if</span> states_num != m:</span><br><span class="line">        <span class="keyword">raise</span> TypeError(<span class="string">&quot;InputSizeMismatch.&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> statenames != <span class="literal">None</span> <span class="keyword">and</span> <span class="built_in">len</span>(statenames) != states_num:</span><br><span class="line">        <span class="keyword">raise</span> TypeError(<span class="string">&quot;BadStateNames.&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> symbols != <span class="literal">None</span> <span class="keyword">and</span> <span class="built_in">len</span>(symbols) != emissions_num:</span><br><span class="line">        <span class="keyword">raise</span> TypeError(<span class="string">&quot;BadSymbols.&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Create two random sequences, one for state changes, one for emission</span></span><br><span class="line">    state_change = np.random.rand(<span class="number">1</span>, l)</span><br><span class="line">    rand_vals = np.random.rand(<span class="number">1</span>, l)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Calculate cumulative probabilities</span></span><br><span class="line">    trans_cumsum = np.cumsum(trans, axis=<span class="number">1</span>)</span><br><span class="line">    emis_cumsum = np.cumsum(emis, axis=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Normalize these just in case they don&#x27;t sum to 1.</span></span><br><span class="line">    trans_cumsum = trans_cumsum/trans_cumsum[:,-<span class="number">1</span>][:,<span class="literal">None</span>]</span><br><span class="line">    emis_cumsum = emis_cumsum/emis_cumsum[:,-<span class="number">1</span>][:,<span class="literal">None</span>]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Assume that we start in state 0.</span></span><br><span class="line">    current_state = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># Main loop</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(l):</span><br><span class="line">        <span class="comment"># Calculate state transition</span></span><br><span class="line">        state_val = state_change[:, i]</span><br><span class="line">        state = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(states_num - <span class="number">2</span>, -<span class="number">1</span>, -<span class="number">1</span>):</span><br><span class="line">            <span class="keyword">if</span> state_val &gt; trans_cumsum[current_state, j]:</span><br><span class="line">                state = j + <span class="number">1</span></span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">        <span class="comment"># Calculate emission</span></span><br><span class="line">        val = state_change[:, i]</span><br><span class="line">        emit = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(emissions_num - <span class="number">2</span>, -<span class="number">1</span>, -<span class="number">1</span>):</span><br><span class="line">            <span class="keyword">if</span> val  &gt; emis_cumsum[state, j]:</span><br><span class="line">                emit = j + <span class="number">1</span></span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># Add values and states to output</span></span><br><span class="line">    seq.append(emit)</span><br><span class="line">    states.append(state)</span><br><span class="line">    current_state = state</span><br><span class="line">    <span class="keyword">if</span> symbols != <span class="literal">None</span>:</span><br><span class="line">        seq = [symbols[i] <span class="keyword">for</span> i <span class="keyword">in</span> seq]</span><br><span class="line">    <span class="keyword">if</span> statenames != <span class="literal">None</span>:</span><br><span class="line">        states = [statenames[i] <span class="keyword">for</span> i <span class="keyword">in</span> states]</span><br><span class="line">    <span class="keyword">return</span> seq, states</span><br></pre></td></tr></table></figure>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly9lbi53aWtpcGVkaWEub3JnL3dpa2kvSGlkZGVuX01hcmtvdl9tb2RlbA==">https://en.wikipedia.org/wiki/Hidden_Markov_model<i class="fa fa-external-link-alt"></i></span><a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>隐式马尔科夫模型</tag>
      </tags>
  </entry>
  <entry>
    <title>约瑟夫环</title>
    <url>/post/josephus-problem/</url>
    <content><![CDATA[<p>约瑟夫Joseph)问题的一种描述是：编号为 <span class="math inline">\(1,2,\dots,n\)</span> 的 <span class="math inline">\(n\)</span> 个人按顺时针方向围坐一圈，每人持有一个密码（正整数）。一开始选任一个正整数作为报数上限值 m，从第一个人开始按顺时针方向自1开始顺序报数，报到 m 时停止报数。报 m 的人出列，将它的密码作为新的 m 值，再从下个人开始新一轮报数，如此反复，直到剩下最后一人则为获胜者。试设计一个程序求出出列顺序。</p>
<span id="more"></span>
<ul>
<li>测试数据
<ul>
<li><strong>Input:</strong> n = 7, m0 = 20, password = 3 1 7 2 4 8 4</li>
<li><strong>Output:</strong> 6 1 4 7 2 3 5</li>
</ul></li>
<li>输入输出
<ul>
<li>输入：输入 n 输入以及每个人的密码；m 的初值</li>
<li>输出：按照出列的顺序印出各人的编号</li>
</ul></li>
</ul>
<h2 id="问题分析">问题分析</h2>
<p>为解决该问题，需利用单向循环链表存储结构模拟此过程。</p>
<blockquote>
<p>单向循环链表也称为环形链表，其结构与单链表相似，只是将单链表的首尾相连，即将单链表的最后一个结点的后继指针指向第一个结点，从而构成一个环状链表。</p>
</blockquote>
<h2 id="程序源码">程序源码</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> Elemtype;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Node</span> &#123;</span></span><br><span class="line">    Elemtype index;</span><br><span class="line">    Elemtype password;</span><br><span class="line">    Node *next;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">Node *<span class="title">create</span><span class="params">(<span class="keyword">int</span> len,Elemtype password[])</span> </span>&#123;</span><br><span class="line">    Node *prev,*head,*temp;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= len; i++)</span><br><span class="line">    &#123;</span><br><span class="line">    temp = (Node*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    <span class="keyword">if</span> (temp == <span class="literal">NULL</span>) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Error - unable to allocate required memory\n&quot;</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">-2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    temp-&gt;index = i;</span><br><span class="line">    temp-&gt;password = password[i - <span class="number">1</span>];</span><br><span class="line">    <span class="keyword">if</span> (i == <span class="number">1</span>)</span><br><span class="line">    head = temp;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    prev-&gt;next = temp;</span><br><span class="line">    prev = temp;</span><br><span class="line">    &#125;</span><br><span class="line">    temp-&gt;next = head;</span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    Node *temp = head;</span><br><span class="line">    <span class="keyword">while</span> (temp-&gt;next != head) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>,temp-&gt;index);</span><br><span class="line">    temp = temp-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">josephus_problem</span><span class="params">(<span class="keyword">int</span> n,<span class="keyword">int</span> m0,<span class="keyword">int</span> password[])</span> </span>&#123;</span><br><span class="line">    Node *head = create(n,password);</span><br><span class="line">    Node *temp = head,*prev;</span><br><span class="line">    <span class="keyword">int</span> len = n,m = m0;</span><br><span class="line">    <span class="keyword">while</span> (len != <span class="number">0</span>)&#123;</span><br><span class="line">    m = m == len ? m % len : m;</span><br><span class="line">    <span class="keyword">if</span> (m &gt; <span class="number">1</span>)&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; m - <span class="number">1</span>; i++)</span><br><span class="line">    temp = temp-&gt;next;</span><br><span class="line">    prev = temp;</span><br><span class="line">    temp = temp-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>,temp-&gt;index);</span><br><span class="line">    m = temp-&gt;password;</span><br><span class="line">    temp = temp-&gt;next;</span><br><span class="line">    prev-&gt;next = prev-&gt;next-&gt;next;</span><br><span class="line">    len--;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> password[<span class="number">1000</span>],m0,n;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;n);</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;m0);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;password[i]);</span><br><span class="line">    josephus_problem(n,m0,password);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>单链表</tag>
      </tags>
  </entry>
  <entry>
    <title>圣杯布局</title>
    <url>/post/layout-holy-grail/</url>
    <content><![CDATA[<p>圣杯布局是典型的 Css 布局问题，有着众多的解决方案。最早的完美实现是来自于 Matthew Levine 的 <span class="exturl" data-url="aHR0cHM6Ly9hbGlzdGFwYXJ0LmNvbS9hcnRpY2xlL2hvbHlncmFpbA==">"In Search of the Holy Grail"<i class="fa fa-external-link-alt"></i></span>，它主要讲述了圣杯布局的最佳实现方法。而现在的最佳解决方法为使用 flex 实现。</p>
<p>圣杯布局具有如下优点：</p>
<ul>
<li>主要内容先加载的优化。</li>
<li>兼容目前所有的主流浏览器。</li>
<li>实现不同的布局方式，可以通过调整相关 Css 属性即可实现。</li>
</ul>
<span id="more"></span>
<h2 id="圣杯布局的传统实现">圣杯布局的传统实现</h2>
<p>在 Matthew Levine 的 <span class="exturl" data-url="aHR0cHM6Ly9hbGlzdGFwYXJ0LmNvbS9hcnRpY2xlL2hvbHlncmFpbA==">"In Search of the Holy Grail"<i class="fa fa-external-link-alt"></i></span>中，作者指出了当时的一些实现方式所存在的问题，如：必须按照源顺序（在 DOM 中表现为先写 Left，然后 Middle，最后，Right）等，它将可能导致代码不够灵活，尤其是从 DOM 的载入顺序上来说，中间的内容不能被首先加载。因此他给出一个方案，它将：</p>
<ul>
<li>两边带有固定宽度中间可以流动（fluid）。</li>
<li>允许中间一栏最先出现。</li>
<li>允许任意一栏放在最上面。</li>
<li>仅需一个额外的 <code>div</code> 标签。</li>
<li>仅需非常简单的 Css，带上最少的兼容性补丁。</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;header&quot;</span>&gt;</span>This is the header.<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;container&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;center&quot;</span> <span class="attr">class</span>=<span class="string">&quot;column&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;left&quot;</span> <span class="attr">class</span>=<span class="string">&quot;column&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;right&quot;</span> <span class="attr">class</span>=<span class="string">&quot;column&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;footer&quot;</span>&gt;</span>This is the footer.<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">    <span class="attribute">min-width</span>: <span class="number">550px</span>;        <span class="comment">/* 2x LC width + RC width */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#container</span> &#123;</span><br><span class="line">    <span class="attribute">padding-left</span>: <span class="number">200px</span>;     <span class="comment">/* LC width */</span></span><br><span class="line">    <span class="attribute">padding-right</span>: <span class="number">150px</span>;    <span class="comment">/* RC width */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#container</span> <span class="selector-class">.column</span> &#123;</span><br><span class="line">    <span class="attribute">height</span>: <span class="number">200px</span>;</span><br><span class="line">    <span class="attribute">position</span>: relative;</span><br><span class="line">    <span class="attribute">float</span>: left;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#center</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#e9e9e9</span>;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#left</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: red;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">200px</span>;                    <span class="comment">/* LC width */</span></span><br><span class="line">    <span class="attribute">right</span>: <span class="number">200px</span>;                    <span class="comment">/* LC width */</span></span><br><span class="line">    <span class="attribute">margin-left</span>: -<span class="number">100%</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#right</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: blue;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">150px</span>;                    <span class="comment">/* RC width */</span></span><br><span class="line">    <span class="attribute">margin-right</span>: -<span class="number">150px</span>;    <span class="comment">/* RC width */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#footer</span> &#123;</span><br><span class="line">    <span class="attribute">clear</span>: both;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#header</span>,</span><br><span class="line"><span class="selector-id">#footer</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#c9c9c9</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*** IE6 Fix ***/</span></span><br><span class="line">* <span class="selector-tag">html</span> <span class="selector-id">#left</span> &#123;</span><br><span class="line">    <span class="attribute">left</span>: <span class="number">150px</span>;                     <span class="comment">/* RC width */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#footer</span> &#123;</span><br><span class="line">    <span class="attribute">clear</span>: both;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvZmlsZXMvbGF5b3V0LWhvbHktZ3JhaWwtZXhhbXBsZTEuaHRtbA==">Source Code<i class="fa fa-external-link-alt"></i></span></p>
<h3 id="美化及实现等高列">美化及实现等高列</h3>
<p>见<span class="exturl" data-url="aHR0cHM6Ly9hbGlzdGFwYXJ0LmNvbS9hcnRpY2xlL2hvbHlncmFpbA==">"In Search of the Holy Grail"<i class="fa fa-external-link-alt"></i></span></p>
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvZmlsZXMvbGF5b3V0LWhvbHktZ3JhaWwtZXhhbXBsZTIuaHRtbA==">Source Code<i class="fa fa-external-link-alt"></i></span></p>
<h2 id="圣杯布局的flex实现">圣杯布局的flex实现</h2>
<p>圣杯布局由页头（header），中间部分（center），页脚（footer），和三栏组成。中间的一栏是主要内容，左边和右边提供如广告、导航的链接。大部分的 Css 解决方案都是以下列为目标：</p>
<ul>
<li>边栏应流动居中，定宽</li>
<li>中间一栏（主要内容）在 HTML 源码中应该首先元素出现</li>
<li>所有栏同高，忽略实际高度</li>
<li>使用的 HTML 标记尽量少</li>
<li>当页面内容不够充满页面时，页脚应 “粘” 在底部</li>
</ul>
<p>不幸的是，这些自然的需求由于原生 Css 的限制，当前经典的圣杯布局的解决方案都不能完美满足以上所有的要点。有了 Flexbox 布局，终极的解决方案终于成为可能。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span> <span class="attr">class</span>=<span class="string">&quot;HolyGrail&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">header</span>&gt;</span><span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;HolyGrail-body&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">main</span> <span class="attr">class</span>=<span class="string">&quot;HolyGrail-content&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">main</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">nav</span> <span class="attr">class</span>=<span class="string">&quot;HolyGrail-nav&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">nav</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aside</span> <span class="attr">class</span>=<span class="string">&quot;HolyGrail-ads&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">aside</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">footer</span>&gt;</span><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>让中间部分撑开并让页脚粘在底部的方法使用了粘性页脚中相同的技术。唯一的不同点是，圣杯布局的中间部分（<code>.HolyGrail-body</code>）需要指定 <code>display:flex</code> 来控制子元素的布局。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">header</span>,<span class="selector-tag">footer</span> &#123;</span><br><span class="line">    <span class="attribute">flex</span>: <span class="number">0</span> <span class="number">0</span> <span class="number">50px</span>;</span><br><span class="line">    <span class="attribute">background</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.HolyGrail</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: flex;</span><br><span class="line">    <span class="attribute">min-height</span>: <span class="number">100vh</span>;</span><br><span class="line">    <span class="attribute">flex-direction</span>: column;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.HolyGrail-body</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: flex;</span><br><span class="line">    <span class="attribute">flex</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.HolyGrail-content</span> &#123;</span><br><span class="line">    <span class="attribute">flex</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">background</span>: <span class="number">#4DBCB0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.HolyGrail-nav</span>,</span><br><span class="line"><span class="selector-class">.HolyGrail-ads</span> &#123;</span><br><span class="line">    <span class="attribute">flex</span>: <span class="number">0</span> <span class="number">0</span> <span class="number">12em</span>;</span><br><span class="line">    <span class="attribute">background</span>: <span class="number">#daf1ef</span>;    <span class="comment">/* 12em is the width of the columns */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.HolyGrail-nav</span> &#123;</span><br><span class="line">    <span class="attribute">order</span>: -<span class="number">1</span>;    <span class="comment">/* put the nav on the left */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvZmlsZXMvbGF5b3V0LWhvbHktZ3JhaWwtZXhhbXBsZTMuaHRtbA==">Source Code<i class="fa fa-external-link-alt"></i></span></p>
<h3 id="响应式">响应式</h3>
<p>圣杯布局来自于每个人都使用个人计算机冲浪的互联网时期的网页设计，但是随着移动设备的激增，圣杯布局已渐渐没落。另一方面，使用 Flexbox 布局，创建一个移动设备优先和移动设备友好版本的圣杯布局很简单。其主旨就是把中间的部分默认指定为 <code>flex-direction:column</code>，然后为拥有更宽屏幕的设备指定 <code>flex-direction:row</code> 。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.HolyGrail</span>,</span><br><span class="line"><span class="selector-class">.HolyGrail-body</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: flex;</span><br><span class="line">    <span class="attribute">flex-direction</span>: column;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.HolyGrail-nav</span> &#123;</span><br><span class="line">    <span class="attribute">order</span>: -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line">@media（min-width: 768px）&#123;</span><br><span class="line">    <span class="selector-class">.HolyGrail-body</span> &#123;</span><br><span class="line">        <span class="attribute">flex-direction</span>: row;</span><br><span class="line">        <span class="attribute">flex</span>: <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="selector-class">.HolyGrail-content</span> &#123;</span><br><span class="line">        <span class="attribute">flex</span>: <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="selector-class">.HolyGrail-nav</span>, <span class="selector-class">.HolyGrail-ads</span> &#123;</span><br><span class="line">        <span class="comment">/* 12em is the width of the columns */</span></span><br><span class="line">        <span class="attribute">flex</span>: <span class="number">0</span> <span class="number">0</span> <span class="number">12em</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端开发</category>
      </categories>
  </entry>
  <entry>
    <title>线性模型</title>
    <url>/post/linear-model/</url>
    <content><![CDATA[<p>线性模型试图学得一个通过属性 <span class="math inline">\(\boldsymbol{w}\)</span> 的线性组合来进行预测的函数，即：</p>
<p><span class="math display">\[
f(\boldsymbol{x})=\boldsymbol{w}^T\boldsymbol{x}+b
\]</span></p>
<p>线性模型形式简单、易于建模，但却蕴含着机器学习中一些重要的基本思想。许多功能更为强大的非线性模型可在线性模型的基础上通过引入层级结构或高位映射而得。</p>
<span id="more"></span>
<h2 id="对数几率回归">对数几率回归</h2>
<p>对于分类任务而言，只需要得到一个单调可微函数即可将分类任务的真实标记 <span class="math inline">\(y\)</span> 与线性回归模型的预测值相联系。</p>
<p>最为理想的函数是阶跃函数，它将输入值映射为输出值“0”或“1”。然而，阶跃函数具有不连续、不光滑等不太好的性质，因此实际常用 Sigmoid 函数作为激活函数，Sigmoid 函数能把可能在较大范围内变化的输入值挤压到 <span class="math inline">\((0,1)\)</span> 输出值范围内，因此有时也被称为“挤压函数”。</p>
<div class="note "><p>Sigmoid 函数也叫 Logistic 函数，因此，对数几率回归也叫 Logistic 回归。</p>
</div>
<p>因此，对数几率回归可表示为：</p>
<p><span class="math display">\[
y=\frac{1}{1+e^{-(\boldsymbol{w}^T\boldsymbol{x}+b)}}
\]</span></p>
<p>可变化为：</p>
<p><span class="math display">\[
\ln\frac{y}{1-y}=\boldsymbol{w}^T\boldsymbol{x}+b
\]</span></p>
<p>若将 <span class="math inline">\(y\)</span> 视为样本 <span class="math inline">\(\boldsymbol{x}\)</span> 作为正例的可能性，则 <span class="math inline">\(1-y\)</span> 是其作为反例的可能性，两者的比值：</p>
<p><span class="math display">\[
\frac{y}{1-y}
\]</span></p>
<p>称为“几率”（odds），反映了 <span class="math inline">\(\boldsymbol{x}\)</span> 作为正例的相对可能性。对几率取对数则得到“对数几率”（log odds， logit）:</p>
<p><span class="math display">\[
\ln\frac{y}{1-y}
\]</span></p>
<p>由此可以看出，对数几率回归实际上是在用线性回归模型的预测结果去逼近真实标记的对数几率。</p>
<p>可通过极大似然法来估计 <span class="math inline">\(\boldsymbol{w}\)</span> 和 <span class="math inline">\(b\)</span>。给定数据集 <span class="math inline">\(D=\{(\boldsymbol{x_1},y_1),(\boldsymbol{x_2},y_2),\ldots,(\boldsymbol{x_m},y_m)\},\boldsymbol{x_i}\in\mathbb{R}^n,y_i\in\left\{0,1\right\}\)</span>，对数几率回归模型的对数似然函数为：</p>
<p><span class="math display">\[
\mathcal{l}(\boldsymbol{w},b)=\sum_{i=1}^m\ln p(y_i|\boldsymbol{x_i};\boldsymbol{w},b)
\]</span></p>
<p>令 <span class="math inline">\(p_1(\boldsymbol{x};\boldsymbol{w},b)=p(y=1|\boldsymbol{x};\boldsymbol{w},b)\)</span>、<span class="math inline">\(p_0(\boldsymbol{x};\boldsymbol{w},b)=p(y=0|\boldsymbol{x};\boldsymbol{w},b)\)</span>，则上式中的似然项可重写为：</p>
<p><span class="math display">\[
p(y_i|\boldsymbol{x_i};\boldsymbol{w},b)=y_ip_1(\boldsymbol{x_i};\boldsymbol{w},b)+(1-y_i)p_0(\boldsymbol{x_i};\boldsymbol{w},b)
\]</span></p>
<p>因此，对数似然函数可写为：</p>
<p><span class="math display">\[
\begin{aligned}
\mathcal{l}(\boldsymbol{w},b)&amp;=\sum_{i=1}^m\ln p(y_i|\boldsymbol{x_i};\boldsymbol{w},b)\\
    &amp;=\sum_{i=1}^m\ln\left(y_ip_1(\boldsymbol{x_i};\boldsymbol{w},b)+(1-y_i)p_0(\boldsymbol{x_i};\boldsymbol{w},b))\right)\\
    &amp;=\sum_{i=1}^m\ln\left(y_ip_1(\boldsymbol{x_i};\boldsymbol{w},b)+(1-y_i)(1-p_1(\boldsymbol{x_i};\boldsymbol{w},b))\right)\\
    &amp;=\sum_{i=1}^m\ln\frac{1-y_i+y_ie^{\boldsymbol{w}^T\boldsymbol{x_i}+b}}{1+e^{\boldsymbol{w}^T\boldsymbol{x_i}+b}}\\
    &amp;=\sum_{i=1}^m\left(\ln(1-y_i+y_ie^{\boldsymbol{w}^T\boldsymbol{x_i}+b})-\ln(1+e^{\boldsymbol{w}^T\boldsymbol{x_i}+b})\right)\\
    &amp;=\sum_{i=1}^m\left(y_i(\boldsymbol{w}^T\boldsymbol{x_i}+b)-\ln(1+e^{\boldsymbol{w}^T\boldsymbol{x_i}+b})\right)
\end{aligned}
\]</span></p>
<p>最大化该对数似然函数等价于最小化：</p>
<p><span class="math display">\[
\mathcal{l}(\boldsymbol{w},b)=\sum_{i=1}^m\left(-y_i(\boldsymbol{w}^T\boldsymbol{x_i}+b)+\ln(1+e^{\boldsymbol{w}^T\boldsymbol{x_i}+b})\right)
\]</span></p>
<p>由于该函数为任意阶可导的凸函数，现有的许多数值优化算法都可直接用于求取最优解，如梯度下降法、牛顿迭代法等。以梯度下降法为例，关于 <span class="math inline">\(\boldsymbol{w},b\)</span> 的一阶导数为：</p>
<p><span class="math display">\[
\begin{aligned}
\frac{\partial\mathcal{l}(\boldsymbol{w},b)}{\partial\boldsymbol{w}}&amp;=\sum_{i=1}^m\left(-y_i\boldsymbol{x_i}+\frac{e^{\boldsymbol{w}^T\boldsymbol{x_i}+b}}{1+e^{\boldsymbol{w}^T\boldsymbol{x_i}+b}}\boldsymbol{x_i}\right)\\
    &amp;=-\sum_{i=1}^m(y_i-p_1(\boldsymbol{x};\boldsymbol{w},b))\boldsymbol{x_i}
\end{aligned}
\]</span></p>
<p><span class="math display">\[
\begin{aligned}
\frac{\partial\mathcal{l}(\boldsymbol{w},b)}{\partial b}&amp;=\sum_{i=1}^m\left(-y_i+\frac{e^{\boldsymbol{w}^T\boldsymbol{x_i}+b}}{1+e^{\boldsymbol{w}^T\boldsymbol{x_i}+b}}\right)\\
    &amp;=-\sum_{i=1}^m(y_i-p_1(\boldsymbol{x};\boldsymbol{w},b))
\end{aligned}
\]</span></p>
<p>因此，对数几率回归模型的学习算法（梯度下降法）如下：</p>
<div class="note primary"><p><strong>输入：</strong> 训练集 <span class="math inline">\(D=\{(\boldsymbol{x_k},y_k)\}_{i=1}^m\)</span>；学习率 <span class="math inline">\(\eta\)</span>； <strong>输出：</strong> 权重 <span class="math inline">\(\boldsymbol{w}\)</span>，截距 <span class="math inline">\(b\)</span>；</p>
<p><strong>过程：</strong></p>
<ol type="1">
<li>随机初始化权重 <span class="math inline">\(\boldsymbol{w}\)</span>，截距 <span class="math inline">\(b\)</span></li>
<li>计算梯度项 <span class="math inline">\(\frac{\partial\mathcal{l}(\boldsymbol{w},b)}{\partial\boldsymbol{w}}\)</span>，<span class="math inline">\(\frac{\partial\mathcal{l}(\boldsymbol{w},b)}{\partial b}\)</span></li>
<li>更新权重 <span class="math inline">\(\boldsymbol{w}=\boldsymbol{w}-\eta\frac{\partial\mathcal{l}(\boldsymbol{w},b)}{\partial\boldsymbol{w}}\)</span>，截距 <span class="math inline">\(b=b-\eta\frac{\partial\mathcal{l}(\boldsymbol{w},b)}{\partial b}\)</span></li>
<li>若模型收敛或达到其他条件，终止；否则转步（2）</li>
</ol>
</div>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>对数几率回归</tag>
      </tags>
  </entry>
  <entry>
    <title>《机器学习》公式推导</title>
    <url>/post/machine-learning-derivation/</url>
    <content><![CDATA[<div class="note info"><ul>
<li>书籍名称：机器学习，周志华</li>
<li>阅读地址：<span class="exturl" data-url="aHR0cHM6Ly9ib29rLmRvdWJhbi5jb20vc3ViamVjdC8yNjcwODExOS8=">https://book.douban.com/subject/26708119/<i class="fa fa-external-link-alt"></i></span></li>
</ul>
</div>
<span id="more"></span>
<h2 id="线性模型">线性模型</h2>
<h3 id="对数几率回归">对数几率回归</h3>
<p>给定数据集 <span class="math inline">\(D=\{(\boldsymbol{x_1},y_1),(\boldsymbol{x_2},y_2),\ldots,(\boldsymbol{x_m},y_m)\},\boldsymbol{x_i}\in\mathbb{R}^n,y_i\in\left\{0,1\right\}\)</span>，对数几率回归模型的对数似然函数为：</p>
<p><span class="math display">\[
\mathcal{l}(\boldsymbol{w},b)=\sum_{i=1}^m\ln p(y_i|\boldsymbol{x_i};\boldsymbol{w},b)
\]</span></p>
<p>令 <span class="math inline">\(\boldsymbol{\beta}=(\boldsymbol{w},b)\)</span>、<span class="math inline">\(\boldsymbol{\hat{x}}=(\boldsymbol{x};1)\)</span>，则 <span class="math inline">\(\boldsymbol{w}^T\boldsymbol{x}+b\)</span> 可简写为 <span class="math inline">\(\boldsymbol{\beta}^T\boldsymbol{\hat{x}}\)</span>。再令 <span class="math inline">\(p_1(\boldsymbol{\hat{x}};\boldsymbol{\beta})=p(y=1|\boldsymbol{\hat{x}};\boldsymbol{\beta})\)</span>、<span class="math inline">\(p_0(\boldsymbol{\hat{x}};\boldsymbol{\beta})=p(y=0|\boldsymbol{\hat{x}};\boldsymbol{\beta})\)</span>，则上式中的似然项可重写为：</p>
<p><span class="math display">\[
p(y_i|\boldsymbol{x_i};\boldsymbol{w},b)=y_ip_1(\boldsymbol{\hat{x_i}};\boldsymbol{\beta})+(1-y_i)p_0(\boldsymbol{\hat{x_i}};\boldsymbol{\beta})
\]</span></p>
<p>因此，对数似然函数可写为：</p>
<p><span class="math display">\[
\begin{aligned}
\mathcal{l}(\boldsymbol{w},b)&amp;=\sum_{i=1}^m\ln p(y_i|\boldsymbol{x_i};\boldsymbol{w},b)\\
    &amp;=\sum_{i=1}^m\ln\left(y_ip_1(\boldsymbol{\hat{x_i}};\boldsymbol{\beta})+(1-y_i)p_0(\boldsymbol{\hat{x_i}};\boldsymbol{\beta}))\right)\\
    &amp;=\sum_{i=1}^m\ln\left(y_ip_1(\boldsymbol{\hat{x_i}};\boldsymbol{\beta})+(1-y_i)(1-p_1(\boldsymbol{\hat{x_i}};\boldsymbol{\beta}))\right)\\
    &amp;=\sum_{i=1}^m\ln\frac{1-y_i+y_ie^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}}}{1+e^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}}}\\
    &amp;=\sum_{i=1}^m\left(\ln(1-y_i+y_ie^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}})-\ln(1+e^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}})\right)\\
    &amp;=\sum_{i=1}^m\left(y_i\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}-\ln(1+e^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}})\right)
\end{aligned}
\]</span></p>
<p>最大化该对数似然函数等价于最小化：</p>
<p><span class="math display">\[
\mathcal{l}(\boldsymbol{\beta})=\sum_{i=1}^m\left(-y_i\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}+\ln(1+e^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}})\right)
\]</span></p>
<p>关于 <span class="math inline">\(\boldsymbol{\beta}\)</span> 的一阶导数为：</p>
<p><span class="math display">\[
\begin{aligned}
\frac{\partial\mathcal{l}(\boldsymbol{\beta})}{\partial\boldsymbol{\beta}}&amp;=\sum_{i=1}^m\left(-y_i\boldsymbol{\hat{x_i}}+\frac{e^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}}}{1+e^{\boldsymbol{\beta}^T\boldsymbol{\hat{x_i}}}}\boldsymbol{\hat{x_i}}\right)\\
    &amp;=-\sum_{i=1}^m(y_i-p_1(\boldsymbol{\hat{x}};\boldsymbol{\beta}))\boldsymbol{\hat{x_i}}
\end{aligned}
\]</span></p>
<h2 id="神经网络">神经网络</h2>
<h3 id="误差逆传播算法">误差逆传播算法</h3>
<p>为方便讨论，给定训练集 <span class="math inline">\(D=\{(\boldsymbol{x_1},\boldsymbol{y_1}),(\boldsymbol{x_2},\boldsymbol{y_2}),\ldots,(\boldsymbol{x_n},\boldsymbol{y_n})\},\boldsymbol{x_i}\in\mathbb{R}^d,\boldsymbol{y_i}\in\mathbb{R}^l\)</span>，即输入示例由 d 个属性描述、输出 l 维实值向量。给出一个拥有 d 个输入神经元、l 个输出神经元以及 q 个隐层神经元的多层前馈网络结构，假设隐层和输出层神经元都使用 Sigmoid 函数。</p>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/bp-neural-network-example.svg" alt="" /><figcaption>BP 网络及算法中的变量符号</figcaption>
</figure>
<p>可以看出：BP 算法首先将误差反向传播给隐层神经元，调节隐层到输出层的连接权重与输出层神经元的阈值；接着根据隐含层神经元的均方误差，来调节输入层到隐含层的连接权值与隐含层神经元的阈值。</p>
<p>假定神经网络的输出为 <span class="math inline">\(\boldsymbol{\hat{y}_k}=\left(\hat{y}_1^k,\hat{y}_2^k,\dots,\hat{y}_l^k\right)\)</span>，即 <span class="math inline">\(\hat{y}_j^k=f\left(\beta_j-\theta_j\right)\)</span>；神经网络的隐层输出为 <span class="math inline">\(\boldsymbol{\hat{b}_k}=\left(\hat{b}_1^k,\hat{b}_2^k,\dots,\hat{b}_q^k\right)\)</span>，即 <span class="math inline">\(\hat{b}_h^k=f\left(\alpha_h-\gamma_h\right)\)</span>。</p>
<p>因此，神经网络在单个训样例 <span class="math inline">\(\left(\boldsymbol{x_k},\boldsymbol{y_k}\right)\)</span> 上的均方误差为：</p>
<p><span class="math display">\[
E_k=\frac{1}{2}\sum_{j=1}^l\left(\hat{y}_j^k-y_j^k\right)^2
\]</span></p>
<p>BP 算法基本的推导过程与感知机的推导过程原理是相同的，给定学习率 <span class="math inline">\(\eta\)</span>，有</p>
<p><span class="math display">\[
\begin{aligned}
\Delta w_{hj}&amp;=-\eta\cdot\frac{\partial E_k}{\partial w_{hj}}\\
             &amp;=-\eta\cdot\frac{\partial E_k}{\partial\hat{y}_j^k}\cdot\frac{\partial\hat{y}_j^k}{\partial\beta_j}\cdot\frac{\partial\beta_j}{\partial w_{hj}}\\
             &amp;=-\eta\cdot\left(\hat{y}_j^k-y_j^k\right)\cdot f^\prime\left(\beta_j-\theta_j\right)\cdot b_h\\
             &amp;=-\eta\cdot\left(\hat{y}_j^k-y_j^k\right)\cdot f\left(\beta_j-\theta_j\right)\left(1-f\left(\beta_j-\theta_j\right)\right)\cdot b_h\\
             &amp;=-\eta\cdot\left(\hat{y}_j^k-y_j^k\right)\cdot\hat{y}_j^k\left(1-\hat{y}_j^k\right)\cdot b_h\\
             &amp;=\eta b_h\hat{y}_j^k\left(1-\hat{y}_j^k\right)\left(y_j^k-\hat{y}_j^k\right)\\
\Delta \theta_j&amp;=-\eta\cdot\frac{\partial E_k}{\partial\theta_j}\\
               &amp;=-\eta\cdot\frac{\partial E_k}{\partial\hat{y}_j^k}\cdot\frac{\partial\hat{y}_j^k}{\partial\theta_j}\\
               &amp;=-\eta\cdot\left(\hat{y}_j^k-y_j^k\right)\cdot (-1)f^\prime\left(\beta_j-\theta_j\right)\\
               &amp;=\eta\hat{y}_j^k\left(1-\hat{y}_j^k\right)\left(\hat{y}_j^k-y_j^k\right)\\
\Delta v_{ih}&amp;=-\eta\cdot\frac{\partial E_k}{\partial v_{ih}}\\
             &amp;=-\eta\cdot\sum_{j=1}^{l}\frac{\partial E_k}{\partial\hat{y}_j^k}\cdot\frac{\partial\hat{y}_j^k}{\partial\beta_h}\cdot\frac{\partial\beta_j}{\partial b_h}\cdot\frac{\partial b_h}{\partial\alpha_h}\cdot\frac{\partial\alpha_h}{\partial v_{ih}}\\
             &amp;=-\eta\cdot\sum_{j=1}^{l}\left(\hat{y}_j^k-y_j^k\right)\cdot\hat{y}_j^k\left(1-\hat{y}_j^k\right)\cdot w_{hj}\cdot f^\prime\left(\alpha_h-\gamma_h\right)\cdot x_i\\
             &amp;=\eta x_ib_h(1-b_h)\sum_{j=1}^lw_{hj}\hat{y}_j^k\left(1-\hat{y}_j^k\right)\left(y_j^k-\hat{y}_j^k\right)\\
\Delta \gamma_h&amp;=-\eta\cdot\frac{\partial E_k}{\partial v_{ih}}\\
               &amp;=-\eta\cdot\sum_{j=1}^{l}\frac{\partial E_k}{\partial\hat{y}_j^k}\cdot\frac{\partial\hat{y}_j^k}{\partial\beta_h}\cdot\frac{\partial\beta_j}{\partial b_h}\cdot\frac{\partial b_h}{\partial\gamma_h}\\
               &amp;=-\eta\cdot\sum_{j=1}^{l}\left(\hat{y}_j^k-y_j^k\right)\cdot\hat{y}_j^k\left(1-\hat{y}_j^k\right)\cdot w_{hj}\cdot(-1)f^\prime\left(\alpha_h-\gamma_h\right)\\
               &amp;=\eta b_h(1-b_h)\sum_{j=1}^lw_{hj}\hat{y}_j^k\left(1-\hat{y}_j^k\right)\left(\hat{y}_j^k-y_j^k\right)
\end{aligned}
\]</span></p>
<p>综上所述，不妨令 <span class="math inline">\(g_j=\hat{y}_j^k\left(1-\hat{y}_j^k\right)\left(y_j^k-\hat{y}_j^k\right)\)</span>，<span class="math inline">\(e_h=b_h(1-b_h)\sum_{j=1}^lw_{hj}g_j\)</span>，则有</p>
<p><span class="math display">\[
\Delta w_{hj}=\eta g_jb_h,\Delta\theta_j=-\eta g_j,\Delta v_{ih}=\eta e_hx_i,\Delta\gamma_h=-\eta e_h
\]</span></p>
<p>任意参数 <span class="math inline">\(v\)</span> 的更新估计式为</p>
<p><span class="math display">\[
v\leftarrow v+\Delta v
\]</span></p>
<p>BP 算法将不断对模型的权重及阈值进行调整，直至模型收敛为止。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
  </entry>
  <entry>
    <title>神经网络</title>
    <url>/post/neural-network/</url>
    <content><![CDATA[<p>无论是线性回归还是逻辑回归都有这样一个缺点，即：当特征太多时，计算的负荷会非常大。普通的模型无法有效处理这么多的特征，这时候我们需要神经网络。</p>
<p>神经网络是由具有适应性的简单单元组成的广泛并行互连的网络，它的组织能够模拟生物神经系统对真实世界物体所作出的交互反应。</p>
<span id="more"></span>
<h2 id="神经元模型">神经元模型</h2>
<p>神经网络中最基本的成分是神经元（neuron）模型，即上述定义中的“简单单元”。类似于生物神经网络，每个神经元接受来自其他神经元的信号改变自身的激活状态，将上述过程抽象为如下的“ M-P 神经元模型”。</p>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/mp-neuron-model.svg" alt="" /><figcaption>M-P神经元模型</figcaption>
</figure>
<p>神经元接受来自 n 个其他神经元传递过来的输入信号，这些输入信号通过带权重的连接进行传递，神经元接受到的总输入值将与神经元的阈值 <span class="math inline">\(\theta\)</span> 进行比较，然后通过“激活函数”处理以产生神经元的输出。</p>
<div class="note "><p>理想中的激活函数是阶跃函数，它将输入值映射为输出值“0”或“1”。然而，阶跃函数具有不连续、不光滑等不太好的性质，因此实际常用 Sigmoid 函数作为激活函数，Sigmoid 函数能把可能在较大范围内变化的输入值挤压到 <span class="math inline">\((0,1)\)</span> 输出值范围内，因此有时也被称为“挤压函数”。</p>
</div>
<div class="note primary"><p>若使用线性函数 <span class="math inline">\(f\left(\boldsymbol{x}\right)=\boldsymbol{w}^T\boldsymbol{x}\)</span> 用作神经元激活函数有什么缺陷么？</p>
<p>使用线性函数作为激活函数时，无论是在隐藏层还是在输出层（无论传递几层），其单元值（在使用激活函数之前）都还是输入 <span class="math inline">\(x\)</span> 的线性组合，这个时候的神经网络其实等价于逻辑回归的，若输出层也使用线性函数作为激活函数，那么就等价于线性回归。</p>
</div>
<p>阈值 <span class="math inline">\(\theta\)</span> 可看作一个固定输入为 -1.0 的“哑节点”所对应的连接权重 <span class="math inline">\(w_{n+1}\)</span>，这样权重和阈值的学习就可统一为权重的学习。</p>
<p>把许多个这样的神经元按一定的层次结构连接起来，就得到了神经网络。</p>
<h2 id="感知机与多层网络">感知机与多层网络</h2>
<h3 id="感知机模型">感知机模型</h3>
<p>感知机（Perceptron）由两层神经元组成，输入层接收外界输入信号后传递给输出层，输出层是“M-P神经元”，亦称“阈值逻辑单元”（threshold logic unit）。感知机模型的公式可表示为：</p>
<p><span class="math display">\[
\hat{y}=f\left(\boldsymbol{w}^T\boldsymbol{x}\right)
\]</span></p>
<p>其中，<span class="math inline">\(y_i\in\{0,1\}\)</span> 为感知机模型的输出；<span class="math inline">\(\boldsymbol{x}\in\mathbb{R}^{n+1}(\boldsymbol{x}_{n+1}\equiv-1)\)</span> 为样本的特征向量，是感知机模型的输入；<span class="math inline">\(\boldsymbol{w}\in\mathbb{R}^{n+1}\)</span> 为感知机模型的权重。</p>
<p>感知机能容易地实现与、或、非运算。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: center;">运算</th>
<th style="text-align: center;"><span class="math inline">\(w_1\)</span></th>
<th style="text-align: center;"><span class="math inline">\(w_2\)</span></th>
<th style="text-align: center;"><span class="math inline">\(\theta\)</span></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;">与</td>
<td style="text-align: center;">1</td>
<td style="text-align: center;">1</td>
<td style="text-align: center;">2</td>
</tr>
<tr class="even">
<td style="text-align: center;">或</td>
<td style="text-align: center;">1</td>
<td style="text-align: center;">1</td>
<td style="text-align: center;">0.5</td>
</tr>
<tr class="odd">
<td style="text-align: center;">非</td>
<td style="text-align: center;">-0.6</td>
<td style="text-align: center;"></td>
<td style="text-align: center;">-0.5</td>
</tr>
</tbody>
</table>
<p>更一般地，给定训练数据集，权重 <span class="math inline">\(w_i,(i=1,2,\dots,n+1)\)</span> 可通过学习得到， 感知机的学习规则较为简单，对训练数据 <span class="math inline">\(D=\{(\boldsymbol{x_1},y_1),(\boldsymbol{x_2},y_2),\ldots,(\boldsymbol{x_n},y_n)\}\)</span>，感知机的学习目标是求得对数据中的正负样本能够完全正确划分的超平面：</p>
<p><span class="math display">\[
\boldsymbol{w}^T\boldsymbol{x}=0
\]</span></p>
<p>给定损失函数：</p>
<p><span class="math display">\[
L(\boldsymbol{w})=\sum_{i=1}^n(\hat{y}_i-y_i)\boldsymbol{w}^T\boldsymbol{x_i}=\sum_{i=1}^n\sum_{j=1}^{n+1}w_jx_{i,j}(\hat{y}_i-y_i)
\]</span></p>
<p>基于梯度下降法，以目标的负梯度方向对参数 <span class="math inline">\(\boldsymbol{w}\)</span> 进行调整，给定学习率 <span class="math inline">\(\eta\in(0,1)\)</span>，有：</p>
<p><span class="math display">\[
\Delta w_i=-\eta\frac{\partial L(\boldsymbol{w})}{\partial w_i}=-\eta\sum_{j=1}^n(\hat{y}_j-y_j)x_{j,i}=\eta\sum_{j=1}^n(y_j-\hat{y}_j)x_{j,i}
\]</span></p>
<p>因此，感知机权重调整公式为：</p>
<p><span class="math display">\[
w_i\leftarrow w_i+\Delta w_i,\Delta w_i=\eta\sum_{j=1}^n(y_j-\hat{y}_j)x_{j,i}
\]</span></p>
<p>其中 <span class="math inline">\(\eta\in(0,1)\)</span> 称为学习率，感知机将根据错误的程度对权重进行调整，直至收敛为止。</p>
<h3 id="多层前馈神经网络">多层前馈神经网络</h3>
<p>感知机只有输出层神经元进行激活函数处理，即只拥有一层功能神经元，其学习能力非常有限，仅能处理线性可分问题。若为非线性可分问题（如“异或”问题），感知机在学习过程中将发生振荡，<span class="math inline">\(\boldsymbol{w}=(w_1;w_2;\dots;w_n;w_{n+1})\)</span> 无法稳定下来，不能求得合适解。</p>
<p>为解决该问题，考虑使用多层功能神经元。例如，在输出层与输入层之间增加一层神经元，被称为隐层或隐含层，隐含层和输出层神经元都是拥有激活函数的功能神经元。</p>
<p>一般的，常见的神经网络是如下所示的层级结构，每层神经元与下一次神经元全互连，神经元之间不存在同层连接，也不存在跨层连接。这样的神经网络结构通常称为“多层前馈神经网络”，其中输入层神经元接收外界输入，隐层（可包含多层）与输出层神经元对信号进行加工，最终结果由输出层神经元输出。</p>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/multi-layer-feedforward-neural-networks.svg" alt="" /><figcaption>多层前馈神经网络结构示意图</figcaption>
</figure>
<h2 id="误差逆传播算法">误差逆传播算法</h2>
<p>误差逆传播算法（error BackPropagation）算法时迄今最为成功的神经网络学习算法，BP 算法不仅可用于多层前馈神经网络，还可同于其他类型的神经网络，例如训练递归神经网络。为方便讨论，给出如下定义：</p>
<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: center;">符号</th>
<th style="text-align: center;">描述</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;"><span class="math inline">\(\boldsymbol{x}\)</span></td>
<td style="text-align: center;">输入向量</td>
</tr>
<tr class="even">
<td style="text-align: center;"><span class="math inline">\(\boldsymbol{y}\)</span></td>
<td style="text-align: center;">目标向量</td>
</tr>
<tr class="odd">
<td style="text-align: center;"><span class="math inline">\(C\)</span></td>
<td style="text-align: center;">误差函数</td>
</tr>
<tr class="even">
<td style="text-align: center;"><span class="math inline">\(L\)</span></td>
<td style="text-align: center;">网络层数</td>
</tr>
<tr class="odd">
<td style="text-align: center;"><span class="math inline">\(\boldsymbol{W^l}=(w_{jk}^l)\)</span></td>
<td style="text-align: center;"><span class="math inline">\(l-1\)</span> 层至 <span class="math inline">\(l\)</span> 层的权重，<span class="math inline">\(w_{jk}^l\)</span> 为 <span class="math inline">\(l-1\)</span> 层第 <span class="math inline">\(k\)</span> 个节点与 <span class="math inline">\(l\)</span> 层第 <span class="math inline">\(j\)</span> 个节点之间的权重</td>
</tr>
<tr class="even">
<td style="text-align: center;"><span class="math inline">\(\boldsymbol{b^l}=(b_k)\)</span></td>
<td style="text-align: center;"><span class="math inline">\(l-1\)</span> 层至 <span class="math inline">\(l\)</span> 层的阈值，<span class="math inline">\(b_k\)</span> 为 <span class="math inline">\(l\)</span> 层第 <span class="math inline">\(k\)</span> 个节点的阈值</td>
</tr>
<tr class="odd">
<td style="text-align: center;"><span class="math inline">\(f^l\)</span></td>
<td style="text-align: center;"><span class="math inline">\(l\)</span> 层的激活函数</td>
</tr>
</tbody>
</table>
<p>因此，整个多层前馈神经网络可表示为多个函数的结合：</p>
<p><span class="math display">\[
g(\boldsymbol{x}):=f^{L}(\boldsymbol{W}^{L}f^{L-1}(\boldsymbol{W}^{L-1}\ldots f^{1}(\boldsymbol{W}^{1}\boldsymbol{x}+\boldsymbol{b}^1)\ldots+\boldsymbol{b}^{L-1})+\boldsymbol{b}^{L})
\]</span></p>
<p>给定单个训样例 <span class="math inline">\(\left(\boldsymbol{x},\boldsymbol{y}\right)\)</span> 上，其损失函数为：</p>
<p><span class="math display">\[
C(\boldsymbol{y},g(\boldsymbol{x}))
\]</span></p>
<p>为方便使用梯度下降法求解损失函数的最小值，定义</p>
<ul>
<li><span class="math inline">\(\boldsymbol{z}^l:=\boldsymbol{W}^l\boldsymbol{\alpha}^{l-1}+\boldsymbol{b}^l\)</span>：第 <span class="math inline">\(l\)</span> 层的加权输入</li>
<li><span class="math inline">\(\boldsymbol{\alpha}^{l}:=f^l(\boldsymbol{z}^{l})\)</span>：第 <span class="math inline">\(l\)</span> 层的输出，其中<span class="math inline">\(\boldsymbol{\alpha}^{0}=\boldsymbol{x}\)</span></li>
</ul>
<p>因此，损失函数 <span class="math inline">\(C\)</span> 对于每一层的权重 <span class="math inline">\(\boldsymbol{W}^l\)</span> 的偏导函数为：</p>
<p><span class="math display">\[
\begin{aligned}
\frac{\partial C}{\partial\boldsymbol{W}^l}&amp;=\frac{\partial C}{\partial\boldsymbol{\alpha}^L}\cdot\frac{\partial\boldsymbol{\alpha}^L}{\partial\boldsymbol{z}^L}\cdot\frac{\partial\boldsymbol{z}^{L}}{\partial\boldsymbol{\alpha}^{L-1}}\cdot\ldots\cdot\frac{\partial\boldsymbol{z}^{l+1}}{\partial\boldsymbol{\alpha}^{l}}\cdot\frac{\partial\boldsymbol{\alpha}^l}{\partial\boldsymbol{z}^l}\cdot\frac{\partial\boldsymbol{z}^{l}}{\partial\boldsymbol{W}^l}\\
    &amp;=(f^{l})^{\prime}\cdot(\boldsymbol{W}^{l+1})^T\odot\ldots\odot(\boldsymbol{W}^{L-1})^T\cdot(f^{L})^{\prime}\odot\frac{\partial C}{\partial\boldsymbol{\alpha}^L}\cdot(\boldsymbol{a}_{l-1})^T
\end{aligned}
\]</span></p>
<p>损失函数 <span class="math inline">\(C\)</span> 对于每一层的阈值 <span class="math inline">\(\boldsymbol{b^l}\)</span> 的偏导函数为：</p>
<p><span class="math display">\[
\begin{aligned}
\frac{\partial C}{\partial\boldsymbol{b}^l}&amp;=\frac{\partial C}{\partial\boldsymbol{\alpha}^L}\cdot\frac{\partial\boldsymbol{\alpha}^L}{\partial\boldsymbol{z}^L}\cdot\frac{\partial\boldsymbol{z}^{L}}{\partial\boldsymbol{\alpha}^{L-1}}\cdot\ldots\cdot\frac{\partial\boldsymbol{z}^{l+1}}{\partial\boldsymbol{\alpha}^{l}}\cdot\frac{\partial\boldsymbol{\alpha}^l}{\partial\boldsymbol{z}^l}\cdot\frac{\partial\boldsymbol{z}^{l}}{\partial\boldsymbol{b}^l}\\
    &amp;=(f^{l})^{\prime}\cdot(\boldsymbol{W}^{l+1})^T\odot\ldots\odot(\boldsymbol{W}^{L-1})^T\cdot(f^{L})^{\prime}\odot\frac{\partial C}{\partial\boldsymbol{\alpha}^L}
\end{aligned}
\]</span></p>
<p>为方便计算神经网络中损失函数 <span class="math inline">\(C\)</span> 对于每一层的权重 <span class="math inline">\(\boldsymbol{W}^l\)</span> 与阈值 <span class="math inline">\(\boldsymbol{b^l}\)</span> 的偏导函数，引入辅助变量 <span class="math inline">\(\boldsymbol{\delta}^l\)</span>：</p>
<p><span class="math display">\[
\boldsymbol{\delta}^l:=(f^{l})^{\prime}\odot(\boldsymbol{W}^{l+1})^T\odot\ldots\odot(\boldsymbol{W}^{L-1})^T\cdot(f^{L})^{\prime}\odot\frac{\partial C}{\partial\boldsymbol{\alpha}^L}
\]</span></p>
<p>所以，<span class="math inline">\(\boldsymbol{\delta}^l\)</span> 可被循环计算如下：</p>
<p><span class="math display">\[
\left\{
\begin{aligned}
\boldsymbol{\delta}^{l-1}&amp;=(f^{l-1})^\prime\cdot(\boldsymbol{W}^{l})^{T}\boldsymbol{\delta}^l\\
\boldsymbol{\delta}^{L}&amp;=(f^{L})^\prime\odot\frac{\partial C}{\partial\boldsymbol{\alpha}^L}
\end{aligned}
\right.
\]</span></p>
<p>所以，每一层的权重 <span class="math inline">\(\boldsymbol{W}^l\)</span> 与阈值 <span class="math inline">\(\boldsymbol{b^l}\)</span> 的偏导函数可表示为：</p>
<p><span class="math display">\[
\left\{
\begin{aligned}
\frac{\partial C}{\partial\boldsymbol{W}^l}&amp;=\boldsymbol{\delta}^l\cdot(\boldsymbol{a}_{l-1})^T\\
\frac{\partial C}{\partial\boldsymbol{b}^l}&amp;=\boldsymbol{\delta}^l\\
\end{aligned}
\right.
\]</span></p>
<p>因此，多层前馈神经网络模型的学习算法（误差逆传播算法）如下：</p>
<div class="note primary"><p><strong>输入：</strong> 训练集 <span class="math inline">\(D=\{(\boldsymbol{x_k},\boldsymbol{y_k})\}_{i=1}^m\)</span>；学习率 <span class="math inline">\(\eta\)</span>； <strong>输出：</strong> 连接权 <span class="math inline">\(\boldsymbol{W^l}\)</span>与阈值 <span class="math inline">\(\boldsymbol{b^l}\)</span> 确定的多层前馈神经网络；</p>
<p><strong>过程：</strong></p>
<ol type="1">
<li>在 <span class="math inline">\((0,1)\)</span> 范围内随机初始化网络中所有连接权 <span class="math inline">\(\boldsymbol{W^l}\)</span> 与阈值 <span class="math inline">\(\boldsymbol{b^l}\)</span></li>
<li>循环计算辅助变量 <span class="math inline">\(\boldsymbol{\delta}^l,l=(L,L-1,\ldots,1)\)</span></li>
<li>计算梯度项 <span class="math inline">\(\frac{\partial C}{\partial\boldsymbol{W}^l}\)</span>，<span class="math inline">\(\frac{\partial C}{\partial\boldsymbol{b}^l}\)</span></li>
<li>更新网络中所有连接权 <span class="math inline">\(\boldsymbol{W^l}=\boldsymbol{W^l}-\eta\frac{\partial C}{\partial\boldsymbol{W}^l}\)</span>，阈值 <span class="math inline">\(\boldsymbol{b^l}=\boldsymbol{b^l}-\eta\frac{\partial\mathcal{l}(\boldsymbol{w},b)}{\partial b}\)</span></li>
<li>若模型收敛或达到其他条件，终止；否则转步（2）</li>
</ol>
</div>
<ul>
<li>网络结构：真正要决定的是隐藏层的层数和每个中间层的单元数，如果隐藏层数大于 1，确保每个隐藏层的单元个数相同，通常情况下隐藏层单元的个数越多越好。</li>
</ul>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL1NpZ25vcmlub1kvbWFyaW5l">程序实现<i class="fa fa-external-link-alt"></i></span></p>
<h2 id="其他神经网络">其他神经网络</h2>
<h2 id="深度学习">深度学习</h2>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>神经网络</tag>
      </tags>
  </entry>
  <entry>
    <title>Oh My Zsh 使用指南</title>
    <url>/post/oh-my-zsh-tutorial/</url>
    <content><![CDATA[<p><span class="exturl" data-url="aHR0cHM6Ly9vaG15ei5zaC8=">Oh My Zsh<i class="fa fa-external-link-alt"></i></span> 是基于 zsh 命令行的一个扩展工具集，提供了丰富的扩展功能。</p>
<span id="more"></span>
<h2 id="安装">安装</h2>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt-get install zsh</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sh -c &quot;$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)&quot;</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 国内镜像</span></span><br><span class="line">sh -c &quot;$(wget -O- https://gitee.com/mirrors/oh-my-zsh/raw/master/tools/install.sh)&quot;</span><br></pre></td></tr></table></figure>
<h2 id="插件">插件</h2>
<p><code>zsh-autosuggestions</code></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/zsh-users/zsh-autosuggestions $&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;/plugins/zsh-autosuggestions</span><br></pre></td></tr></table></figure>
<p><code>zsh-syntax-highlighting</code></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/zsh-users/zsh-syntax-highlighting.git $&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;/plugins/zsh-syntax-highlighting</span><br></pre></td></tr></table></figure>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"><span class="deletion">- plugins=(git)</span></span><br><span class="line"><span class="addition">+ plugins=(git zsh-autosuggestions zsh-syntax-highlighting)</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
  </entry>
  <entry>
    <title>The R\Python Graph Gallery</title>
    <url>/post/r-python-graph-gallery/</url>
    <content><![CDATA[<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/united-states-economic-time-series.png" alt="" /><figcaption>United States Economic Time Series</figcaption>
</figure>
<span id="more"></span>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvdW5pdGVkX3N0YXRlc19lY29ub21pY190aW1lX3Nlcmllcy5y">Source Code<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/corruption-and-human-development.png" alt="" /><figcaption>Corruption and Human Development</figcaption>
</figure>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvY29ycnVwdGlvbl9hbmRfaHVtYW5fZGV2ZWxvcG1lbnQucg==">Source Code<i class="fa fa-external-link-alt"></i></span> | <span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvZGF0YS9oZGlfY3BpLmNzdg==">Data<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/population-in-america-counties-2012.png" alt="" /><figcaption>Population in America Counties, 2012</figcaption>
</figure>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvcG9wdWxhdGlvbl9pbl9hbWVyaWNhX2NvdW50aWVzXzIwMTIucg==">Source Code<i class="fa fa-external-link-alt"></i></span> | <span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvZGF0YS91c19wb3AuY3N2">Data<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
]]></content>
      <categories>
        <category>闲来无事</category>
      </categories>
  </entry>
  <entry>
    <title>单链表（1）</title>
    <url>/post/singly-linked-list-1/</url>
    <content><![CDATA[<p>单向链表（又名单链表、线性链表）是链表的一种，其特点是链表的链接方向是单向的，对链表的访问要通过从头部开始，依序往下读取。</p>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/singly-linked-list.png" alt="" /><figcaption>单链表，其结点包含两个字段：整数值和指向下一个结点的指针</figcaption>
</figure>
<span id="more"></span>
<ul>
<li>线性表的链式存储结构的特点是用一组任意的存储单元存储线性表中的数据元素，这组存储单元可以存放在内存中未被占用的任意位置。</li>
<li>相比顺序存储结构，链式存储结构中，除了需要存储数据元素信息之外，还需要存储它的后继元素的存储地址（指针）。</li>
<li>如果链表的每个结点中只包含一个指针域，那就叫做单链表。</li>
</ul>
<p>单链表具有如下优点：</p>
<ul>
<li>动态大小</li>
<li>易于插入与删除</li>
</ul>
<p>同时，也具有如下缺点：</p>
<ul>
<li>不允许随机访问，我们必须从第一个结点开始按顺序访问元素。</li>
<li>每个元素都需要额外的指针内存空间。</li>
</ul>
<h2 id="单链表的表示">单链表的表示</h2>
<p>结点构造 &lt;--&gt; 数据字段 + 指针字段</p>
<ul>
<li>数据字段：元素自身信息</li>
<li>指针字段：指明本元素的直接后继（存储）位置</li>
</ul>
<p>链表由指向链表的第一个结点的指针来进行唯一表示。</p>
<p>第一个结点称为 <code>head</code>。有时为了操作方便，第一个结点的数据字段一般不存放具体的值，指针字段存放指向下一个结点的指针。如果链表为空，则 <code>head</code> 的指针字段为空指针 <code>NULL</code>。</p>
<blockquote>
<p>（本文所写的单链表也是带头结点的单链表）</p>
</blockquote>
<p>单链表中的最后一个结点由于没有后继，所以它的指针字段为空指针 <code>NULL</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Node</span> &#123;</span></span><br><span class="line">    Elemtype data;</span><br><span class="line">    Node *next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表的初始化操作">单链表的初始化操作</h2>
<p>创建一个空结点作为头结点，只记录下一个结点的地址，方便后面进行插入与删除操作，并返回头结点地址作为该链表的唯一标识。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始化链表，返回其头结点地址</span></span><br><span class="line"><span class="function">Node *<span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建头结点并动态分配内存</span></span><br><span class="line">    Node *head = (Node*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    <span class="comment">// 若分配内存失败则报错</span></span><br><span class="line">    <span class="keyword">if</span> (head == <span class="literal">NULL</span>) &#123;</span><br><span class="line">       <span class="built_in">printf</span>(<span class="string">&quot;Error - unable to allocate required memory\n&quot;</span>);</span><br><span class="line">       <span class="built_in">exit</span>(<span class="number">-2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 初始化头结点，使数据字段与指针字段皆为 NULL</span></span><br><span class="line">    head-&gt;data = <span class="literal">NULL</span>;</span><br><span class="line">    head-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">// 返回头结点地址</span></span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="查看单链表结点地址">查看单链表结点地址</h2>
<p>无论是进行插入、删除、修改、查看都需要知道其进行操作的结点的地址，故将其抽象成一个函数。</p>
<p>由于单链表的存储空间不连续，所以它不能像顺序表那样通过位序号来定位其存储地址。</p>
<p>单链表是一种顺序存储的结构，即要取第 index 个结点的值，只能从头指针所指的结点开始沿着指针字段依次进行查找。若查询第 0 个结点，即头结点，则不进行遍历，直接返回头结点；若不是头结点，则需进行遍历并判断该结点是否存在。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回第 index 个结点的地址，若不存在则报错</span></span><br><span class="line"><span class="function">Node *<span class="title">locationOf</span><span class="params">(Node *head, <span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建临时结点以遍历链表</span></span><br><span class="line">    Node* temp = head;</span><br><span class="line">    <span class="keyword">if</span> (index != <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; index; i++) &#123;</span><br><span class="line">         <span class="keyword">if</span> (temp == <span class="literal">NULL</span>) &#123;</span><br><span class="line">          <span class="comment">// 判断插入位置是否有效(位置是否超过单链表长度）</span></span><br><span class="line">          <span class="built_in">printf</span>(<span class="string">&quot;Error - wrong location\n&quot;</span>);</span><br><span class="line">          <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="comment">// 指向下一个结点</span></span><br><span class="line">         temp = temp-&gt;next;</span><br><span class="line">       &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 返回第 index 个结点的地址</span></span><br><span class="line">    <span class="keyword">return</span> temp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表上的插入操作">单链表上的插入操作</h2>
<p>首先需查询要插入的地址是否存在，若存在，则为新建的结点动态申请内存空间，然后将新建结点添加到链表中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在第 index 个结点后插入结点</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insertById</span><span class="params">(Node *head, <span class="keyword">int</span> index, Elemtype elem)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 获得第 index 个结点的地址</span></span><br><span class="line">    Node* temp = locationOf(head, index);</span><br><span class="line">    <span class="comment">// 创建新的结点 c</span></span><br><span class="line">    Node *c = (Node*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    <span class="comment">// 若分配内存失败则报错</span></span><br><span class="line">    <span class="keyword">if</span> (c == <span class="literal">NULL</span>) &#123;</span><br><span class="line">       <span class="built_in">printf</span>(<span class="string">&quot;Error - unable to allocate required memory\n&quot;</span>);</span><br><span class="line">       <span class="built_in">exit</span>(<span class="number">-2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 插入新的结点</span></span><br><span class="line">    c-&gt;data = elem;</span><br><span class="line">    <span class="comment">// 新的结点连接原结点的后继结点</span></span><br><span class="line">    c-&gt;next = temp-&gt;next;</span><br><span class="line">    <span class="comment">// 原结点连接新的结点</span></span><br><span class="line">    temp-&gt;next = c;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表上的删除操作">单链表上的删除操作</h2>
<p>查询要删除结点的前驱结点的地址，并判断要删除结点是否存在，若存在，则记录要删除结点的后继结点的地址，将前驱结点的指针字段指向后继结点的地址；若不存在，则跳出。</p>
<p>结点被删除后，只表示将它从链表中断开而已，它仍占用着内存，必须要释放这个内存，否则会出现内存泄漏。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 删除第 index 个结点</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">deleteById</span><span class="params">(Node *head, <span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 获取第 index 个结点前驱结点的地址</span></span><br><span class="line">    Node *temp1 = locationOf(head, index - <span class="number">1</span>);</span><br><span class="line">    <span class="comment">// 记录第 index 个结点的地址供释放空间用</span></span><br><span class="line">    Node *temp2 = temp1-&gt;next;</span><br><span class="line">    <span class="comment">// 若第 index 个结点不存在,则函数结束</span></span><br><span class="line">    <span class="keyword">if</span> (temp2 == <span class="literal">NULL</span>)</span><br><span class="line">       <span class="keyword">return</span>;</span><br><span class="line">    <span class="comment">// 将第 index 个结点的前驱指点的指针指向其后继结点</span></span><br><span class="line">    temp1-&gt;next = temp2-&gt;next;</span><br><span class="line">    <span class="comment">// 释放第 index 个结点的内存空间</span></span><br><span class="line">    <span class="built_in">free</span>(temp2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表上的更新操作">单链表上的更新操作</h2>
<p>获取第 index 个结点的地址，若存在则修改该结点所存储的数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 更新第index个结点</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">updateById</span><span class="params">(Node *head, <span class="keyword">int</span> index, Elemtype elem)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 获取第 index 个结点的地址</span></span><br><span class="line">    Node* temp = locationOf(head, index);</span><br><span class="line">    <span class="comment">// 修改第 index 个结点的值</span></span><br><span class="line">    temp-&gt;data = elem;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表上的查找操作">单链表上的查找操作</h2>
<ol type="1">
<li>按序号查找</li>
</ol>
<p>获取第 index 个结点的地址，若存在则返回该结点所存储的数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 查看第 index 个结点并返回其值</span></span><br><span class="line"><span class="function">Elemtype <span class="title">selectById</span><span class="params">(Node *head, <span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 获取第 index 个结点的地址</span></span><br><span class="line">    Node* temp = locationOf(head, index);</span><br><span class="line">    <span class="comment">// 返回第 index 个结点的值</span></span><br><span class="line">    <span class="keyword">return</span> temp-&gt;data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol start="2" type="1">
<li>按值查找</li>
</ol>
<p>为获取该单链表上第一个与给定值 <code>elem</code> 相同的结点，从该单链表的头结点开始沿着后继指针一次对各结点的数据字段与 <code>elem</code> 进行比较，直到找到数据字段为 <code>elem</code> 的结点或到达该单链表的标为为止。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 按值进行查找，若存在则返回其 index，不存在返回 -1</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">indexOf</span><span class="params">(Node *head, Elemtype elem)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> index = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 创建临时结点以遍历链表</span></span><br><span class="line">    Node *temp = head;</span><br><span class="line">    <span class="comment">// 从头结点开始查找,直到 temp-&gt;data 为 elem 或到达链表的结尾</span></span><br><span class="line">    <span class="keyword">while</span> (temp != <span class="literal">NULL</span> &amp;&amp; temp-&gt;data != elem)</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="comment">// 指向下一个结点</span></span><br><span class="line">       temp = temp-&gt;next;</span><br><span class="line">       <span class="comment">// 计数器增加1</span></span><br><span class="line">       index++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 若 elem 在结点中，则返回其在单链表中的位置；若不存在，则返回 -1</span></span><br><span class="line">    <span class="keyword">if</span> (temp != <span class="literal">NULL</span>)</span><br><span class="line">       <span class="keyword">return</span> index;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">       <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表上的清空操作">单链表上的清空操作</h2>
<p>清空单链表，从首结点开始，先记录其后继结点的地址，后释放该结点的内存，后重复进行，直至到达尾结点为止。最后需将头结点的指针字段指向 <code>NULL</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 清空该链表</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    Node *p, *q;</span><br><span class="line">    <span class="comment">// 从头结点开始查找</span></span><br><span class="line">    p = head-&gt;next;</span><br><span class="line">    <span class="keyword">while</span> (p)</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="comment">// 记录其后继结点</span></span><br><span class="line">       q = p-&gt;next;</span><br><span class="line">       <span class="comment">// 释放该结点内存空间</span></span><br><span class="line">       <span class="built_in">free</span>(p);</span><br><span class="line">       <span class="comment">// 将后继结点赋值给 p</span></span><br><span class="line">       p = q;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 使头结点的指针指向 NULL</span></span><br><span class="line">    head-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表上的输出操作">单链表上的输出操作</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 输出该链表</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建临时结点以遍历链表</span></span><br><span class="line">    Node *temp = head;</span><br><span class="line">    <span class="comment">// 只要temp有后续结点就输出</span></span><br><span class="line">    <span class="keyword">while</span> (temp-&gt;next) &#123;</span><br><span class="line">       <span class="comment">// 指向下一个结点</span></span><br><span class="line">       temp = temp-&gt;next;</span><br><span class="line">       <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, temp-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="判断单链表是否为空">判断单链表是否为空</h2>
<p>因为采用的是带头结点的单链表，故只需判断其指针字段是否指向 <code>NULL</code>即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 判断该链表是否为空</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">isEmpty</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> head-&gt;next == <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="返回单链表的长度">返回单链表的长度</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回该单链表的长度</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">length</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> length = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 创建临时结点以遍历链表</span></span><br><span class="line">    Node *temp = head-&gt;next;</span><br><span class="line">    <span class="keyword">while</span> (temp)</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="comment">// 指向下一个结点</span></span><br><span class="line">       temp = temp-&gt;next;</span><br><span class="line">       <span class="comment">// 计数器增加1</span></span><br><span class="line">       length++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 返回该链表的长度</span></span><br><span class="line">    <span class="keyword">return</span> length;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>单链表</tag>
      </tags>
  </entry>
  <entry>
    <title>单链表（2）</title>
    <url>/post/singly-linked-list-2/</url>
    <content><![CDATA[<p>在完成上述<a href="./singly-linked-list-1">单链表的基本操作</a>后，可以利用单链表的特点完成下列操作：</p>
<blockquote>
<p>创建两个单链表的，排序后，合并成一个有序链表；并实现就地转置（即不利用额外空间实现转置）。</p>
</blockquote>
<span id="more"></span>
<p>因为后面有要求要对有序链表进行合并操作，使其仍然保持有序性，所以根据这一要求采用归并排序。</p>
<h2 id="单链表上的归并排序">单链表上的归并排序</h2>
<h3 id="归并操作">归并操作</h3>
<p>首先考虑下如何将将二个有序数列合并，只要从比较二个数列的第一个数，谁小就先取谁，取了后就在对应数列中删除这个数。然后再进行比较，如果有数列为空，那直接将另一个数列的数据依次取出即可。可以看出合并有序数列的效率是比较高的，可以达到 <span class="math inline">\(O(n)\)</span>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 输入两单链表的头结点，从小到大进行归并后，返回归并后的单链表头结点</span></span><br><span class="line"><span class="function">Node *<span class="title">merge</span><span class="params">(Node *head1, Node *head2)</span> </span>&#123;</span><br><span class="line">    Node *head = init();</span><br><span class="line">    <span class="keyword">int</span> length = <span class="number">0</span>, a, b;</span><br><span class="line">    <span class="comment">// 任一链表为空时，则表示剩余的结点中存储的数据皆比已归并的结点中存储数据大，故可直接拼接</span></span><br><span class="line">    <span class="keyword">while</span> (!isEmpty(head1) &amp;&amp; !isEmpty(head2))</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="comment">// 记录两单链表中首结点存储的数据</span></span><br><span class="line">       a = selectById(head1, <span class="number">1</span>);</span><br><span class="line">       b = selectById(head2, <span class="number">1</span>);</span><br><span class="line">       <span class="comment">// a，b两数，谁小谁就插入到新的单链表中，且删除原单链表中结点</span></span><br><span class="line">       <span class="keyword">if</span> (a &lt; b) &#123;</span><br><span class="line">         insertById(head, length, a);</span><br><span class="line">         deleteById(head1, <span class="number">1</span>);</span><br><span class="line">         length++;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">else</span> &#123;</span><br><span class="line">         insertById(head, length, b);</span><br><span class="line">         deleteById(head2, <span class="number">1</span>);</span><br><span class="line">         length++;</span><br><span class="line">       &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 拼接剩余元素</span></span><br><span class="line">    Node *temp = locationOf(head, length);</span><br><span class="line">    temp-&gt;next = isEmpty(head1)?locationOf(head2, <span class="number">1</span>):locationOf(head1, <span class="number">1</span>);</span><br><span class="line">    <span class="comment">// 释放原先的单链表的头结点所占用的空间</span></span><br><span class="line">    <span class="built_in">free</span>(head1);</span><br><span class="line">    <span class="built_in">free</span>(head2);</span><br><span class="line">    <span class="comment">// 返回进行归并操作后的新的单链表</span></span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="归并排序">归并排序</h3>
<p>再来看归并排序，其的基本思路就是将数组分成二组A、B，如果这二组组内的数据都是有序的，那么就可以很方便的将这二组数据进行排序。如何让这二组组内数据有序了？</p>
<p>可以将A、B组各自再分成二组。依次类推，当分出来的小组只有一个数据时，可以认为这个小组组内已经达到了有序，然后再合并相邻的二个小组就可以了。这样通过先递归的分解数列，再合并数列就完成了归并排序。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对单链表进行从小到大的归并排序</span></span><br><span class="line"><span class="function">Node *<span class="title">mergeSort</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 当单链表只有一个或没有结点时，可认为已达到组内有序，返回</span></span><br><span class="line">    <span class="keyword">if</span> (length(head) == <span class="number">0</span> || length(head) == <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="keyword">return</span> head;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 对单链表进行二分，拆分成两个单链表</span></span><br><span class="line">    Node *middleNode = locationOf(head, length(head) / <span class="number">2</span>);;</span><br><span class="line">    Node *head2 = init();</span><br><span class="line">    head2-&gt;next = middleNode-&gt;next;</span><br><span class="line">    Node *head1 = head;</span><br><span class="line">    middleNode-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">// 分别对这两个单链表进行归并排序</span></span><br><span class="line">    head1 = mergeSort(head1);</span><br><span class="line">    head2 = mergeSort(head2);</span><br><span class="line">    <span class="comment">// 对两个已经有序的单链表进行归并操作，合并成一个有序的单链表</span></span><br><span class="line">    <span class="keyword">return</span> merge(head1, head2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="单链表上的反转操作">单链表上的反转操作</h2>
<ol type="1">
<li>思路一</li>
</ol>
<p>平常较常使用的是线性顺序表进行存储，不需要考虑怎么进行反转，只需要按照存储地址逆序输出就好。在看到这道题目的第一想法就是，先将其转为顺序表后逆序生成单链表，但本题不允许使用额外空间，故考虑可否把所有结点的前后顺序进行反转。</p>
<p>基于此思路，使用3个指针遍历单链表，逐个结点进行反转。</p>
<p>利用两个结点指针和一个中间结点指针 <code>next</code>（用来记录当前结点的下一个结点的位置），分别指向当前结点和前一个结点，每次循环让当前结点的指针字段指向前一个结点即可，翻转结束后，需要为该单链表添加头结点，并释放原来的头结点所占据的内存。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对链表进行反转并返回新链表的头结点</span></span><br><span class="line"><span class="comment">// 使用3个指针遍历单链表，逐个结点进行反转</span></span><br><span class="line"><span class="function">Node *<span class="title">reverse</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    Node *curr = head-&gt;next;</span><br><span class="line">    Node *prev = <span class="literal">NULL</span>;</span><br><span class="line">    Node *next = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">while</span> (curr != <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">       next = curr-&gt;next;</span><br><span class="line">       curr-&gt;next = prev;</span><br><span class="line">       prev = curr;</span><br><span class="line">       curr = next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 添加头结点并动态分配内存</span></span><br><span class="line">    Node *newHead = init();</span><br><span class="line">    newHead-&gt;next = prev;</span><br><span class="line">    <span class="built_in">free</span>(head);</span><br><span class="line">    <span class="keyword">return</span> newHead;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol start="2" type="1">
<li>思路二</li>
</ol>
<p>除上一种思路外，还想出了一种思路更加易于理解。</p>
<p>从第 1 个结点到第 N 个结点，依次逐结点插入到头结点之后，即可完成反转。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//对链表进行反转并返回新链表的头结点</span></span><br><span class="line"><span class="comment">//从第 1 个结点到第 N 个结点，依次逐结点插入到头结点之后，即可完成反转</span></span><br><span class="line"><span class="function">Node *<span class="title">reverse1</span><span class="params">(Node *head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> l = length(head);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; l; i++)</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="keyword">int</span> temp = selectById(head, <span class="number">1</span>);</span><br><span class="line">       insertById(head, l, temp);</span><br><span class="line">       deleteById(head, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>为了方便理解，本方法使用了现成的方法，性能不高，存在较多的重复使用的现象，可以进一步改进。</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>单链表</tag>
        <tag>归并排序</tag>
      </tags>
  </entry>
  <entry>
    <title>SSH (Secure Shell) 原理与应用</title>
    <url>/post/ssh-tutorial/</url>
    <content><![CDATA[<p>Secure Shell（安全外壳协议，简称 SSH）是一种加密的网络传输协议，可在不安全的网络中为网络服务提供安全的传输环境。SSH 通过在网络中创建安全隧道来实现 SSH 客户端与服务器之间的连接。虽然任何网络服务都可以通过 SSH 实现安全传输，SSH最常见的用途是远程登录系统，人们通常利用 SSH 来传输命令行界面和远程执行命令。<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<span id="more"></span>
<h2 id="ssh-原理">SSH 原理</h2>
<p>SSH 使用对称密钥来加密整个连接。与某些用户所假设的相反，可以创建的公/私非对称密钥对仅用于身份验证，而不是加密连接。对称加密允许甚至密码身份验证免受窥探。</p>
<h2 id="ssh-安装">SSH 安装</h2>
<p>一般情况下，Linux 操作系统已默认安装了 SSH Client、SSH Server，若需要安装，可以通过 <code>yum</code> 或 <code>apt-get</code> 进行安装</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># CentOS</span></span><br><span class="line">yum install -y openssh-client openssh-server</span><br><span class="line"><span class="comment"># Ubuntu</span></span><br><span class="line">apt-get install -y openssh-client openssh-server</span><br></pre></td></tr></table></figure>
<blockquote>
<p>现在 Windows 下同样提供了原生的 SSH 服务，可参照 <span class="exturl" data-url="aHR0cHM6Ly9kb2NzLm1pY3Jvc29mdC5jb20vemgtY24vd2luZG93cy1zZXJ2ZXIvYWRtaW5pc3RyYXRpb24vb3BlbnNzaC9vcGVuc3NoX2luc3RhbGxfZmlyc3R1c2U=">安装适用于 Windows Server 2019 和 Windows 10 的 OpenSSH<i class="fa fa-external-link-alt"></i></span> 进行安装。</p>
</blockquote>
<h2 id="ssh-口令登录">SSH 口令登录</h2>
<p>执行如下命令即可使用 SSH 登陆本地服务器：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">ssh user@localhost</span><br></pre></td></tr></table></figure>
<h2 id="ssh-公钥登录">SSH 公钥登录</h2>
<p>但这样登陆是需要每次输入密码的，在后面我们有多个服务器需要进行登陆时，重复输入密码往往时一件比较费时费事的事情，所以我们需要配置成 SSH 无密码登陆。</p>
<p>下面以在本地生成 SSH 无密码登陆为例，进行示范，在本地配置多个服务器的 SSH 无密码登陆可类似执行。</p>
<p>首先输入 <code>exit</code> 退出刚才的 SSH ，就回到了我们原先的终端窗口，然后利用 <code>ssh-keygen</code> 生成密钥，并将密钥加入到授权中：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">exit</span></span><br><span class="line"><span class="built_in">cd</span> ~/.ssh/</span><br><span class="line">ssh-keygen -t rsa</span><br><span class="line">cat id_rsa.pub &gt;&gt; authorized_keys</span><br><span class="line">chmod 600 ./authorized_keys</span><br></pre></td></tr></table></figure>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly93d3cuaWV0Zi5vcmcvcmZjL3JmYzQyNTEudHh0">The Secure Shell (SSH) Protocol Architecture<i class="fa fa-external-link-alt"></i></span><a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>开发者手册</category>
      </categories>
  </entry>
  <entry>
    <title>支持向量机</title>
    <url>/post/support-vector-machine/</url>
    <content><![CDATA[<p>在机器学习中，支持向量机（英语：Support Vector Machine，常简称为 SVM，又名支持向量网络）是在分类与回归分析中分析数据的监督式学习模型与相关的学习算法。给定一组训练实例，每个训练实例被标记为属于两个类别中的一个或另一个，SVM训练算法创建一个将新的实例分配给两个类别之一的模型，使其成为非概率二元线性分类器。SVM模型是将实例表示为空间中的点，这样映射就使得单独类别的实例被尽可能宽的明显的间隔分开。然后，将新的实例映射到同一空间，并基于它们落在间隔的哪一侧来预测所属类别。</p>
<p>除了进行线性分类之外，SVM还可以使用所谓的核技巧有效地进行非线性分类，将其输入隐式映射到高维特征空间中。</p>
<p>当数据未被标记时，不能进行监督式学习，需要用非监督式学习，它会尝试找出数据到簇的自然聚类，并将新数据映射到这些已形成的簇。将支持向量机改进的聚类算法被称为支持向量聚类，当数据未被标记或者仅一些数据被标记时，支持向量聚类经常在工业应用中用作分类步骤的预处理。<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<span id="more"></span>
<p>通过上述对于支持向量机的描述，我们给出支持向量机的定义如下：</p>
<p>假设给定一个特征空间上的训练数据集</p>
<p><span class="math display">\[ T = \left\{\left(\vec{x_1},y_1\right), \left(\vec{x_2},y_2\right),\dots,\left(\vec{x_N},y_N\right)\right\} \]</span></p>
<p>其中，<span class="math inline">\(\vec{x_i} \in \mathcal{X} = R^n, y_i \in \mathcal{Y} =\left\{+1,-1\right\}, i=1,2,\dots,N\)</span>，<span class="math inline">\(\vec{x_i}\)</span> 为第 <span class="math inline">\(i\)</span> 个特征向量，也称为实例，<span class="math inline">\(y_i\)</span> 为 <span class="math inline">\(\vec{x_i}\)</span> 的类标记，当 <span class="math inline">\(y_i = +1\)</span>时，称 <span class="math inline">\(\vec{x_i}\)</span> 为正例；当 <span class="math inline">\(y_i = -1\)</span>时，称 <span class="math inline">\(\vec{x_i}\)</span> 为负例，<span class="math inline">\(\left(\vec{x_i},y_i\right)\)</span> 称为样本点。</p>
<p>学习的目标是在特征空间中找到一个分离超平面，能够将实例分到不同的类。分离超平面对应于方程 <span class="math inline">\(\vec{w}^T \vec{x} + b = 0\)</span>，它由法向量 <span class="math inline">\(\vec{w}\)</span> 和截距 <span class="math inline">\(b\)</span> 决定，可用 <span class="math inline">\(\left(\vec{w}, b\right)\)</span>表示。分离超平面将特征空间划分为两部分，一部分是正类，另一部分是负类。</p>
<h2 id="线性可分支持向量机">线性可分支持向量机</h2>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/svm-separating-hyperplanes.png" alt="" /><figcaption>存在多个划分超平面将两类训练样本分开</figcaption>
</figure>
<p>对于给定的训练数据集 <span class="math inline">\(T\)</span> 和超平面 <span class="math inline">\((\vec{w},b)\)</span>，定义超平面 <span class="math inline">\((\vec{w},b)\)</span> 关于样本点 <span class="math inline">\((\vec{x_i}, y_i)\)</span> 的函数间隔为</p>
<p><span class="math display">\[\hat{\gamma_i} = y_i \left( \vec{w}^T \vec{x_i} + b \right)\]</span></p>
<p>定义超平面 <span class="math inline">\((\vec{w},b)\)</span> 关于训练数据集 <span class="math inline">\(T\)</span> 的函数间隔为超平面 <span class="math inline">\((\vec{w},b)\)</span> 关于 <span class="math inline">\(T\)</span> 中所有样本点 <span class="math inline">\((\vec{x_i}, y_i)\)</span>的函数间隔之间的最小值，即</p>
<p><span class="math display">\[\hat{\gamma} = \min_{i=1,\dots,N} \hat{\gamma_i}\]</span></p>
<p>对于给定的训练数据集 <span class="math inline">\(T\)</span> 和超平面 <span class="math inline">\((\vec{w},b)\)</span>，定义超平面 <span class="math inline">\((\vec{w},b)\)</span> 关于样本点 <span class="math inline">\((\vec{x_i}, y_i)\)</span> 的几何间隔为</p>
<p><span class="math display">\[\gamma_i = \frac{y_i \left( \vec{w}^T \vec{x_i} + b \right)}{\left\| \vec{w} \right\|}\]</span></p>
<p>定义超平面 <span class="math inline">\((\vec{w},b)\)</span> 关于训练数据集 <span class="math inline">\(T\)</span> 的几何间隔为超平面 <span class="math inline">\((\vec{w},b)\)</span> 关于 <span class="math inline">\(T\)</span> 中所有样本点 <span class="math inline">\((\vec{x_i}, y_i)\)</span>的几何间隔之间的最小值，即</p>
<p><span class="math display">\[\gamma = \min_{i=1,\dots,N} \gamma_i\]</span></p>
<p><span class="math display">\[\max\hat{\gamma}, \quad \text{s.t.} \quad y_i \left( \vec{w}^T \vec{x_i} + b \right) \geq \hat{\gamma}, i=1,\dots,N\]</span></p>
<p><span class="math display">\[\max \frac{1}{2} {\left\| \vec{w} \right\|}^2, \quad \text{s.t.} \quad y_i \left( \vec{w}^T \vec{x_i} + b \right) \geq 1, i=1,\dots,N\]</span></p>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/support-vector-and-margin.png" alt="" /><figcaption>支持向量和间隔边界</figcaption>
</figure>
<h2 id="线性支持向量机">线性支持向量机</h2>
<h2 id="非线性支持向量机">非线性支持向量机</h2>
<h2 id="序列最小最优化算法">序列最小最优化算法</h2>
<h2 id="支持向量机的推广">支持向量机的推广</h2>
<h2 id="附件">附件</h2>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvZmlsZXMvc3VwcG9ydF92ZWN0b3JfbWFjaGluZS5wZGY=">Slide of Support Vector Machine<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvZmlsZXMv5pSv5oyB5ZCR6YeP5py66YCa5L+X5a+86K66LnBkZg==">支持向量机通俗导论——理解 SVM 的三重境界<i class="fa fa-external-link-alt"></i></span></li>
</ul>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly9lbi53aWtpcGVkaWEub3JnL3dpa2kvU3VwcG9ydC12ZWN0b3JfbWFjaGluZQ==">https://en.wikipedia.org/wiki/Support-vector_machine<i class="fa fa-external-link-alt"></i></span><a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>支持向量机</tag>
      </tags>
  </entry>
  <entry>
    <title>TF-IDF</title>
    <url>/post/tf-idf/</url>
    <content><![CDATA[<p>TF-IDF（英语：term frequency–inverse document frequency）是一种用于信息检索与文本挖掘的常用加权技术.<strong>TF-IDF是一种统计方法,用以评估一字词对于一个文件集或一个语料库中的其中一份文件的重要程度</strong>。字词的重要性随着它在文件中出现的次数成正比增加,但同时会随着它在语料库中出现的频率成反比下降。TF-IDF加权的各种形式常被搜索引擎应用,作为文件与用户查询之间相关程度的度量或评级。除了TF-IDF以外,互联网上的搜索引擎还会使用基于链接分析的评级方法,以确定文件在搜索结果中出现的顺序。<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<span id="more"></span>
<h2 id="原理">原理</h2>
<p>在一份给定的文件里,词频（term frequency,tf）指的是某一个给定的词语在该文件中出现的频率。这个数字是对词数（term count）的归一化,以防止它偏向长的文件。（同一个词语在长文件里可能会比短文件有更高的词数,而不管该词语重要与否。）对于在某一特定文件里的词语<span class="math inline">\(t_i\)</span>来说,它的重要性可表示为：</p>
<p><span class="math display">\[tf_{i,j}=\frac{n_{i,j}}{\sum_{k}n_{k,j}}\]</span></p>
<p>以上式子中<span class="math inline">\(n_{i,j}\)</span>是该词在文件<span class="math inline">\(d_j\)</span>中的出现次数,而分母则是在文件<span class="math inline">\(d_j\)</span>中所有字词的出现次数之和。</p>
<p>逆向文件频率（inverse document frequency,idf）是一个词语普遍重要性的度量。某一特定词语的idf,可以由总文件数目除以包含该词语之文件的数目,再将得到的商取以10为底的对数得到：</p>
<p><span class="math display">\[idf_{i} =\lg {\frac {|D|}{|\{j:t_{i}\in d_{j}\}|}}\]</span></p>
<p>其中：</p>
<ul>
<li><span class="math inline">\(|D|\)</span>：语料库中的文件总数</li>
<li><span class="math inline">\(|\{j:t_{i}\in d_{j}\}|\)</span>：包含词语<span class="math inline">\(t_i\)</span>的文件数目（即<span class="math inline">\(n_{i,j}\neq 0\)</span>的文件数目）如果词语不在数据中,就导致分母为零,因此一般情况下使用 <span class="math inline">\(1+|\{j:t_{i}\in d_{j}\}|\)</span></li>
</ul>
<p>然后</p>
<p><span class="math display">\[tf{}idf_{i,j}=tf_{i,j}\times idf_{i}\]</span></p>
<p>某一特定文件内的高词语频率,以及该词语在整个文件集合中的低文件频率,可以产生出高权重的tf-idf.因此,tf-idf倾向于过滤掉常见的词语,保留重要的词语.<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a></p>
<h2 id="举例">举例</h2>
<p>在某个一共有一千词的网页中“原子能”、“的”和“应用”分别出现了 2 次、35 次 和 5 次,那么它们的词频就分别是 0.002、0.035 和 0.005. 我们将这三个数相加,其和 0.042 就是相应网页和查询“原子能的应用” 相关性的一个简单的度量.概括地讲,如果一个查询包含关键词 <span class="math inline">\(w_1,w_2,\dots,w_N\)</span>, 它们在一篇特定网页中的词频分别是: <span class="math inline">\(TF_1, TF_2,\dots, TF_N\)</span>.（TF: term frequency). 那么,这个查询和该网页的相关性就是：<span class="math inline">\(TF_1, TF_2,\dots, TF_N\)</span>.<a href="#fn3" class="footnote-ref" id="fnref3" role="doc-noteref"><sup>3</sup></a></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">tf = <span class="number">2</span>/<span class="number">1000</span>+<span class="number">35</span>/<span class="number">1000</span>+<span class="number">5</span>/<span class="number">1000</span></span><br><span class="line">tf</span><br></pre></td></tr></table></figure>
<p>我们很容易发现,如果一个关键词只在很少的网页中出现,我们通过它就容易锁定搜索目标,它的权重也就应该大.反之如果一个词在大量网页中出现,我们看到它仍然不是很清楚要找什么内容,因此它应该小.概括地讲,假定一个关键词 <span class="math inline">\(w\)</span> 在 <span class="math inline">\(D_w\)</span> 个网页中出现过,那么 <span class="math inline">\(D_w\)</span> 越大,<span class="math inline">\(w\)</span> 的权重越小,反之亦然.在信息检索中,使用最多的权重是“逆文本频率指数” （Inverse document frequency 缩写为IDF）.利用 IDF,上述相关性计算的公式就由词频的简单求和变成了加权求和,即：</p>
<p><span class="math display">\[TF_1\times IDF_1 + TF_2\times IDF_2 +... + TF_N\times IDF_N\]</span></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> math</span><br><span class="line">tf_idf = <span class="number">2</span>/<span class="number">1000</span>*math.log2(<span class="number">1e9</span>/<span class="number">2e6</span>) + <span class="number">35</span>/<span class="number">1000</span>*math.log2(<span class="number">1e9</span>/<span class="number">1e9</span>) + <span class="number">5</span>/<span class="number">1000</span>*math.log2(<span class="number">1e9</span>/<span class="number">5e8</span>)</span><br><span class="line">tf_idf</span><br></pre></td></tr></table></figure>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly96aC53aWtpcGVkaWEub3JnL3cvaW5kZXgucGhwP3RpdGxlPVRmLWlkZiZhbXA7b2xkaWQ9NDg1MTgwMzY=">https://zh.wikipedia.org/w/index.php?title=Tf-idf&amp;oldid=48518036<i class="fa fa-external-link-alt"></i></span><a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly96aC53aWtpcGVkaWEub3JnL3cvaW5kZXgucGhwP3RpdGxlPVRmLWlkZiZhbXA7b2xkaWQ9NDg1MTgwMzY=">https://zh.wikipedia.org/w/index.php?title=Tf-idf&amp;oldid=48518036<i class="fa fa-external-link-alt"></i></span><a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn3" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly9iYWlrZS5iYWlkdS5jb20vaXRlbS90Zi1pZGYvODgxNjEzND9mcj1hbGFkZGlu">https://baike.baidu.com/item/tf-idf/8816134?fr=aladdin<i class="fa fa-external-link-alt"></i></span><a href="#fnref3" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>自然语言处理</category>
      </categories>
      <tags>
        <tag>TF-IDF</tag>
      </tags>
  </entry>
  <entry>
    <title>《数学之美》</title>
    <url>/post/the-beauty-of-mathematics/</url>
    <content><![CDATA[<p>本文讲述数学如何应用于互联网行业，改变人们的日常生活，集中阐述了作者对数学和信息处理这些专业学科的理解。主要围绕作者所从事的工作——搜索引擎进行展开，包括以下几个方面：</p>
<ul>
<li>自然语言处理</li>
<li>搜索引擎</li>
<li>其他数学模型</li>
<li>数学建模思想</li>
</ul>
<p>本书未泛泛而谈数学于生活的意义，而是通过一个个应用并改变生活的实例，说明本科阶段所学的各种基础数学理论知识并非枯燥无用，而是可以实实在在解决现实问题的。本书并未通过通篇的数学公式来解释问题，而是以一种科普的方式通过具体的例子使读者学到如何用数学去思考、去解决问题，如何跳出固有的思维去创新，同时为想要进一步钻研的人以参考文献和延伸阅读的形式留了足够的思考深度。 <span id="more"></span></p>
<h2 id="思维导图">思维导图</h2>
<figure>
<img data-src="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/images/the-beauty-of-mathematics-mind-map.png" alt="" /><figcaption>思维导图</figcaption>
</figure>
<h2 id="原文摘录">原文摘录</h2>
<p>基于统计的自然语言处理方法，在数学模型上和通信是相通的，甚至就是相同的。因此，在数学意义上自然语言处理又和语言的初衷——通信联系在一起了。但是，科学家们认识到这个联系却花了几十年的时间。</p>
<p>古德-图灵估计（Good-Turing Estimate）：对于没有看见的事件，我们不能认为它的发生概率就是零，因此我们从概率的总量中，分配一个很小的概率给予这些没有看见的事件。这样一来，看见的时间的概率总和就要小于 1 了。至于小多少，要根据“越是不可信的统计折扣越多”的方法进行。</p>
<ul>
<li>训练语料和模型应用的领域须保持一致，以减少噪音。</li>
<li>语料数据通常是越多越好，可以利用平滑过渡方法解决小/零概率事件。</li>
<li>最好能预处理能具有规律的、量比较大的噪音。</li>
</ul>
<p>信息是消除系统不确定性的唯一办法。 合理利用信息，而不是玩弄什么公式和机器学习算法，是做好搜索的关键。</p>
<p>在计算机科学领域，一个好的算法应该像 AK-47 冲锋枪那样：简单、有效、可靠性好且容易读懂（或者说易操作），而不应该是故弄玄虚。</p>
<p>先解决 80% 的问题，再慢慢解决剩下的 20% 问题。一开始就追求大而全的方案，到最后大多不了了之。</p>
<p>选取简单方案的一个原因在于易于解释每一步骤和方法背后的道理，便于差错和找到今后改进目标。在改进时要知其所以然，说不清理由的改进，即使看上去有效也不应采用，否则将来可能成为隐患。</p>
<ol type="1">
<li>一个正确的数学模型应当在形式上是简单的。</li>
<li>一个正确的模型一开始可能还不如一个精雕细琢过的错误模型来的准确，但是，如果我们认定大方向是对的，就应该坚持下去。</li>
<li>大量准确的数据对研发很重要。</li>
<li>正确的模型也可能受噪音干扰，而显得不准确; 这时不应该用一种凑合的修正方法来弥补它，而是要找到噪音的根源，这也许能通往重大的发现。</li>
</ol>
<h2 id="读后感">读后感</h2>
<h3 id="数学到底有什么用">数学到底有什么用</h3>
<p>数学这门学科经过几千年的抽象化，与我们的日常生活似乎是渐行渐远，很多人在大学中所学的数学在毕业后难以有机会去应用，对于数学的印象可能只剩下抽象的数字、符号、公式和定理。</p>
<p>其实，数学的用途远超人们的想象，甚至可以说数学在生活中无处不在。不止在与日常生活联系较少的原子能和航天领域应用到大量的数学知识，更在我们天天用到的产品和技术，其背后也蕴藏着支持他们的数学基础。对于本科学习统计的我来说，在了解到《数学之美》前也是如此，难以想象本科所学到的数理知识竟可以通过如此一种巧妙的转换应用于实际解决一个又一个初看似乎无法解决的问题。</p>
<p>数学可以用一些通用的模型来描述一些看似不同的实际问题，并能够给出非常漂亮的解决方法。比如，在语音识别、机器翻译和自然语言处理等领域中，虽然人类的语言有成百上千种，但处理它们的数学模型却是相同的或相似的。</p>
<p>这种在不同问题上方法的一致性似乎可以回答“数学到底有什么用？”这个问题，<strong>数学可以让我们抓住做事的规律</strong>，数学能够帮助我们发现仅凭经验无法发现的规律，找到仅凭经验无法总结出来的方法。</p>
<h3 id="数学之美即简单之美">数学之美即简单之美</h3>
<p>在这本书中，除了具体到问题的数学模型以及相关知识外，给予我最深印象的就是作者所提到的数学模型本身的高度简洁及具有概括力，一个极为复杂的现象也可以用一个简单的数学模型来进行描述（虽然数学实际应用起来可能比较复杂，但其背后所蕴含的思想却是相对简单的）。</p>
<p>无数科学理论无不是沿着这个方向进行发展。比如说，如何描述天体的运动轨迹，从两千多年前托勒密所提出的地心说——通过 40 - 60 个小圆套大圆的方法计算出所有行星的运动轨迹，到哥白尼提出由 8 - 10 个圆描述行星运动的日心说，再到开普勒提出的三个定律，仅需要一个椭圆就可以将星体的运动规律描述清楚。正如爱因斯坦所说：“从希腊哲学到现代物理学的整个科学史中，不断有人力图把表面上极为复杂的自然现象归结为几个简单的基本概念和关系。”我非常羡慕这些可以将“简单”哲学贯彻到底的人，能将复杂的问题一步步简化，并在实用性和完备性中找到那个完美的中间点。</p>
<p>在生产领域也同样应该追求这种简单之美。在实际解决问题的时候，有时候靠瞎凑也能够得到一个凑合可用的结果，但长期来看维护这些瞎凑搭起来的东西代价非常巨大，不仅结构混乱丑陋，而且由于说不清瞎凑背后的道理，在以后的修改维护时也根本无从下手；反之，如果从更高的数学模型层面去抽象问题，去寻找一个正确的模型框架，就可以有条理地慢慢去填充细节，逐渐达到完善。这样的解决方案不仅能达到需求，而且结构清晰道理明了，便于日后的维护和修正。正如作者在后记里是这样说明他的写作意图的：“我更希望让做工程的年轻人看到信息技术行业正确的做事情的方法。”</p>
<h3 id="追求简单有效的解决方法">追求简单有效的解决方法</h3>
<p>在作者对美国工程院士阿米特·辛格博士的介绍中，提到了这样一种处世原则——追求简单有效的解决方法。</p>
<p>这种做事情的原则十分值得我们借鉴，即先帮用户解决主要的问题，再决定要不要纠结在次要的部分上；要知道修改代码的所作所为，知其所以然；能用简单方法解决就用简单的，可读性很重要。正如一个漂亮的 PageRank 矩阵乘法迭代加上一个 TF-IDF 公式，就可以大程度地改善搜索结果的质量，无一不体现出简单即是美的特点。许多失败并不是因为人不优秀，而是做事情的方法不对，一开始追求大而全的解决方案，之后长时间不能完成，最后不了了之。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
  </entry>
  <entry>
    <title>线索二叉树</title>
    <url>/post/thread-binary-tree/</url>
    <content><![CDATA[<p>二叉树的遍历本质上是将一个复杂的非线性结构转换为线性结构，使每个结点都有了唯一前驱和后继（第一个结点无前驱，最后一个结点无后继）。对于二叉树的一个结点，查找其左右子女是方便的，其前驱后继只有在遍历中得到。为了容易找到前驱和后继，有两种方法。一是在结点结构中增加向前和向后的指针，这种方法增加了存储开销，不可取；二是利用二叉树的空链指针，即线索二叉树。</p>
<span id="more"></span>
<ul>
<li>利用线索二叉树进行中序遍历时，不必采用堆栈处理，速度较一般二叉树的遍历速度快，且节约存储空间</li>
<li>任意一个结点都能直接找到它的前驱和后继结点</li>
</ul>
<h2 id="线索二叉树结点类">线索二叉树结点类</h2>
<p>线索二叉树的结点类的描述如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadBinaryTreeNode</span> </span>&#123;</span><br><span class="line">    <span class="comment">//数据域</span></span><br><span class="line">    <span class="keyword">public</span> Object data;</span><br><span class="line">    <span class="comment">//左指针域</span></span><br><span class="line">    ThreadBinaryTreeNode left;</span><br><span class="line">    <span class="comment">//右指针域</span></span><br><span class="line">    ThreadBinaryTreeNode right;</span><br><span class="line">    <span class="comment">//左指针域类型  false：指向子节点、true：前驱或后继线索</span></span><br><span class="line">    <span class="keyword">boolean</span> isLeftThread;</span><br><span class="line">    <span class="comment">//右指针域类型  false：指向子节点、true：前驱或后继线索</span></span><br><span class="line">    <span class="keyword">boolean</span> isRightThread;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadBinaryTreeNode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(<span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadBinaryTreeNode</span><span class="params">(Object data)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(data, <span class="keyword">null</span>, <span class="keyword">null</span>, <span class="keyword">false</span>, <span class="keyword">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadBinaryTreeNode</span><span class="params">(Object data, ThreadBinaryTreeNode left, ThreadBinaryTreeNode right)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(data, left, right, <span class="keyword">false</span>, <span class="keyword">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadBinaryTreeNode</span><span class="params">(Object data, ThreadBinaryTreeNode left, ThreadBinaryTreeNode right, <span class="keyword">boolean</span> isLeftThread, <span class="keyword">boolean</span> isRightThread)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">        <span class="keyword">this</span>.left = left;</span><br><span class="line">        <span class="keyword">this</span>.right = right;</span><br><span class="line">        <span class="keyword">this</span>.isLeftThread = isLeftThread;</span><br><span class="line">        <span class="keyword">this</span>.isRightThread = isRightThread;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="线索二叉树线索二叉树类">线索二叉树线索二叉树类</h2>
<p>线索二叉树的线索二叉树的描述如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadBinaryTree</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> ThreadBinaryTreeNode root;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadBinaryTree</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.root = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadBinaryTree</span><span class="params">(ThreadBinaryTreeNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.root = root;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="由二叉树创建线索二叉树">由二叉树创建线索二叉树</h2>
<p>如果，我们需要从二叉树类复制为线索二叉树，可以通过对二叉树的遍历的方法来进行复制，此处通过将二叉树链式存储结构先转换为顺序存储结构，再由顺序存储结构创建线索二叉树的方法创建线索二叉树。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 由二叉树创建线索二叉树</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ThreadBinaryTree</span><span class="params">(BinaryTree tree)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (tree != <span class="keyword">null</span>) &#123;</span><br><span class="line">        Object[] sequence = tree.toSequence();</span><br><span class="line">        <span class="keyword">this</span>.root = createFromSequence(sequence);</span><br><span class="line">        preThreadOrder(<span class="keyword">this</span>.root);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> ThreadBinaryTreeNode <span class="title">createFromSequence</span><span class="params">(Object[] sequence)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> createFromSequence(sequence, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> ThreadBinaryTreeNode <span class="title">createFromSequence</span><span class="params">(Object[] sequence, <span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">    ThreadBinaryTreeNode node = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">if</span> (index &lt; sequence.length) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!sequence[index].toString().equals(<span class="string">&quot; &quot;</span>)) &#123;</span><br><span class="line">            node = <span class="keyword">new</span> ThreadBinaryTreeNode(sequence[index]);</span><br><span class="line">            node.left = createFromSequence(sequence, index * <span class="number">2</span> + <span class="number">1</span>);</span><br><span class="line">            node.right = createFromSequence(sequence, index * <span class="number">2</span> + <span class="number">2</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            node = <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>由于线索化的实质是将二叉链表中的空指针改为指向前驱或后继的线索，而前驱或后继的信息只有在遍历时才能得到，因此线索化的过程即为在遍历的过程中修改空指针的过程。为了下遍历过程中访问结点的先后关系，设置了一个指针指向刚刚访问过的结点。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * 前序线索化二叉树</span></span><br><span class="line"><span class="comment">  */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//线索化时记录前一个节点</span></span><br><span class="line"><span class="keyword">private</span> ThreadBinaryTreeNode preNode;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">preThreadOrder</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.preThreadOrder(root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">preThreadOrder</span><span class="params">(ThreadBinaryTreeNode node)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (node == <span class="keyword">null</span>) <span class="keyword">return</span>;</span><br><span class="line">    <span class="comment">//左指针为空，将左指针指向前驱节点</span></span><br><span class="line">    <span class="keyword">if</span> (node.left == <span class="keyword">null</span>) &#123;</span><br><span class="line">        node.left = preNode;</span><br><span class="line">        node.isLeftThread = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//前一个节点的后继节点指向当前节点</span></span><br><span class="line">    <span class="keyword">if</span> (preNode != <span class="keyword">null</span> &amp;&amp; preNode.right == <span class="keyword">null</span>) &#123;</span><br><span class="line">        preNode.right = node;</span><br><span class="line">        preNode.isRightThread = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    preNode = node;</span><br><span class="line">    <span class="keyword">if</span> (!node.isLeftThread) &#123;</span><br><span class="line">        preThreadOrder(node.left);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!node.isRightThread) &#123;</span><br><span class="line">        preThreadOrder(node.right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="非递归地输出先序遍历序列">非递归地输出先序遍历序列</h2>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 前序遍历线索二叉树（按照后继线索遍历）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">preThreadList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ThreadBinaryTreeNode node = root;</span><br><span class="line">    <span class="keyword">while</span> (node != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">while</span> (!node.isLeftThread) &#123;</span><br><span class="line">            System.out.print(node.data);</span><br><span class="line">            node = node.left;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.print(node.data);</span><br><span class="line">        node = node.right;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="线索二叉树测试类">线索二叉树测试类</h2>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"><span class="keyword">import</span> org.junit.Before;</span><br><span class="line"><span class="keyword">import</span> org.junit.After;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * ThreadBinaryTree Tester.</span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadBinaryTreeTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testCreateThreadBinaryTreeSequence</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        BinaryTree binaryTree = <span class="keyword">new</span> BinaryTree(<span class="string">&quot;ABDEGCFH&quot;</span>, <span class="string">&quot;DBGEAFHC&quot;</span>);</span><br><span class="line">        ThreadBinaryTree threadBinaryTree = <span class="keyword">new</span> ThreadBinaryTree(binaryTree);</span><br><span class="line">        threadBinaryTree.preThreadList();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ABDEGCFH</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>二叉树</tag>
        <tag>线索二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>The TikZ Gallery</title>
    <url>/post/tikz-gallery/</url>
    <content><![CDATA[<div class="pdf-container" data-target="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/files/mp-neuron-model.pdf" data-height="500px"></div>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvbXAtbmV1cm9uLW1vZGVsLnRleA==">Source Code<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
<span id="more"></span>
<div class="pdf-container" data-target="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/files/bp-neural-network-example.pdf" data-height="500px"></div>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvYnAtbmV1cmFsLW5ldHdvcmstZXhhbXBsZS50ZXg=">Source Code<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
<div class="pdf-container" data-target="https://signorinoy.oss-cn-beijing.aliyuncs.com/upload/files/multi-layer-feedforward-neural-networks.pdf" data-height="500px"></div>
<blockquote class="blockquote-center">
<p><span class="exturl" data-url="aHR0cHM6Ly9zaWdub3Jpbm95Lm9zcy1jbi1iZWlqaW5nLmFsaXl1bmNzLmNvbS91cGxvYWQvY29kZXMvbXVsdGktbGF5ZXItZmVlZGZvcndhcmQtbmV1cmFsLW5ldHdvcmtzLnRleA==">Source Code<i class="fa fa-external-link-alt"></i></span></p>

</blockquote>
]]></content>
      <categories>
        <category>闲来无事</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
        <tag>TikZ</tag>
      </tags>
  </entry>
  <entry>
    <title>并查集</title>
    <url>/post/union-find/</url>
    <content><![CDATA[<div class="note info"><ul>
<li>课程名称：Algorithms, Part Ⅰ</li>
<li>课程地址：<span class="exturl" data-url="aHR0cHM6Ly93d3cuY291cnNlcmEub3JnL2xlYXJuL2FsZ29yaXRobXMtcGFydDEv">https://www.coursera.org/learn/algorithms-part1/<i class="fa fa-external-link-alt"></i></span></li>
</ul>
</div>
<p>在计算机科学中，并查集是一种树型的数据结构，用于处理一些不交集（Disjoint Sets）的合并及查询问题。由于支持这两种操作，一个不相交集也常被称为联合-查找数据结构（union-find data structure）或合并-查找集合（merge-find set）。<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<span id="more"></span>
<h2 id="问题描述">问题描述</h2>
<p>在给定 N 个元素的集合中，使其支持如下两个操作：</p>
<ul>
<li>Find：确定元素属于哪一个子集，用来确定两个元素是否属于同一子集。</li>
<li>Union：将两个子集合并成同一个集合。</li>
</ul>
<h2 id="问题建模">问题建模</h2>
<p>对于连接，从数学上看，是一个“等价”关系。若用 <span class="math inline">\(\equiv\)</span> 表示集合上的连接关系，那么对于该集合中的任意对象 <span class="math inline">\(p\)</span>，<span class="math inline">\(q\)</span>，<span class="math inline">\(r\)</span>，具有如下性质：</p>
<ul>
<li>自反性：<span class="math inline">\(p \equiv p\)</span>。</li>
<li>对称性：若 <span class="math inline">\(p \equiv q\)</span>，则 <span class="math inline">\(q \equiv p\)</span>。</li>
<li>传递性：若 <span class="math inline">\(p \equiv q\)</span> 且 <span class="math inline">\(q \equiv r\)</span>，则 <span class="math inline">\(p \equiv r\)</span>。</li>
</ul>
<p>当我们得到“等价”关系后，一个元素以及其连接的集合所构成的子集就称为连通分量。</p>
<ul>
<li>连通分量：互相连接的元素的最大集合。</li>
</ul>
<p>连通分量具有如下性质：</p>
<ul>
<li>连通分量中的任意两个元素都是连接的，连通分量中的元素不与连通分量之外的元素相连接。</li>
</ul>
<p>我们通过维护连通分量来实现并查集的操作，Find 就是检查两个元素是否在同一个连通分量中，Union 就是将包含两个元素的连通分量替换为其并集。</p>
<p>我们得到并查集的 API 设计如下：</p>
<ul>
<li>具有巨大的对象数量与操作数量。</li>
<li>具有大量的合并与连接查询操作。</li>
</ul>
<table>
<thead>
<tr class="header">
<th style="text-align: center;">函数名称</th>
<th style="text-align: center;">函数功能</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;"><code>UnionFind(int N)</code></td>
<td style="text-align: center;">初始化具有 N 个元素的并查集数据结构</td>
</tr>
<tr class="even">
<td style="text-align: center;"><code>void union(int p, int q)</code></td>
<td style="text-align: center;">在 p 与 q 元素之间添加连接</td>
</tr>
<tr class="odd">
<td style="text-align: center;"><code>int find(int p)</code></td>
<td style="text-align: center;">查找 p 的连通向量 id</td>
</tr>
<tr class="even">
<td style="text-align: center;"><code>boolean connected(int p, int q)</code></td>
<td style="text-align: center;">p 和 q 是否存在于同一个连通分量</td>
</tr>
<tr class="odd">
<td style="text-align: center;"><code>int count()</code></td>
<td style="text-align: center;">连通向量的数目</td>
</tr>
</tbody>
</table>
<h2 id="算法实现">算法实现</h2>
<p>为实现这份 API，所有的实现都应该：</p>
<ul>
<li>定义一种数据结构表示已知的连接；</li>
<li>基于此数据结构实现高效的 API 方法。</li>
</ul>
<p>因此：</p>
<ul>
<li>我们将元素作为索引的数组 <code>id[]</code> 作为基础数据结构表示所有连通分量， 在开始时，每个元素都构成一个仅含有自己的连通分量，因此我们将 <code>id[i]</code> 的值初始化为 <code>i</code>，其中 <span class="math inline">\(0 \leq i \leq N\)</span>。</li>
<li><code>find(p)</code> 方法返回元素所在的连通分量的 <code>id</code>，<code>connected(p, q)</code> 方法判断两个元素所在的连通分量 <code>id</code> 是否相同（<code>find(p) == find(q)</code>），来判断两元素是否存在于同一个连通分量之中。</li>
<li><code>count()</code> 方法会返回所有连通分量的数量，在开始时，有 N 个连通分量，每次对两个连通分量进行归并操作（<code>union(p, q)</code>）时会使分量总数减一。</li>
</ul>
<p>根据以上思路，实现了 <code>UnionFind</code> 的基本结构，如下所示（<code>find(p)</code> 与 <code>union(p, q)</code> 方法将在其后进行讨论）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UnionFind</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] id; <span class="comment">// 连通分量 id (以元素作为索引)</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count; <span class="comment">// 连通分量数量</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> class <span class="title">UnionFind</span><span class="params">(<span class="keyword">int</span> N)</span> </span>&#123;</span><br><span class="line">        count = N;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">            id[i] = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">    <span class="keyword">public</span> <span class="keyword">int</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> p)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">    <span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">connected</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> find(p) == find(q);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">count</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="quick-find-算法">quick-find 算法</h3>
<p>最简单的一种方法为保证在同一个连通分量中的所有元素在 <code>id[]</code> 中的值全部相同，即保证 <code>id[p]</code> 等于 <code>id[q]</code> 时 p 和 q 是连通的。</p>
<p>也就是说，<code>connected(p, q)</code> 只需要判断 <code>id[p] == id[q]</code>，因此，<code>find(p)</code> 返回元素 p 所在连通分量的索引 <code>id[p]</code>。同时，为确保这一点，<code>union(p, q)</code> 首先需要检查元素 p 和 q 是否已经存在于同一个分量之中，若是，就不需要采取任何操作，否则，需要将元素 p 和 q 所属的连通分量中的所有元素的 <code>id[]</code> 值修改为同一个值，因此，我们可遍历整个数组，将所有和 <code>id[p]</code> 相等的元素的值修改为 <code>id[q]</code> 的值，归并后，连通分量数目减一。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> id[p];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> pId = find(p);</span><br><span class="line">    <span class="keyword">int</span> qId = find(q);</span><br><span class="line">    <span class="keyword">if</span> (pId == qId) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; id.length; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (id[i] == pId) &#123;</span><br><span class="line">            id[i] = qId;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    count--;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>对于 quick-find 算法，<code>find()</code> 操作速度是很快的，只需要访问 <code>id[]</code> 数组一次，但对于 <code>union()</code> 来说，每一次操作都需要扫描整个 <code>id[]</code> 数组，因此，该算法无法处理大型问题。</p>
<h3 id="quick-union-算法">quick-union 算法</h3>
<p>每一个元素所对应的 <code>id[]</code> 都是同一个分量中的另一个元素的名称，我们称之为“链接”。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(p != id[p]) &#123;</span><br><span class="line">        p = id[p];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> pRoot = find(p);</span><br><span class="line">    <span class="keyword">int</span> qRoot = find(q);</span><br><span class="line">    <span class="keyword">if</span> (pRoot == qRoot) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    id[pRoot] = qRoot;</span><br><span class="line">    count--;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="加权-quick-union-算法">加权 quick-union 算法</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(p != id[p]) &#123;</span><br><span class="line">        p = id[p];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = find(p);</span><br><span class="line">    <span class="keyword">int</span> j = find(q);</span><br><span class="line">    <span class="keyword">if</span> (i == j) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (sz[i] &lt; sz[j]) &#123;</span><br><span class="line">        id[i] = j;</span><br><span class="line">        sz[j] += sz[i];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        id[j] = i;</span><br><span class="line">        sz[i] += sz[j];</span><br><span class="line">    &#125;</span><br><span class="line">    count--;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p><span class="exturl" data-url="aHR0cHM6Ly96aC53aWtpcGVkaWEub3JnL3dpa2kvJUU1JUI5JUI2JUU2JTlGJUE1JUU5JTlCJTg2">https://zh.wikipedia.org/wiki/并查集<i class="fa fa-external-link-alt"></i></span><a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>Coursera</tag>
        <tag>并查集</tag>
      </tags>
  </entry>
</search>
